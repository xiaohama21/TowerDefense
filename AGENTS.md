# AGENTS.md — 开发约定（Codex 记忆）

本文件是仓库级开发约定，所有 Codex 会话必须遵守。项目为《烽火连营·三国塔防》Godot 塔防，总纲见 `docs/GAME_DESIGN.md`，模块文档见 `docs/modules/`。

## 1. 设计文档同步（防缺失 / 找不到 / 未关联）

1. **先文档后代码**：任何功能/设计改动必须先同步对应设计文档（总纲或对应模块 `docs/modules/*.md`），再进入开发。
2. **模块索引强制登记**：新增模块文档必须在总纲 0.1「模块文档索引」登记（文档/职责/承载原章节），否则视为未关联；修改既有模块职责时同步更新索引行。
3. **总纲回写**：跨模块或影响总纲结论的变更必须回写总纲，并递增总纲版本号（vX.Y.Z）+ 顶部变更行；README「当前状态」的文档版本同步递增。
4. **历史 changelog 保留**：版本变更行保留历史原貌（如旧职业名、旧技能名），只更新正文与当前状态区。
5. 带独立版本号的模块文档（如 SKILLS.md / CHARACTER_SKILLS.md / UI_LAYOUT.md / ENCYCLOPEDIA.md），内容变更时同步升版并记录变更。

## 2. Bug 修复记录（强制）

1. 每次 bug 修复必须在 `docs/modules/BUGS.md` 新增条目，包含：问题描述、复现步骤、根因、处理方式、**关联版本号**（文档版本 + 程序版本）。
2. 修复若改变设计结论，同步回写对应模块文档与总纲（按第 1 节流程）。
3. 历史 bug 以总纲 changelog 与 BUGS.md 回填条目为准；自 v0.27.6 起所有修复强制登记。

## 3. 其他

- 提交信息格式：`类型(范围): 摘要`，如 `docs(v0.27.5): ...`、`feat(v0.27.0): ...`、`fix(v0.21.1): ...`。
- 推送到 GitHub 直连常超时，使用本地代理：`git -c http.proxy=http://127.0.0.1:7897 push`。

## 4. 提交编号（整数顺延，禁止字母后缀）

1. 阶段内提交编号一律整数顺延（1、2、3…），**禁止字母后缀拆分**（如 6a / 6b / 7a），否则会污染游戏版本号（程序版本 = 0.阶段.提交.修复，如 0.8.6.0 = 阶段 8·提交 6·修复 0）。
2. 改动过大需要拆分时，编号直接按下一个整数顺延（如 5、6、7…），不得引入字母或子编号。

## 5. 分支规范（阶段 8·提交 7 起生效）

> 红线速记：**开发完成 ≠ 合并 main**。main 的唯一合并入口 = **启动下一个编号提交时**（见 5.3）；修复/延伸一律在版本分支提交；任何会话收尾时禁止把版本分支内容合并/推送到 main（文档先行除外，见 5.6）。

1. **一个开发提交 = 一个分支**：每个编号提交（如阶段 8·提交 7）在独立分支上开发；分支命名 = 版本号去掉修复位：`0.阶段.提交`（阶段 8·提交 7 → `0.8.7`，阶段 8·提交 10 → `0.8.10`），禁止在分支名携带修复位或字母后缀。
2. **创建时机与起点**：分支在该提交**正式开发开始时**才创建（未开始的提交不提前建空分支；上一编号提交交付后处于修复/延伸期时，继续在当前版本分支上开发，main 保持上一合并状态，待开发新编号提交时再同步）；一律从 main 最新 HEAD 切出（确保包含已完成提交及 main 上的修复），禁止从旧提交或旧分支派生。**每次开工第一步：先 `git pull` 再重读本文件**——AGENTS.md 是会话启动快照，规范一律以 main 最新提交为准，长会话/跨版本会话必须先刷新再动手。
3. **main 唯一合并入口（开发新的编号提交时）**：只有**开始开发编号提交 X+1 的那一刻**才允许把当前版本分支合并回 main，动作顺序固定：① 合并 `0.8.X` 分支（含其全部修复位，如 0.8.X.1 / 0.8.X.2）进 main（`git merge --no-ff`，保留合并节点便于回溯交付时间）→ ② 推送 main → ③ 从 main 切出下一个分支 `0.8.(X+1)`。除该入口外，任何会话、任何时机都**禁止**把版本分支内容（功能或修复）提交、合并、推送到 main（文档先行除外，见 5.6）。
4. **交付动作（开发完成后、下一提交开始前）**：编号提交开发完成后的全部动作：① 功能代码与落地文档全部提交在 `0.8.X` 分支并推送 → ② 向用户报告交付（程序版本号、测试状态）→ ③ 留在 `0.8.X` 分支继续修复/延伸，或切回 main 待命（仅可做文档先行改动）。**期间 main 保持冻结**；禁止“交付即合并”“落地即合并”，合并信息也不得写成“XX 落地”（见 5.8），防止错误先例误导后续会话。
5. **修复归属（延伸在当前版本分支提交，不开新分支）**：当前版本（含已合并过 main 的版本）发现的 bug 与延伸修复一律提交在**当前版本分支**上（修复位顺延，如 0.8.7.1 提交在 0.8.7 分支），**不直接提交 main、不为此另开分支**；修复随 5.3 的唯一合并入口进入 main。
6. **文档先行不受影响**：设计定稿 / 排期补充等文档改动（AGENTS.md §1「先文档后代码」）仍可直接提交 main，不视为该编号提交的开发提交；功能代码及其落地文档在分支上完成，合并后文档与程序版本在 main 对齐。
7. **分支内容边界**：一个分支只承载一个编号提交的内容；若排期阶段把改动拆分为多个编号提交，按拆分结果逐个提交各开分支，禁止中途混入下一提交内容。
8. **合并信息模板（5.3 入口的 merge 提交必填）**：合并信息必须写明触发原因，格式：`merge(0.8.X): 启动 0.8.(X+1) 开发前同步（含 0.8.X.Y 修复）`；禁止使用“XX 落地 / XX 交付”这类不带触发时机的措辞。
9. **远程同步**：分支与 main 均推送 GitHub；版本分支（如 0.8.7）合并回 main 后仍保留并推送，不清理、不删除；开发期间 main 出现新提交（文档先行等）时，先把 main 合并进分支再继续，避免合并回退冲突。
