# 关卡设计（STAGES）

> 隶属《烽火连营·三国塔防》设计文档体系，总纲见 [../GAME_DESIGN.md](../GAME_DESIGN.md)。
> 承载总纲原章节：5.1、5.2、5.4、5.6、6。章节编号沿用全局稳定 ID。
> 经济与数值基调见 [NUMBERS.md](NUMBERS.md)（5.3 / 10.3）；敌人设计见 [ENEMIES.md](ENEMIES.md)。

---

## 5.1 关卡结构

关卡由 `StageData` 定义：`starting_lives`（基地生命）、`starting_currency`（初始金币）、`squad_size`（可出战武将数）、`build_slot_count`（建造槽）、`waves`（波次列表）、`prerequisite_stage_ids`（前置关卡）、局内升级与回收参数（`max_inbattle_upgrade_level` / `upgrade_cost_factor` / `sell_refund_ratio`，见 5.4，v0.9 已实施）。

**布局字段（v0.9 数据驱动化，`StageData` 新增）**：`path_points`（直角折线拐点，含图外出入口延长段）、`build_slot_positions`（建造位坐标）、`decor_cells`（装饰格）。道路格子由 `path_points` 逐格推导（`GridBackground.derive_road_cells`），出入口地标取首/末图内格；建造位、道路、地标均不再硬编码于场景。

**建造位结构化（v0.11 定稿，阶段 3）**：建造位从坐标数组升级为 `BuildSlotData`（`position` + `slot_type: melee/ranged/any` + `locked: bool`）：
- **类型化**：近战优位（转角双覆盖）/ 远程优位（开阔覆盖）/ 通用位，空位绘制对应职业图标供玩家识别；类型为**软引导**（不限制放置），匹配类型的职业获得放置折扣或轻微加成【数值待定】。
- **解锁式建造位**：进阶关可配置预锁位（`locked`），花金币解锁（50~80/个）——"空间"成为经济决策旋钮；教学关全开。
- 投石车的 `min_range` 机制与远程位天然联动（近距离无法攻击，必须放远程位）。

**剧情对话字段（v0.11，阶段 3）**：`StageData.dialogue_id` → `DialogueData`（说话人 + 台词行数组）。战斗开场准备期前播放对话覆盖层（点击推进、可跳过）；第一章 8 关各配 3~6 句（黄巾史实背景 + 出场武将台词）；设置面板同步增加"剧情速进"开关。

- **胜利条件**：全部波次清空且基地生命 > 0。
- **失败条件**：基地生命归零。
- **波次**（`WaveData`）：由多个出生组（`EnemySpawnData`：敌人类型+数量+间隔）构成；`preparation_time` 为波前准备期（v0.7 明确：当前版本采用**手动开波**，玩家点击开始按钮放行，该字段保留给【远期】"准备期倒计时自动开波"模式）；`completion_currency` 为波次完成奖励（默认 0 不启用，见 [NUMBERS.md](NUMBERS.md) 5.3）；`is_boss_wave` 标记 Boss 波。

---

## 5.2 波次设计规范（模板）

每关建议 5~8 波，节奏遵循模板：

| 波次位置 | 内容 | 目的 |
|---|---|---|
| 第 1 波 | 纯基础兵，数量少 | 教学/热身 |
| 第 2~3 波 | 基础兵+轻骑混编 | 引入新敌人 |
| 第 4 波 | 精英（伍长）+混编 | 压力测试 |
| 第 5 波 | 快慢搭配（轻骑先、步卒后） | 漏怪考验 |
| 第 6~7 波 | 新敌人机制（弓手/祭酒） | 机制教学 |
| 末波（Boss） | Boss + 护卫 | 综合验收 |

---

## 5.4 局内升级与回收

战斗内对已建塔的两项金币操作，均为**局内临时状态**（与 4.4 的局外等级完全独立，不写入存档）。

- **局内升级**：
  - 每座塔的局内等级（battle_level）从 0 开始，每关最多升至 `max_inbattle_upgrade_level`（初稿 2）。
  - 第 n 次升级费用 = `build_cost × upgrade_cost_factor × n`（初稿 factor=0.8；例：关羽 70 → 第一次 56、第二次 112）。
  - 每次升级效果：伤害 +25%；攻速、射程、特性不变；大招强度随普攻自然提升（倍率定义见 [NUMBERS.md](NUMBERS.md) 10.5）；怒气获取规则不变。
  - 升级入口位于已建塔交互面板，准备期与波次中均可操作。
- **回收（卖塔）**：
  - 回收返还 = 该塔总投入（建造 + 已花费升级）× `sell_refund_ratio`（初稿 0.6），向上取整。
  - 回收后建造槽立即可复用；该塔局内等级与怒气丢弃。
  - 准备期与波次中均可回收。
- **数据层扩展（v0.9 已实施）**：`StageData` 新增 `max_inbattle_upgrade_level: int`、`upgrade_cost_factor: float`、`sell_refund_ratio: float`；战斗内通过已建塔交互面板操作，`TowerManager.upgrade_tower/sell_tower` 执行；数值均为初稿，平衡调整须同步更新本文档与 [NUMBERS.md](NUMBERS.md) 10.3。

---

## 5.6 关卡布局设计规范（v0.7 新增，数值为初稿）

地图为 16×9 的 80px 网格，路径为直角折线，建造位对齐网格并贴邻道路。短射程职业（骑兵、枪兵）的价值高度依赖"贴路、转角位"的分布，为保证职业差异化成立，新建关卡（`ch01_s02` 起）须遵循：

- **路径**：入口与基地分居地图两端；路径总长度（按格数）初稿 ≥ 24 格；至少 2 个直角转角——转角贴路位是近战/短射程职业的优位。
- **黄金位（v0.11 新增）**：每关至少 1~2 个"双覆盖转角位"（相邻两段道路都在射程内），并以视觉引导标识（地面纹理、相邻装饰）——每个建造位都应有明确使命（黄金位/减速链位/漏怪兜底位），不做均匀撒点。
- **建造位档位**：
  - **贴路位**：与道路格正相邻——骑兵、枪兵等短射程职业的优位；
  - **中程位**：距道路 1 格——术士等中射程职业；
  - **远程位**：距道路 2 格以上——弓箭手等长射程职业的覆盖位。
- **配比下限（每关）**：贴路转角位 ≥ 2、贴路直道位 ≥ 2、中/远程位 ≥ 3；建造位总数 = `build_slot_count`，应比预期成塔数（6~9）多留 2~3 个空位供规划取舍。
- **避免**：整关没有转角（近战职业无优位）、大段无建造位的"真空长廊"（远程也够不着）、路径过度回折导致贴路位过密。
- **联调基准**：每关布局与波次联调时，以"建造位全空、仅初始金币"开局试打，第 1~2 波可存活为下限；末波靠满配阵容可守为上限。

---

## 5.7 地图主题与绘制（v0.11 新增，阶段 3 起分步）

当前所有地图共用同一棋盘格配色，章节特色（夜袭、水战）无视觉表达。分三步走：

- **第一步（阶段 3，零美术成本）——主题层**：`StageData`/`ChapterData` 新增 `theme` 字段，`GridBackground` 按主题渲染。主题 = 调色板（棋盘双色/道路色/装饰色）+ 装饰类型（树/石/旗帜/帐篷）+ 氛围效果。示例：**夜袭**（第三章）整体调暗 + 路径火把光圈；**水战**（第五章）路径两侧水纹色带 + 芦苇装饰。一个 `ThemeData` 资源驱动，纯程序化。
- **第二步（顺手的观感升级）**：路径转角圆角化、路面磨损斑、入口旗帜 sin 波飘动、基地受击冒烟。
- **第三步（阶段 5/6）**：装饰素材化——程序化树/石头替换为手绘素材（`assets/` 目录），布局数据不变。
- **路径拓扑演进**：阶段 3 的 s08 试点**分叉路径**（张梁召唤护卫走岔路——塔防战术深度最大的机制，需 PathFollow2D 多路径 + 波次指派路径），成则成为后续章节标配；地形效果格（减速水面/可破坏障碍）留【远期】。

---

## 6. 第一章关卡规划（黄巾之乱）

第一章共 **8 关**（v0.9：前 3 关已建成可玩，s04~s08 待建），从涿郡起兵到广宗决战。关卡名对齐黄巾之乱主要战役（长社、广宗、下曲阳、宛城等）。

| 关 | ID | 关卡名 | 主题 | 主要敌人 | 首通解锁/奖励 | 教学点 | 状态 |
|---|---|---|---|---|---|---|---|
| 1 | `ch01_s01` | 桃园起兵·涿郡 | 教学关 | 黄巾步卒、轻骑 | 刘备、关羽（已配置）；黄巾布×2 | 建造、波次、胜负 | ✅ 5 波 |
| 2 | `ch01_s02` | 长社火攻 | 人海教学 | 步卒海+轻骑 | 张飞；黄巾布×2 | 范围伤害、漏怪惩罚 | ✅ 6 波（v0.9） |
| 3 | `ch01_s03` | 颖川之战 | 远程新兵 | 步卒+弓手 | 黄忠、皇甫嵩（v0.11.1）；黄巾布×2 | 远程双兵：弓手+投石车 | ✅ 6 波（v0.9） |
| 4 | `ch01_s04` | 东郡驰援 | 精英战 | 伍长+轻骑+步卒 | 貂蝉（已配置 `ch01_s04`） | 精英目标、光环辅助 | 待建（阶段 3） |
| 5 | `ch01_s05` | 宛城之围 | 高血战 | 力士+步卒 | 周仓【新增角色】 | 高血量敌人、集火 | 待建（阶段 3） |
| 6 | `ch01_s06` | 下曲阳之战 | 混编压力 | 全兵种混合 | 赵云【新增角色】 | 综合压力、经济分配 | 待建（阶段 3） |
| 7 | `ch01_s07` | 广宗前哨 | 机制战 | 祭酒光环+混编 | 诸葛亮【新增角色】 | 光环敌人对策 | 待建（阶段 3） |
| 8 | `ch01_s08` | 广宗决战 | Boss 关 | 渠帅（张梁）+护卫 | 求贤令×3（为阶段4铺垫） | Boss 战、通关成就感 | 待建（阶段 3） |

> 历史说明：貂蝉、赵云、诸葛亮按史实尚未在黄巾时期登场，此处为游戏性需要提前登场【待定：可接受则保留，不接受则替换为周仓/张梁之外的蜀汉将领】。

**第一章角色池（9 名，v0.11.1 扩容）**：刘备（剑客·辅助，`rage_gain_mode=support`，v0.9.2 由术士调整）、关羽（骑兵·爆发）、张飞（剑客·近战）【新增】、黄忠（弓箭手）、皇甫嵩（投石车·范围，v0.11.1 新增，汉室阵营）、貂蝉（舞娘·辅助）、周仓（剑客·近战）【新增】、赵云（剑客·快攻）【新增】、诸葛亮（术士·范围）【新增】。武将池不限阵营（汉室/群雄可入池）。角色系统细则见 [CHARACTERS.md](CHARACTERS.md)。

> 转职资源现状（v0.7 更新）：`resources/promotions/` 已有 `guan_yu_charger.tres`（关羽，正式数值），并已补齐 `liu_bei_commander` / `huang_zhong_sharpshooter` / `diao_chan_inspirer` 的**占位配置**（倍率 1.0、无授予技能），阶段 2 细化数值、技能与外观。冒烟测试的资源完整性扫描会拦截"角色引用不存在的转职"这类断链。
