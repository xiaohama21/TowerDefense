# 敌人设计（ENEMIES）

> 隶属《烽火连营·三国塔防》设计文档体系，总纲见 [../GAME_DESIGN.md](../GAME_DESIGN.md)。
> 承载总纲原章节：5.5。章节编号沿用全局稳定 ID。
> 特殊行为的实现状态与规格清单见 [BEHAVIORS.md](BEHAVIORS.md) B.3.2。

---

## 5.5 敌人设计

敌人由 `EnemyData` 定义：`max_hp`、`move_speed`、`armor`、`damage_to_base`（漏怪伤害）、`special_behavior_id`（特殊行为，行为脚本按 ID 注册）、`tags`（阵营/兵种标签，用于掉落与克制）。

**护甲减算公式（v0.7 落地实现）**：实际伤害 = `max(伤害 − 护甲, 向上取整(伤害 × 10%))`，即护甲无论如何不会完全免伤。第一章敌人护甲均为 0，第二章"高护甲骑兵"起生效；护甲作用于所有伤害来源（普攻、大招、范围伤害均在 `take_damage` 内统一结算）。

**敌人模板机制（✅ v0.18.0 阶段 6 试点，第二章派生基准）**：`EnemyData` 可引用 `template: EnemyTemplateData`（`resources/enemy_templates/`）批量派生——自身字段为 0/空哨兵时继承模板值（`resolved()` 返回合并副本，`EnemyManager` 生成时自动应用）。哨兵约定：`max_hp / move_speed / damage_to_base / currency_reward / kill_xp` 为 0、`armor` 为 -1、`special_behavior_id / tags` 为空、`body_color` 为全透明、`body_size` 为零向量 = 继承模板；引用模板的敌人无需逐字段复制，第二章内容直接堆 `.tres`。试点模板：`heavy_cavalry`（高护甲骑兵：180HP / 145 速 / 护甲 20，配套护甲减算公式已就绪）。

### 5.5.1 敌人维度

- 兵种：步卒 / 轻骑 / 弓手 / 力士 / 妖道（祭酒）。
- 定位：炮灰（低血低伤）、常规（数量中）、精英（高血、漏怪伤害高）、Boss（章节守关）。
- 特殊行为（`special_behavior_id`，逐期实现；实现状态见 [BEHAVIORS.md](BEHAVIORS.md) B.3.2）：
  - `none`：默认走直线。
  - `fast_charger`：速度型，漏怪伤害更高（轻骑已有）。
  - `healer_aura`：为周围敌人回血（黄巾祭酒）。
  - `armor_aura`：为周围敌人加护甲（黄巾祭酒变体）。
  - `suicide`：到点自爆造成范围伤害【远期】。
  - `swarm`：分裂/召唤小怪【远期】。

### 5.5.2 第一章敌人表（黄巾军）

| 敌人 ID | 名称 | 定位 | 数值基调 | 状态 |
|---|---|---|---|---|
| `yellow_turban_soldier` | 黄巾步卒 | 炮灰 | 100HP / 80速 / 漏1血 | 已有 |
| `yellow_turban_cavalry` | 黄巾轻骑 | 快速 | 110HP / 145速 / 漏2血 | 已有 |
| `yellow_turban_sergeant` | 黄巾伍长 | 精英 | 300HP / 65速 / 漏2血 | 已有 |
| `yellow_turban_archer` | 黄巾弓手 | 远程（慢速高漏伤） | 90HP / 60速 / 漏3血 | 已有（v0.9） |
| `yellow_turban_berserker` | 黄巾力士 | 高血坦克 | 450HP / 45速 / 漏2血 | 已有（v0.11.3） |
| `yellow_turban_sorcerer` | 黄巾祭酒 | 光环支援 | 200HP / 55速 / 治疗光环（healer_aura：每 2s 治疗周围 120px 友军 15 点） | 已有（v0.11.3） |
| `yellow_turban_general` | 黄巾渠帅（张梁） | Boss | 3000HP / 30速 / 漏10血 | 已有（v0.11.3，summon_guard：每 8s 自岔路召唤 2 名步卒——s08 分叉试点） |

**敌人规则**：每章敌人必须与章节主题一致（第一章=黄巾军）；新增敌人 = 先建 `EnemyData` 再按需实现 `special_behavior_id`；禁止脱离配置凭空造怪。第一章现有敌人按 5.5.3 模板回填（阶段 9 统一执行，见总纲阶段 9 提交 3），不阻塞当前开发。

### 5.5.3 敌人设计模板（v0.24.0 拍板，硬性规范）

每个新敌人都应具备：

- 一个主要能力（唯一核心机制，不做多机制拼盘）。
- 一个清晰预警（行为前有视觉/音效提示，玩家可识别）。
- 至少两个有效反制（不同职业 / 军需 / 站位至少两种解法）。
- 一个失败保护机制（应对反制失效的兜底，如伤害上限 / 召唤上限 / 撤退）。

**禁止**同时叠加：高血量 + 高护甲 + 高速度 + 治疗 + 召唤（只能以一个属性或机制为核心，其余只能作弱辅助）。

### 5.5.4 Boss 设计模板（v0.24.0 拍板，硬性规范）

每个 Boss 至少包含：

- 登场预警（Boss 横幅 + 专属演出，见 UI.md 9）。
- 两到三个阶段（血量阈值切换）。
- 每阶段一个新行为（阶段差异可感知）。
- 技能前摇和可识别范围（前摇期间有明确视觉提示）。
- 阶段切换后的输出窗口（切换时给玩家安全输出时间）。
- 召唤数量上限（防召唤流无限膨胀）。
- 控制抗性递增（每阶段控制时长减免 30%、上限 60%，**非永久免疫**；数值见 NUMBERS.md 10.11）。
- 战败原因可解释（玩家可复盘：输出不足 / 漏怪 / 召唤未清）。

Boss 应考验目标选择和资源时机，而不是单纯提高生命值。

第一章 Boss（黄巾渠帅·张梁）按模板补齐——补充阶段划分与每阶段新行为、控制抗性递增（阶段 9 统一执行，见总纲阶段 9 提交 3）。

### 5.5.5 第一章战斗内容扩充（2026-09-09 拍板，实现排期 阶段 8·提交 13 = 0.8.13.0~0.8.13.5）

> 本小节为 2026-09-09 内容丰富重设计定稿，替换本节此前与敌人相关的旧口径（护甲/伤害类型公式见 NUMBERS 10.12；完整方案见 `docs/proposals/CH1_COMBAT_RICHNESS_PLAN.md` v0.4）。旧正文保留作历史。

**护甲模型（替换 5.5 首段 v0.7 线性减算）**：伤害类型分 物理（算全部护甲）/ 魔法（算 50%）/ 真实（无视护甲、不可被任何减伤）；护甲结算顺序 = 类型系数 → 百分比穿甲（跨来源乘算、单源 ≤30%、同源取最高）→ 固定穿透 → `减伤 = armor/(armor+50)`；**不设保底**（真实伤害满伤）。公式见 NUMBERS 10.12。

**第一章全敌护甲重标（C=50，物理/魔法减伤占比）**：

| 敌人 | 定位 | 甲值 | 物理减伤 | 魔法减伤 |
|---|---|---|---|---|
| 黄巾步卒 | 炮灰 | 0 | 0% | 0% |
| 黄巾弓手 | 远程低血 | 0 | 0% | 0% |
| 黄巾轻骑 | 快速 | 2 | 3.8% | 2% |
| 黄巾祭酒 | 光环支援 | 4 | 7.4% | 3.8% |
| 黄巾伍长 | 精英 | 8 | 13.8% | 7.4% |
| 精锐伍长 | 精英·军阵 | 8（aura +6） | 13.8%（aura 后 ~21.9%） | 7.4%→~12.2% |
| 黄巾力士 | 高血坦克 | 10 | 16.7% | 9.1% |
| 披甲力士 | 中甲坦克 | 22 | 30.6% | 18% |
| 黄巾符祭 | 甲光环 | 6 | 10.7% | 5.7% |
| 张宝（s06 中 Boss） | 首领 | 18 | 26.5% | 15.2% |
| 张梁（s03 Boss） | 首领 | 20 | 28.6% | 16.7% |
| 张角（s08 终 Boss） | Boss | 30 | 37.5% | 23.1% |
| heavy_cavalry 模板（二章） | 重甲骑兵 | 32 | 39% | 24.2% |

**新增敌人（排期 0.8.13.2）**：披甲力士 `yt_heavy_berserker`（s05，中甲 22）、精锐伍长 `yt_elite_sergeant`（s03，甲 8 + 军阵光环 +6 甲）、夜行刺 `yt_stealth_assassin`（s06，隐匿）、隐方士 `yt_stealth_healer`（s07，隐匿 + 治疗光环）、符祭 `yt_armor_aura_caster`（s07，甲光环）。隐匿/甲光环行为登记见 BEHAVIORS B.3.5。

**Boss 分布（取代"张梁=s08 唯一 Boss"旧结论）**：

- s03 颖川之战 = 张梁（**复用不改**，保留 `summon_guard` 召唤护卫；HP 按关位标定，甲 20 中甲档）。
- s06 下曲阳之战 = 张宝（中 Boss）：P1 术法直伤 + 治疗光环；P2（≤50%）召唤夜行刺 + 输出窗口；草案 HP≈2200~2800、速 28、漏 8、召唤上限 4~6、甲 18。
- s08 广宗决战 = 张角（终 Boss）：P1 布阵（步卒+符祭甲光环）/ P2 唤雨（治疗峰值+轻骑快攻）/ P3 狂雷（范围术法+隐匿亲卫）三阶段；草案 HP≈8000~10000、速 26、漏 12、召唤上限 6~8、甲 30。

> 旧 5.5.2 敌人表与 5.5.4"张梁=s08 待模板补齐"保留作历史；5.5.4 的首章 Boss 模板补齐目标改为张角，随 0.8.13.5 排期执行。
