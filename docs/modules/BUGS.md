# Bug 修复记录（BUGS）

> 隶属《烽火连营·三国塔防》设计文档体系，总纲见 [../GAME_DESIGN.md](../GAME_DESIGN.md)。
> 文档版本：v0.41（2026-09-08）
> v0.41 变更（2026-09-08）：新增 B-054——地图选择关卡预告条内容越出面板/屏幕（阶段 8·提交 11 延伸，v0.37.6 / 程序 0.8.11.3 / UI_LAYOUT v0.20.27）：用户反馈「地图选择界面s03，关卡详情面板直接超出界面，要做好自适应啊」——s03（首通解锁黄忠+皇甫嵩 2 枚武将徽章）等奖励/敌人信息较长的关卡，底部详情条 columns HBox 被子内容最小宽度撑破锚点：s03 徽章行越出面板 64px 并出屏，s06/s07 越 34~36px，s08 越 141px（出征按钮被推出白卡、部分出屏）；根因 = 容器子项（统计胶囊值文本、首通奖励徽章）Label 最小宽度=全文宽，HBox 最小宽度和超过锚点可用宽时容器自撑越界（Panel 非容器不裁剪子控件）；处理 = ①统计胶囊值 Label `text_overrun_behavior=TRIM_ELLIPSIS`（机制实测 Label 最小宽度 368→1）+ EXPAND_FILL 均分胶囊宽，超宽省略号截断；②首通奖励徽章行 HBoxContainer→HFlowContainer（h_separation 8 / v_separation 6）放不下自动折行（右列高 128 内 2 行徽章+按钮仍容纳，面板 148 高不变）；修复后 8 关逐关控件几何扫描 0 越界；FlowRunner 新增逐关越界回归断言，并顺带修复设置隔离（user://settings.cfg `skip_dialogue=true` 曾使开场对话 5 条用例误失败——跑前强制关闭、_cleanup 还原用户设置）。回归 Smoke/Flow/Stage0/ExitFlow/PromoteDialog 全绿。
> v0.40 变更（2026-09-08）：新增 B-053——怒气条 0 怒气时不可见（阶段 8·提交 11 延伸，v0.37.4 / 程序 0.8.11.2 / UI_LAYOUT v0.20.25）：用户反馈「怒气条默认不显示？攻击敌人获取怒气后才显示吗」——新建塔怒气为 0 时脚下无任何怒气提示，玩家无法预知怒气/大招机制；根因 = `Tower._draw_rage_bar` 在 `rage <= 0` 时直接 return，0 怒气连底槽都不画；处理 = 移除早退，**空槽常驻弱显**——0 怒气画半透明底槽（alpha 0.35，不抢眼），获得怒气后底槽加深（0.66）+ 灰蓝填充，满怒金色呼吸/外发光不变（填充仅在 ratio>0 绘制）。
> v0.39 变更（2026-09-08）：新增 B-051·B-052——①**B-051 怒气胶囊越出人物所在格**（阶段 8·提交 11 延伸，v0.37.3 / 程序 0.8.11.1 / UI_LAYOUT v0.20.24 / ART_ASSETS v0.9）：用户反馈「怒气胶囊不够圆润，摆放位置都超出人物所在格了」——胶囊 44×7 中心 y48（spine 塔）时下缘 y51.5 > 格底 y40，越出格子下边；处理 = 改 **38×10 圆角胶囊**、中心 spine y30 / 普通 y28（下缘 ≤ 格底 40），满怒放大呼吸与外发光保留；②**B-052 顶栏/建造卡条遮挡地图**（阶段 8·提交 11 延伸，v0.37.3 / 程序 0.8.11.1 / UI_LAYOUT v0.20.24）：用户反馈「顶部面板遮挡地图，顶部两排地图无法建造」——旧版顶栏浮条 y16..196 + 建造卡条 y92..196 覆盖 row0..2，`is_point_over_battle_ui` 禁建区 y≤196 致 row1..2 不可建；处理 = 顶栏贴顶全宽 0..80、建造卡条移底部常驻 640..720、禁建区改 y≤80 / y≥640、塔详情面板与结算展示时收起卡条、剧情对白面板上移 448..638 不遮卡条、`ch01_s03` 路径/建造位/禁建格重排避让 row0/row8（路格 21→26、转角 4，MapValidator + FlowRunner 布局规范通过）；修复后战场 row1..7 完整可建。
> v0.38 变更（2026-09-08）：新增 B-050——角色技能冷却环脱战冻结（阶段 8·提交 11，v0.37.1 / 程序 0.8.11.0 / UI_LAYOUT v0.20.22 / ART_ASSETS v0.8）：A 型角色技能冷却期间若脱战（范围内无目标），塔身冷却环弧线停留旧值而实际冷却在走——根因=冷却环绘制只随攻击闪光/挥击等事件 `queue_redraw`，脱战无重绘源；修复=`Tower._process` A 型冷却递减期间**每帧 `queue_redraw()`**，弧线随剩余比例平滑收缩、就绪隐藏；顺带 spine 角色环半径 36→40 防素材遮挡。回归 Smoke / Flow 全绿。
> v0.37 变更（2026-09-06）：新增 B-049——百科武将图鉴页签与详情内边距两处偏差（用户反馈「武将图鉴的页签名称 scroll 删除」「详情面板里文字离面板边框太近了，你再看看 UI」）：①TabContainer 页签标题默认取直接子节点名，`_make_tab` 把滚动容器直接挂为页签子节点（ScrollContainer 命名「基础Scroll…」），页签标题带「Scroll」后缀（与养成页签同款问题的百科残留）；②页签体内容直接铺在浅蓝描边内侧、无概念 `.tabbody padding:10px 14px` 内边距——像素级量测实现文字距页签浅蓝边框左 3px / 上 4px，概念图为 17px / 13px（v0.36.24 / 程序 0.8.10.32 / UI_LAYOUT v0.20.19 / ENCYCLOPEDIA v0.1.9）。修复=`_make_tab` 用 `set_tab_title` 显式设页签标题（节点名不再进标题）；页签滚动内容外包 MarginContainer（14/14/10/10，对齐 .tabbody padding）并横向 EXPAND_FILL 拉伸；EncShotProbe 增补页签标题断言 + 逐页签切换零越界审计，像素比色核验文字距边框 17px 与概念图一致；回归 Smoke / Flow 绿。
> v0.36 变更（2026-09-06）：新增 B-048——百科 v0.20.17 对齐后仍与概念图 ui_encyclopedia.png / ui_encyclopedia_enemy.png 有残留偏差（用户再对照概念图反馈「还有问题」，像素级区域比对定位）：①武将/敌人分段切换按钮误用浅金卡选中样式（apply_light_selectable），概念 .segbtn 为 Kenney 蓝底白字（选中）/ 灰底深字（未选），与科技树 .tabs 同源（v0.19.1 曾修复过同款，v0.20.17 重构时回归）；②武将左列与详情头头像全为蓝色（avatar_label 仅 7 色键且调用恒传 "blue"+职业基色，概念按角色九色：金/红/蓝/绿/紫/橙/棕/粉/青，周仓/赵云/诸葛亮需要新增 brown/pink/teal 键）；③CHARACTER_ORDER 概念序第 4/5 位对调笔误（概念稿黄忠在皇甫嵩前）；④默认选中未对齐概念稿（概念=武将张飞 / 敌人黄巾渠帅·张梁，实现=列表首位刘备/黄巾步卒）；⑤EnemyAvatar 方形渐变头像白屏——渐变纹理在 `_draw` 内每帧现建（GradientTexture2D → ImageTexture），Compatibility 渲染器运行时纹理上传时序问题致同帧绘制无内容（纯白样式底可见），探针同参数 TextureRect 正常而 EnemyAvatar 全白（v0.36.23 / 程序 0.8.10.31 / UI_LAYOUT v0.20.18 / ENCYCLOPEDIA v0.1.8）。修复=分段按钮改 `apply_kenney_rect_button` 蓝/灰（同科技树）、avatar_label 补三键 + `CHARACTER_AVATAR_COLORS` 九色映射落地（详情/列表同源）、`CHARACTER_ORDER` 顺序更正、默认选中改 `DEFAULT_CHARACTER_ID`（张飞）/ `DEFAULT_ENEMY_ID`（黄巾渠帅·张梁）、EnemyAvatar setup 预生成 ImageTexture 缓存绘制；像素探针同坐标比色核验九色头像/渐变方块/蓝灰分段按钮，EncShotProbe / FlowRunner 断言同步（默认张飞、张梁），回归 Smoke / Flow 绿。
> v0.35 变更（2026-09-06）：新增 B-047——百科两屏与概念图 ui_encyclopedia.png / ui_encyclopedia_enemy.png 终版多处偏差（用户逐项对照概念图找差异后拍板按推荐顺序修改、文档同步按概念图执行）：①武将左列按资源 id 字典序排（非概念顺序），卡副行旧口径「职业名 · 战斗定位词」（非「职业名 · 打法词」），缺列表头与虚线分隔；②武将详情头部无虚线分隔、chips 与右侧「获取方式」排布不符概念 .dhead；「基础」页签统计格未按概念 .stat 四卡（含金色建造费用），模拟器嵌在详情内而非底部整行 .sim 卡；③敌人左列卡缺方形渐变头像、副行「定位·阵营」非「定位词 · 打法词」；④敌人详情体未卡化（缺五统计卡 / 特殊行为卡 / 各档难度卡行），头部缺「出现关卡」摘要与虚线分隔（v0.36.22 / 程序 0.8.10.30 / UI_LAYOUT v0.20.17 / ENCYCLOPEDIA v0.1.7）。修复=新增 DashedLine / EnemyAvatar 自绘小部件（StyleBox 不支持 dashed / 渐变头像）、CHARACTER_ORDER / CHARACTER_FLAVOR / ENEMY_LOCATIONS / ENEMY_TACTIC_SUB 概念字典落地、模拟器改武将视图底部整行卡（升阶 0~3 按钮 + 三枚金色结果卡）、敌人详情五统计卡 + 难度卡行 + 出现摘要；EncShotProbe 几何探针核验武将/敌人/逐敌选中/模拟器各态零越界；回归 FlowRunner 百科段 / Smoke 绿。
> v0.34 变更（2026-09-06）：新增 B-046——科技树 v0.20.15 重构两处交互回归（用户反馈「切换科技块有黑色闪烁」「点击解锁无反应」）：①非选中节点 pressed/hover_pressed 覆盖被 `_apply_node_selected_style` 非选中分支移除，鼠标按下瞬间回落项目默认深色 pressed 样式（B-034 同款黑闪）；②详情右列「解锁」按钮在重构中丢失 `pressed` 信号连接（B-037 类漏挂载回归），点击无反应、永不调用 `_on_unlock_pressed`（v0.36.21 / 程序 0.8.10.29 / UI_LAYOUT v0.20.16）。修复=非选中分支保留状态底色覆盖（仅 `set_pressed_no_signal(false)`）+ 补 `_unlock_button.pressed.connect(_on_unlock_pressed)`；新增 tests/TechUnlockRunner headless 点击链路回归（选节点→解锁钮可用→点击→已解锁入库 + 按钮「已解锁 ✓」+ 底部 ✓ + 非选中节点 pressed 覆盖断言）；回归 Smoke 绿。
> v0.33 变更（2026-09-06）：新增 B-045——科技树版式与概念图 ui_tech.png / ui_tech.html 偏差多处（用户「看科技树页面与概念图的区别，找一下问题」后拍板按推荐顺序修改、保留「科技点+50（测试）」按钮）：①树 GridContainer 每行固定 8 格（层级标签列 + 7 链列占位），列宽被内容撑开、占位无最小宽，连接带竖线与节点列错位折行，层级标签单行横排随行高拉进 26px 连接带、文字挤在左侧；②分类仍 TabContainer 页签、顶栏标题独占一行，概念为 .tabs 4 枚按钮 + 顶栏一行 + .legend 图例；③军略链序按 id 字典序（prof_archer 打头）非概念（全武将→虎贲→骑兵→弓手→术士→舞娘→投石车）；④节点卡状态色/卡内结构与概念 .node 不符，军略终极层缺 .t3-note 虚线提示条；⑤详情区缺第 3 枚「职业加成合计 +9%（上限 +10%）」pill（v0.36.20 / 程序 0.8.10.28 / UI_LAYOUT v0.20.15）。修复=树改行式（52px 竖排层级胶囊 + 节点 HBox 行 + 26px 连接带独立行逐链画竖线）、军略概念链序 + 终极层 .t3-note、节点三态概念色卡、分类 .tabs Kenney 按钮 + .legend、详情第 3 pill 与「军略 · 职业强化」chip；几何探针核验层级行/连接带/节点列/提示条/详情横向纵向零越界；回归 Smoke 绿。
> v0.32 变更（2026-09-06）：新增 B-044——转职详情叠层与概念图 ui_develop_promo.png 仍有整体偏差（用户反馈「改了啥啊，一点变化都没有」：①顶部虚线长胶囊缺失、文字挤在左侧 ②蓝头 who 角色信息在右侧、应贴「转职详情」标题 ③中间面板宽高对不上）：叠层仍挂在养成内容区（~988px 宽）而非整窗、外框 1160×660 偏高；顶部图例与底部说明是单色横条非虚线胶囊；蓝头单色且 who 被标题 expand 挤到最右。修复=叠层改挂 GameHub 顶层整窗居中 + 外框 1160×570；新增 DashedPill 虚线胶囊（`.ptip`/`.pfoot`）与 PromoTreeRail 竖轨自绘；蓝头改渐变、who 紧贴标题；节点卡去头像圆（v0.36.18 / 程序 0.8.10.26 / UI_LAYOUT v0.20.13）。三态几何探针零越界、纵向一屏放下；回归 PromoteDialog / Smoke 绿。
> v0.31 变更（2026-09-06）：新增 B-043——武将养成转职详情叠层与概念图 ui_develop_promo.html 的多处实现偏差（用户确认按 1→7 顺序修改，明令「别再出现超出面板的问题」）：①二转分支标题需求误取一转候选数据（显示 Lv 10 · 黄巾布 ×10，实为 Lv 20 · ×20）；②基础节点状态文案错位（已一转后仍显示「基础职业 · 未转职」）；③顶部图例写「绿点/金点/灰点」而画面是 ↓ 箭头树；④已一转后二转两分支未并排（branch_row 只在未转职分支预览成立，真实候选仍纵向全宽堆叠）；⑤二转分支卡缺强化/新技能数值详情；⑥节点名称字号与 act 按钮宽度不符概念（180/128 定宽）；⑦叠层外框（底/描边/圆角/粗下边）与头部标题未对齐概念 `.promo-panel`（v0.36.17 / 程序 0.8.10.25 / UI_LAYOUT v0.20.12）。修复=节点顶行（头像+名称 18+状态 tag+step 浅字+右列定宽 act）+ 底部通栏（描述/`.fx` 强化或新技能数值/条件胶囊 HFlow）重构；分支行一律两卡并排各半宽；需求文案取分支数据；几何探针核验三态零横向越界；回归 PromoteDialog / Smoke 绿。
> v0.30 变更（2026-09-06）：新增 B-042——武将养成职业页签内容横向溢出详情面板（用户反馈「详情面板都超出去了」）：转职进度卡三枚 chips 用不可换行 HBox 与标题/按钮同行，最小宽 ~660px 撑破页签体（~602px），整页内容右溢约 80px 顶出面板/窗口；修复=chips 按概念 `.jchips flex-wrap:wrap` 改 HFlowContainer 自动折行 + 职业页签内容套纵向 ScrollContainer 兜底（v0.36.15 / 程序 0.8.10.23 / UI_LAYOUT v0.20.10）。本版同时将 BUGS 文档版本由 v0.29 升至 v0.30（表内 B-038~B-041 随 UI_LAYOUT v0.20.9 / GDD v0.36.14 已登记，此前未升 BUGS 自身版本，一并收敛）。
> v0.29 变更（2026-09-05）：新增 B-037——转职详情叠层蓝色标题条未渲染：历次补丁将 `box.add_child(header)` 丢失（header 构建完整但从未挂进弹层），且头部 Panel 无最小高度；修复=补挂载 + min height 56（v0.36.13 / 程序 0.8.10.21 / UI_LAYOUT v0.20.8）。
> v0.28 变更（2026-09-05）：新增 B-036——章节下拉弹层固定高 308px 而七行实际需 ~322px，末行「第七章 · 北伐中原」被截断一半（用户反馈「下拉框把第七章挡住了一半，过长就加一个滚动条」）；修复：弹层列表套 ScrollContainer 限高 ~262px（约 5 行）超出滚动，滚动条样式对齐概念图 .scroll（浅蓝圆角细条）（v0.36.11 / 程序 0.8.10.19 / UI_LAYOUT v0.20.5）。
> v0.27 变更（2026-09-05）：新增 B-035——v0.20.3 下拉自绘补丁三处回归（预告统计胶囊整体消失[循环漏 stats.add_child]、预留章行缺「第二章…」编号、框按钮右侧三角标缺失[Button 无 OptionButton arrow 槽]），用户真机反馈发现；修复并首次以真机鼠标点击跑通完整交互链路验证（开→禁用行无响应→选行关闭→文字保持）（v0.36.10 / 程序 0.8.10.18 / UI_LAYOUT v0.20.4）。
> v0.26 变更（2026-09-05）：新增 B-034——科技树节点点击黑色闪烁（未选中节点未覆盖 pressed/hover_pressed 样式，点击瞬间回落项目默认深色主题）；修复：未选中态同样覆盖 pressed 两样式（v0.36.8 / 程序 0.8.10.16 / UI_LAYOUT v0.20.2）。
> v0.25 变更（2026-09-05）：新增 B-033——设置页三分组面板塌缩互相重叠（Panel + 全矩形锚定内容盒不向父级传导最小尺寸，三个分组全部 0 高叠在一起、文字叠印）；改 PanelContainer 让内容撑开（v0.36.6 / 程序 0.8.10.14 / UI_LAYOUT v0.20.0）。
> v0.24 变更（2026-09-05）：新增 B-032——换肤后部分按钮「未加载」：百科图鉴切换未选中侧、背包类目筛选未选中项从未应用样式回落默认深色主题（百科仅在切换选中时才 apply）；Kenney 按钮 disabled 态无样式同样回落。修复：百科分段按钮创建即应用浅卡样式且切换时全量应用；背包类目按钮创建即应用灰 Kenney；`apply_kenney_rect_button` 补 disabled 态（灰 Kenney + 弱化字）（v0.36.5 / 程序 0.8.10.13 / UI_LAYOUT v0.19.1）。
> v0.23 变更（2026-09-05）：新增 B-031——地图编辑器窗口布局挤压：根 `MarginContainer` 为 `Window` 直接 Control 子节点但未设锚点，Godot 4 不自动拉伸 Window 的直接 Control 子节点，整个 UI 按最小尺寸挤在窗口左上角、画布仅 ~325px 宽（用户反馈「整块窗口只用了 1/4 展示」；根容器补全矩形锚点 + 默认尺寸 1604×822）（v0.35.8 / 程序 0.8.10.6 / MAP_EDITOR v0.5）。
> v0.22 变更（2026-09-05）：新增 B-029——地图编辑器窗口右上角 X 关闭无响应（Godot 4 `Window` 点关闭只发 `close_requested` 信号、不自动关闭，补连接 → `hide()`）；新增 B-030——编辑器画布裁切：`SubViewportContainer.stretch` 把 SubViewport 尺寸覆写为容器实际尺寸，1280×720 内容 1:1 渲染被裁切、16×9 网格显示不全（画布等比适配：`size_2d_override` + `AspectRatioContainer`）（v0.35.7 / 程序 0.8.10.5 / MAP_EDITOR v0.4）。
> v0.21 变更（2026-09-04）：新增 B-028——地图编辑器插件加载失败：`plugin.cfg` 指向的 EditorPlugin 脚本未标 `@tool`，Godot 编辑器启动弹「无法从路径 res://addons/map_editor/map_editor_plugin.gd 加载附加插件脚本：脚本不在工具模式下」且插件不加载（v0.35.6 / 程序 0.8.10.4 / MAP_EDITOR v0.3）。
> v0.20 变更（2026-09-04）：新增 B-026——BenchmarkRunner 跨窗布局列号不重置致塔位越出 `Path2D`、木桩被钳到路径末端整窗零伤害（每窗列号归零 + 几何越界防回归断言）；新增 B-027——破阵 40px 击退把静止木桩累计推出射程致塔停手（木桩锚点 + 每 tick 回锚，位移控制价值不计入纯 DPS 口径）（v0.35.0 / 程序 0.8.10.0）。
> v0.19 变更（2026-09-04）：新增 B-025——编队概念图武将卡「建造费用」贴片式右栏观感割裂（右侧虚线隔离竖排区如后补补丁），改卡面底部内嵌通栏费用条：金色「建造费用」小标签 + 金币数值居左、出战 / 可上阵 / 未解锁状态徽标居通栏右侧，锁定卡通栏左侧 🔒 不显费用（v0.34.3 / 程序 0.8.9.3 / UI_LAYOUT v0.17.3）。
> v0.18 变更（2026-09-03）：新增 B-024——百科武将卡第二行文本过长触发省略号截断（“舞娘 · 辅助 · 光…”式半截文本不知所云）；卡片按“放得下才写”精简，移除省略号（v0.34.2 / 程序 0.8.9.2）。
> v0.17 变更（2026-09-03）：新增 B-023——游戏百科左列 2 列网格列宽塌陷（仅默认首位貂蝉可见/可点）+ 敌人图鉴缺顶部章节选择行、左列整列竖排（v0.34.1 / 程序 0.8.9.1）。
> v0.16 变更（2026-09-03）：新增 B-022——角色图鉴数据缺口（初始武将获取方式残留 + 赵云/周仓/诸葛亮特性未配置 + 观星射程判定顺序错误）（v0.33.7 / 程序 0.8.8.7）。
> v0.15 变更（2026-09-03）：B-021 演进注记——0.8.8.3 起建造交互改为唯一拖拽建造（移除点击建造 / 绿色 + 推荐位 / 预锁位），B-021 悬停预览退役，可建反馈由拖拽虚影绿 / 红承担（v0.33.3 / 程序 0.8.8.3）。
> v0.14 变更（2026-09-03）：新增 B-020——编队双羁绊武将卡片标签选中态裁剪（「1/3」被裁成「1/」）；新增 B-021——战场可建格辨识度不足（绿色 + 含义不明、空地可建无可视提示）（v0.33.2 / 程序 0.8.8.2）。
> v0.13 变更（2026-09-03）：新增 B-019——局内遗物消耗实现与设计不符（出征即扣库存），且用户拍板（2026-09-03）遗物改永久使用、移除养成「遗物」页签并新增编队持久记忆（v0.33.1 / 程序 0.8.8.1）。
> v0.12 变更（2026-09-03）：新增 B-018——效果型大招（鼓舞/破阵激励段）未乘转职 ultimate_multiplier，凤仪/绕梁/陷阵大招倍率不生效（v0.33.0 / 程序 0.8.8.0）。
> v0.11 变更（2026-09-03）：新增 B-017——武将一转确认弹窗误用 Godot 3 旧信号名 cancelled，点击一转（如关羽→铁骑）在弹窗弹出前即报 Invalid access、确认弹窗无法出现（v0.32.2 / 程序 0.8.7.2）。
> v0.10 变更（2026-09-02）：新增 B-015——一键通关后首通解锁武将未在养成面板出现且左侧等级滞留（v0.32.1 / 程序 0.8.7.1）；新增 B-016——养成面板遗物刷新对普通道具误报资源加载错误（v0.32.1 / 程序 0.8.7.1）。
> v0.9 变更（2026-09-02）：新增 B-014——远程弹道在发射时提前计怒气/稳射计数（命中事件时机错误）（v0.31.4 / 程序 0.8.6.2）。
> v0.8 变更（2026-09-02）：新增 B-013——地图面板难度切换复选样式可多选/选中高亮不明显、出征按钮无确认可误触（v0.31.2 / 程序 0.8.6.1）。
> v0.7 变更（2026-09-02）：新增 B-012——职业级转职树落地后旧档角色绑定转职路径作废，存档 schema v1→v2 迁移清空全部 promotion_path，玩家按职业树重新转职（v0.31.0 / 程序 0.8.6.0）。
> v0.6 变更（2026-09-02）：新增 B-011——初始武将回滚（关羽恢复初始拥有、s01 首通奖励移除关羽）（v0.30.6 / 程序 0.8.5.6）。
> v0.5 变更（2026-09-02）：新增 B-010——科技树选中无高亮/解锁按钮不醒目、养成面板未解锁分组混排（v0.30.5 / 程序 0.8.5.5）。
> v0.4 变更（2026-09-02）：B-009 补记 v0.30.3 布局修正（同级并排 + 根/叶样式区分）。
> v0.3 变更（2026-09-02）：新增 B-009——科技树非树状/effect 键泄漏/重置无确认框/首通文案混淆/初始武将重复，五项 UI 与配置修复登记（v0.30.2 / 程序 0.8.5.2）。
> v0.2 变更（2026-09-01）：新增 B-008——职业技能参数分层读取取默认值、青龙偃月击杀返冷却被覆盖、辅助经验未按塔实例去重，三项修复登记（v0.29.0 / 程序 0.8.5.0）。
> 约定：**每次 bug 修复必须新增条目并关联版本号**（文档版本 vX.Y.Z + 程序版本 0.阶段.提交.修复）；修复涉及设计结论时同步回写对应模块文档与总纲（见 [AGENTS.md](../../AGENTS.md) 第 2 节）。
> 历史条目（v0.27.5 及以前）由总纲 changelog 回填，细节以总纲为准；自 v0.27.6 起强制登记。

## 条目格式

| 编号 | 关联版本 | 问题描述 | 复现步骤 | 根因 | 处理方式 | 状态 |
|---|---|---|---|---|---|---|

## 修复记录

### 历史回填（v0.27.5 及以前）

| 编号 | 关联版本 | 问题描述 | 复现步骤 | 根因 | 处理方式 | 状态 |
|---|---|---|---|---|---|---|
| B-001 | v0.19.6（程序 0.9.0-alpha） | 打包版目录扫描失败，资源读取异常 | 运行打包版 exe | 目录扫描未兼容 PCK 打包结构 | 目录扫描修复，PCK 兼容 | 已修复 |
| B-002 | v0.21.1（程序 0.8.1.1） | 手动通关时首通英雄解锁/掉落可能丢失 | 手动击败最后一波后结算 | 手动通关与一键通关的奖励收集/结算顺序不一致 | 手动通关先收集奖励再结算（顺序统一）；BattleSession 结算窗口放宽；新增常驻回归测试 UnlockReproRunner | 已修复 |
| B-003 | v0.16.0 | 关卡难度选择未生效 | 面板选难度后进入关卡/一键通关 | 面板难度未随关卡进入生效 | 面板难度随关卡进入、一键通关按所选难度生效 | 已修复 |
| B-004 | v0.10.0 | FlowRunner 流程测试断言空跑 | 流程测试全绿但用例未真实执行 | 测试协程未 await，异步空跑 | FlowRunner 补齐 await，断言真实执行 | 已修复 |
| B-005 | v0.11.2 | 怒气获取模式互相污染 | 刘备与张飞共享职业时配置覆盖 | `rage_gain_mode` 放职业级，角色级覆盖互相污染 | 角色级覆盖移至 `CharacterData`（`rage_gain_mode_override`） | 已修复 |
| B-006 | v0.25.1 | `CharacterData.initial_skill_ids` 死字段残留 | 代码扫描/存档检查 | Stage 0"每个武将天生技能"方案遗留，运行时从未读取 | 删除字段声明与 9 名武将 `.tres` 占位配置；修正 CHARACTERS.md 4.1 / 阶段 5 过期表述 | 已修复 |
| B-007 | v0.22.0 | 局内遗物消耗时机导致失败/退出也损失 | 进入战斗即消耗遗物，失败或退出后库存减少 | 消耗时机过早 | 调整为胜利结算后消耗（失败/退出/崩溃不扣） | 已修复 |

### v0.27.6 起（强制登记）

| 编号 | 关联版本 | 问题描述 | 复现步骤 | 根因 | 处理方式 | 状态 |
|---|---|---|---|---|---|---|
| B-014 | v0.31.4（程序 0.8.6.2） | 远程攻击（弓箭手/术士弹道、投石车抛射）在发射瞬间即计怒气、触发技能命中钩子（稳射计数/追加）与命中型效果；子弹飞行中目标死亡/丢失或空爆时仍积怒；追加伤害先于箭矢视觉生效 | 远程塔普攻后立即查看怒气/稳射计数；目标飞行中被其他塔击杀 | 命中事件挂在发射执行器（BehaviorRegistry），真正伤害在 Bullet 命中/爆炸时才结算 | 命中事件拆分：发射只创建弹道并锁定伤害；怒气 / `SkillRegistry.on_attack_hit`（稳射等）/ 命中型特性统一改在子弹实际命中造成伤害后由 `Tower.notify_attack_damage_dealt` 触发；AOE 攻击级事件每发一次（命中≥1 才计，空爆不计），大招弹道不接普攻命中事件；击杀箭计入命中计数、追加顺延至目标存活的下一次命中 | 已修复 |

| B-015 | v0.32.1（程序 0.8.7.1） | 一键通关（测试）首通解锁的武将未出现在武将养成面板（图鉴仍置灰显示获取方式），且左侧已拥有卡片等级滞留构建时值（右侧详情已正确显示 6 级、左侧仍 Lv.1） | 新档 → 大厅「地图选择」连续一键通关 s01~s05（全员 Lv.1→6，解锁张飞等 5 将）→ 切到「武将养成」 | GameHub 常驻五个面板：DevelopPanel 仅在 _ready 构建时装载已拥有/未解锁列表与左侧卡片文本，_on_shown 只刷新右侧详情，战斗/一键通关后的解锁与升级不回流左侧 | _on_shown 改为整体重载：重取档案 → 重算已拥有/未解锁 → _rebuild_left_list 重建左侧（先摘除再释放，避免当帧新旧按钮并存）→ 保持/回退选中；_refresh 末尾 _sync_owned_button_labels 同步卡片等级（练兵令直接升级场景）；新增常驻回归测试 DevelopRefreshRunner（一键通关 5 关后切回养成：7 名已拥有、未解锁仅剩 2 名、刘备/关羽卡片 Lv.6） | 已修复 |
| B-016 | v0.32.1（程序 0.8.7.1） | 持有普通材料（如黄巾布）时打开武将养成面板持续报 `Cannot open file 'res://resources/battle_relics/yellow_turban_cloth.tres'` 加载错误 | 获得黄巾布后进入养成面板（每次刷新都会执行遗物库存扫描） | GameFlow.load_battle_relic_data 直接 load() 未先检查资源存在，背包内普通道具也被当作遗物尝试加载 | 仿 load_item_data 先 ResourceLoader.exists 再 load，非遗物道具返回 null、不再产生加载错误 | 已修复 |
| B-017 | v0.32.2（程序 0.8.7.2） | 武将养成点击一转（如关羽→铁骑）立即报错 `Invalid access to property or key 'cancelled' on a base object of type 'ConfirmationDialog'`，确认弹窗无法弹出 | 养成面板选中关羽（等级 10、黄巾布 10）→ 点击「转职为铁骑」 | 误用 Godot 3 旧信号名 `cancelled`（Godot 4 改名 `canceled`）；连接不存在的信号在弹窗弹出前即抛错，popup 永不执行 | 改用 Godot 4 `canceled` + `close_requested` 释放；新增常驻回归测试 PromoteDialogRunner——弹出可见、取消/右上角关闭不写入转职、确认后 promotion_path 写入铁骑并扣除 10 黄巾布 | 已修复 |
| B-018 | v0.33.0（程序 0.8.8.0） | 效果型大招（舞娘鼓舞、虎贲破阵激励段）未乘转职 `ultimate_multiplier`——凤仪大招 ×1.2 / 绕梁 ×1.15 / 陷阵激励段 ×1.25 实际不生效（伤害型大招正常） | 转职凤仪/陷阵后释放大招，对比未转职的友方 buff 强度 | NUMBERS 10.5 规定大招「伤害/效果」均乘转职倍率，但实现中仅伤害型大招经 `ultimate_power()` 接入 `ultimate_multiplier`，效果段只乘局内升阶 buff 倍率 | `Tower` 新增 `ultimate_effect_power()`（返回 `ultimate_multiplier`），鼓舞与破阵激励段的 power_mult 乘入；SmokeRunner 新增提交 8 断言（凤仪鼓舞 1+0.3×1.2=1.36、陷阵激励 1+0.15×1.25=1.1875） | 已修复 |
| B-020 | v0.33.2（程序 0.8.8.2） | 编队卡片羁绊标签行被裁剪——双羁绊武将（关羽/张飞）标签行实测宽 186px，卡片固定宽 200px 未选中已贴边；选中后金色 pressed 样式内边距更大（content margin 左右各 10 + 边框 2，可用约 176px），行尾「1/3」被裁成「1/」 | 编队页点击关羽/张飞卡片，观察标签行行尾 | 卡片固定宽 200px < 双羁绊标签行 186px + 选中态内边距增量；按钮文本只在 `_build_ui` 构建一次，勾选后不随羁绊预览计数刷新 | 卡片加宽至 **232px**（4 列网格 1280 宽仍一屏容纳）；文本构建收敛为 `_character_button_text()`，`_refresh()` 末尾 `_refresh_character_button_texts()` 全量刷新——羁绊 X/Y 预览随勾选实时更新，不再依赖构建时快照 | 已修复 |
| B-021 | v0.33.2（程序 0.8.8.2） | 战场可建格辨识度不足：「绿色 +」无说明、易被误读为唯一可建点；空地棋盘格与地形/背景区分弱，悬停与点击前看不出哪里能建、哪里不能建 | 进入战斗观察地图与开局引导，按文案点「绿色 +」建造 | 阶段 8 提交 3 起为自由建造（任意空地可建）后，推荐位降级为软引导但视觉与文案未澄清（引导仍写「点击绿色 + 建造」）；空地无 hover 可建提示 | 开局引导文案改为「选择武将后点击空地建造（绿色 + 为推荐位，任意空地均可建）」；选中武将后鼠标扫过可建格显示**半透明绿色悬停预览**（道路/禁建/已占用格不显示；与两次点击待确认高亮互斥，未选武将/战后不显示） | 已修复（v0.33.3 起建造改为唯一拖拽，悬停预览随点击建造退役，可建反馈由拖拽绿/红虚影承担） |
| B-022 | v0.33.7（程序 0.8.8.7） | ①初始武将刘备/关羽 `unlock_stage_id` 残留 `ch01_s01`——图鉴/灰卡获取方式将误显「通关桃园起兵·涿郡首通解锁」，与"初始武将、s01 无首通武将奖励"（B-011/v0.30.6 回滚）矛盾；②赵云/周仓/诸葛亮 `.tres` 缺 `trait_id`（龙魂/统军/观星局内不生效，与 CHARACTERS 4.7 / BEHAVIORS B.3.4 规格不符）；③诸葛亮观星射程 +12% 恒不生效 | ①百科/养成查看关羽、刘备获取方式；②局内建造赵云/周仓/诸葛亮后触发特性；③诸葛亮 1 级塔查看射程 | ①v0.30.6 回滚初始武将时只改代码与关卡奖励，角色资源 `unlock_stage_id` 未清空；②v0.11.3 新增赵云/周仓/诸葛亮时特性字段未铺设（关羽/张飞/黄忠/刘备/貂蝉/皇甫嵩已配）；③`Tower.apply_character` 中观星射程判定（第 216 行）先于 `_trait_id` 赋值（原第 256 行）执行 | ①清空 guan_yu/liu_bei 的 `unlock_stage_id`（空值经 `GameFlow.get_acquisition_text` 显示「初始解锁」，与 `INITIAL_CHARACTER_IDS` 一致）；②zhao_yun/zhou_wei/zhuge_liang 补 `trait_id` + `trait_params`（龙魂：+4%/层、上限 10；统军：阈值 70%、+30%；观星：射程 +12%、范围减速 8%），默认数值与 CHARACTERS 4.7 一致；③特性赋值提前至 `apply_character` 属性计算前，龙魂/统军硬编码数值参数化（读 trait_params，默认值不变）；SmokeRunner 新增 0.8.8.7 数据完整性断言（9 名角色 trait 齐全 + 初始武将文案 + 观星射程实算） | 已修复 |
| B-023 | v0.34.1（程序 0.8.9.1） | ①武将图鉴左侧只见默认首位（貂蝉）数据，其余 8 将不可见、无法点选切换；②敌人图鉴左侧为整列竖排全宽卡片（规格应为 2 列网格），且视图顶部缺少章节选择行 | ①大厅 → 百科，武将图鉴页观察左侧 9 将网格并尝试点选其余武将；②切至敌人图鉴页观察左列布局与顶部 | ①`EncyclopediaPanel._rebuild_left_list` 网格单元格未设 `SIZE_EXPAND_FILL`，GridContainer 按单元格最小宽度（≈8px）排布、不铺满 300px 左栏，卡片文字裁切 → 用户只看得见右侧默认选中的貂蝉详情；②敌人列表用 VBox 直排全宽 Button（未按 UI_LAYOUT §12「2 列网格」落地）；敌人页未实现章节选择行（地图选关已有同类控件） | ①左栏 ScrollContainer 禁横向滚动，武将/敌人统一为铺满式 2 列网格（网格与单元格均 `SIZE_EXPAND_FILL`，卡片 146×88、字号 15 防溢出），点选切换/选中态经 `_sync_selected_buttons` 同步；②敌人图鉴视图新增顶部章节选择行——第一章可点、后续章节灰显「敬请期待」（复用 `MapPanel.RESERVED_CHAPTERS` 单源，与地图选关一致）；③FlowRunner 增补 B-023 回归断言（武将/敌人卡列宽 ≥100px、9 将/7 敌计数、章节行随视图显隐） | 已修复 |
| B-026 | v0.35.0（程序 0.8.10.0） | BenchmarkRunner 跨窗布局几何越界致整窗零伤害：`_layout_col` 跨窗只增不减、塔位 x 不断右移，超过 `Path2D` 长度（20000）后 `PathFollow2D` 把木桩 progress 钳到路径末端，桩距塔数万像素超出射程，整窗采样 DPS≈0（基准 1 后半段假性 `FAIL`） | 全量跑 BenchmarkRunner 至多窗场景 | `_teardown_window` 未重置布局列号，几何随窗右移无界；`Path2D` 对超长 progress 静默钳制到末端 | `_teardown_window` 每窗重置 `_layout_col` = 0；`_make_group` 增加几何越界防回归断言（origin.x + 射程 < 18000、路径 20000 内）；每窗布局自包含 | 已修复 |
| B-027 | v0.35.0（程序 0.8.10.0） | BenchmarkRunner 位移型技能污染纯 DPS 口径：虎贲「破阵」大招 40px 击退把静止木桩累计推出射程、塔停手，后续窗 DPS 偏低/波动（真实敌人会继续前进不受影响） | 基准中含破阵大招（张飞/虎贲）的窗口采样 | 纯 DPS 基准要求木桩静止，但位移技能对静止桩的击退持续累积（40px × 多次释放可推出射程） | 木桩生成时记录锚点（`bench_home_x` / `bench_home_pos` meta），`_simulate_window` 每 tick 调 `_reanchor_dummies` 把木桩 progress/坐标拉回锚点（死亡/待删桩跳过）；位移控制价值不计入纯 DPS 口径，由真实战斗表现验收 | 已修复 |
| B-028 | v0.35.6（程序 0.8.10.4） | 地图编辑器插件无法加载——Godot 编辑器启动弹警告「无法从路径 res://addons/map_editor/map_editor_plugin.gd 加载附加插件脚本：脚本不在工具模式下」，工具菜单无「地图编辑器」入口 | 0.8.10.3 启用插件后打开 Godot 编辑器 | Godot 4 要求 `plugin.cfg` 指定脚本为工具脚本（`script.is_tool()`）才允许作为 EditorPlugin 加载；M1 三个 addon 脚本均未加 `@tool` 注解 | 三个脚本（`map_editor_plugin.gd` / `map_editor_overlay.gd` / `map_editor_window.gd`）头部补 `@tool`（插件在编辑器内实例化窗口与覆盖层，一并标注）；headless 编辑器验证插件 `_enter_tree` 正常执行、无加载/脚本错误 | 已修复 |
| B-029 | v0.35.7（程序 0.8.10.5） | 地图编辑器窗口右上角 X 点击无响应，窗口无法关闭（只能停用插件） | 打开 Godot 编辑器 → 项目 → 工具 → 地图编辑器 → 点窗口右上角 X | Godot 4 `Window` 节点点原生关闭按钮只发 `close_requested` 信号、不自动隐藏/释放；M1 未连接该信号，窗口保持打开 | `map_editor_window.gd` 连接 `close_requested` → `hide()`（实例与已载入状态保留，工具菜单再次打开时复用原实例重新弹出）；新增 MapEditorRunner「发射 close_requested → 窗口隐藏」断言 | 已修复 |
| B-030 | v0.35.7（程序 0.8.10.5） | 编辑器画布显示与游戏不符：窗口内画布区域小于 1280×720 时地图右侧/底部被裁切，16×9 网格显示不全（用户反馈「地图整体大小与网格数量与游戏对不上」）；画布区域大于 1280×720 时则 1:1 渲染留空边、不随窗口缩放 | 打开地图编辑器，观察画布网格数量（缺列/缺行）与游戏内战场对比 | `SubViewportContainer.stretch = true` 把 SubViewport 实际尺寸覆写为容器尺寸（非 1280×720），而 GridBackground 内容按 1280×720 逻辑坐标 1:1 绘制——小则裁切、大则留边，均非等比缩放 | SubViewport 设 `size_2d_override = (1280, 720)` + `size_2d_override_stretch = true`（内容恒按 1280×720 逻辑分辨率布局、缩放铺满视口），SubViewportContainer 外包 `AspectRatioContainer`（16:9 FIT 居中）保比例不变形；数据面板补「网格 16×9（80px/格 · 1280×720）」规格行；MapEditorRunner 增补画布结构断言（override 值 / AspectRatioContainer 容器链） | 已修复 |
| B-043 | v0.36.17（程序 0.8.10.25） | 转职详情叠层与概念图 ui_develop_promo.html 偏差多处：二转分支标题需求误用一转数据（Lv 10 · ×10，实需 Lv 20 · ×20）；已一转后基础节点仍写「基础职业 · 未转职」；图例写「绿点/金点/灰点」而画面为 ↓ 箭头；已一转后二转两分支未并排仍全宽堆叠；分支卡缺强化/新技能数值详情；节点名称 16px/act 按钮宽不符概念；叠层外框与头部标题未对齐 | 武将养成 → 职业页签 → 转职详情（未转 / 已一转 / 已二转三态查看） | 转职树渲染按旧卡片结构拼接：分支预览只在未转职态生效、真实二转候选仍纵向堆叠；分支需求文案复用一转候选数据；节点无 step 浅字/无概念 `.fx` 数值行；外框沿用通用弹窗样式 | `_refresh_promotion` 与节点卡重构：基础节点 tag/step 分层；分支行（预览灰锁 / 真实候选灰锁或金可转职）一律两卡并排各半宽；`.fx` 强化线由 `skill_params` 数据驱动拼增量、新技能线拼 SECOND_SKILL_HINTS；名称 18px + 定宽 act（180/176/128）；叠层外框/头部按概念对齐；几何探针三态横向零越界；回归 PromoteDialog / Smoke 绿 | 已修复 |
| B-044 | v0.36.18（程序 0.8.10.26） | 转职详情叠层与概念图仍有整体偏差：①顶部虚线长胶囊缺失、说明文字挤在左侧；②蓝头 who 角色信息被挤到最右（应在左侧紧贴「转职详情」标题）；③中间面板宽高对不上（未整窗居中、偏高） | 武将养成 → 职业页签 → 转职详情（未转 / 已一转 / 已二转三态查看） | 叠层挂在养成内容区（~988px 宽）子节点上，1160 面板无法整窗居中、右缘越窗且外框 660 偏高；顶部图例/底部说明用单色横条非概念虚线胶囊；蓝头单色、标题 expand 把 who 挤到最右；`.ptree` 无竖轨 | 叠层改挂 GameHub 顶层整窗居中、外框 1160×570（x 60..1220 / y 75..645）；蓝头改垂直渐变 #46b7ea→#1c9fd7 + who 胶囊紧贴标题 + ✕ 贴右；`.ptip`/`.pfoot` 新增 DashedPill 自绘 1px 虚线长胶囊（pfoot 弱色文字）；树改 PromoTreeRail 3px 竖轨 + 状态点骑轨（核心+白边+状态色外环）；节点卡去头像圆；几何探针三态横向零越界、纵向一屏放下；回归 PromoteDialog / Smoke 绿 | 已修复 |
| B-045 | v0.36.20（程序 0.8.10.28） | 科技树版式与概念图 ui_tech 偏差：GridContainer 每行 8 格占位列把连接带竖线挤错列折行、层级标签落进 26px 连接带且文字挤左；分类页签/顶栏/图例、军略链序、节点卡三态色、终极层提示条、详情第 3 pill 不符概念 | 大厅 → 科技树，逐类查看树/节点/详情（军略/后勤/工事/将略） | GridContainer 列宽由占位/内容撑开且占位无最小宽，竖线与节点列错位；层级标签横排随行高拉伸落带；TabContainer 与概念 .tabs 按钮行不一致 | 树改行式（52px 竖排层级胶囊 + 节点 HBox 行 + 26px 连接带独立行逐链画竖线，卡宽 (1000−52−链×8)/链 等分、118~480 钳制）；军略链序按概念；节点卡概念三态（#f2fbf5 绿/#fffdf0 金/#eef3f7 灰 + 3px #52c48a/#ffcc00/#cddbe6 描边）+ 左上层级符/右上状态字/底部消耗行；军略终极层 .t3-note DashedPill；详情补「职业加成合计 +9%（上限 +10%）」第 3 pill 与「军略 · 职业强化」chip；顶栏一行 + .tabs Kenney 按钮 + .legend 图例；保留「科技点+50（测试）」测试钮；几何探针零越界；回归 Smoke 绿 | 已修复 |
| B-046 | v0.36.21（程序 0.8.10.29） | 科技树 v0.20.15 重构回归：①点击/切换科技节点瞬间黑闪 ②详情「解锁」按钮点击无反应 | 大厅 → 科技树：反复点击不同节点；选中可解锁节点点击「解锁」 | ①非选中分支 remove 掉 pressed/hover_pressed 覆盖，按下瞬间回落主题默认深色 pressed（B-034 同款）；②重构漏 `_unlock_button.pressed.connect`，按钮无信号处理 | ①非选中分支保留 pressed/hover_pressed 状态底色覆盖（仅 set_pressed_no_signal(false)）；②补回 `_unlock_button.pressed` → `_on_unlock_pressed`（unlock_tech + 存档 + _refresh）；新增 tests/TechUnlockRunner headless 点击链路回归；Smoke 绿 | 已修复 |
| B-047 | v0.36.22（程序 0.8.10.30） | 百科两屏与概念图 ui_encyclopedia.png / ui_encyclopedia_enemy.png 终版偏差：武将列表顺序与卡副行口径、详情头部虚线分隔与 chips、基础页 stat 卡、模拟器位置、敌人方形渐变头像与副行、敌人详情卡化与出现关卡摘要 | 大厅 → 百科：武将/敌人图鉴逐项对照概念图 | 武将列表按资源 id 字典序、卡副行用战斗定位词；头部无虚线分隔；模拟器内嵌详情；敌人卡无方形头像、详情未卡化 | DashedLine/EnemyAvatar 自绘（2px 虚线、方形对角渐变）；CHARACTER_ORDER/CHARACTER_FLAVOR/ENEMY_LOCATIONS/ENEMY_TACTIC_SUB 概念字典；模拟器改底部整行 .sim 卡（升阶 0~3 + 三枚金色结果卡）；敌人详情五统计卡 + 难度卡行 + 出现摘要；EncShotProbe 零越界；回归 FlowRunner 百科段 / Smoke 绿 | 已修复 |
| B-048 | v0.36.23（程序 0.8.10.31） | 百科 v0.20.17 对齐后残留偏差：分段按钮误用浅金卡选中（概念 .segbtn Kenney 蓝选中/灰未选）；头像全蓝非概念九色；CHARACTER_ORDER 概念序笔误；默认选中未对齐概念稿；EnemyAvatar 渐变白屏（Compatibility 渲染器纹理上传时序） | 大厅 → 百科逐项对照概念图；探针同参数 TextureRect 正常而 EnemyAvatar 全白 | 分段按钮样式回归；avatar_label 仅 7 色键且恒传 "blue"；顺序字典第 4/5 位对调；默认取列表首位；渐变纹理在 _draw 内现建同帧画不上 | 分段按钮改 apply_kenney_rect_button 蓝/灰；avatar_label 补 brown/pink/teal + CHARACTER_AVATAR_COLORS 九色映射；CHARACTER_ORDER 更正；默认选中张飞/黄巾渠帅·张梁；EnemyAvatar setup 预生成 ImageTexture 缓存绘制；EncShotProbe/FlowRunner 断言同步 | 已修复 |
| B-049 | v0.36.24（程序 0.8.10.32） | 百科武将图鉴页签标题带「Scroll」后缀（页签名「基础Scroll…」）；详情页签内容文字紧贴页签浅蓝描边（左 3px/上 4px，概念 .tabbody padding 10px 14px 为 17px/13px） | 大厅 → 百科 → 武将图鉴：看页签名；看「基础」页内容距边框 | TabContainer 页签标题默认取直接子节点名，滚动容器直接作页签子节点致标题带其名字后缀；页签内容无 .tabbody 内边距 | `_make_tab` 改 `set_tab_title` 显式设标题（基础/技能/转职/信物/特性，不含节点名）；页签滚动内容外包 MarginContainer（margin 14/14/10/10 + 横向 EXPAND_FILL）对齐概念 padding；EncShotProbe 断言页签标题 + 逐页签零越界；像素核验文字距边框 17px = 概念图 | 已修复 |
| B-050 | v0.37.1（程序 0.8.11.0） | 角色技能 A 型冷却环脱战冻结：塔释放技能后冷却期间无目标（脱战）时，塔身冷却环弧线停留旧值，而实际冷却在走 | 塔释放 A 型角色技能后脱战观察塔身冷却环 | 冷却环绘制仅随攻击闪光/挥击/选中等事件 queue_redraw；脱战无目标时无重绘事件源 | `Tower._process` A 型冷却递减期间每帧 `queue_redraw()`（环随剩余比例平滑收缩、就绪隐藏）；顺带 spine 角色环半径 36→40 防遮挡（提交 11 落地，关联 UI_LAYOUT v0.20.22 / ART_ASSETS v0.8） | 已修复 |
| B-042 | v0.36.15（程序 0.8.10.23） | 武将养成职业页签内容横向溢出：整页内容右溢详情面板约 80px（末段顶出窗口右缘）——用户反馈「详情面板都超出去了」 | 武将养成 → 职业页签（未一转武将） | 转职进度卡三枚 chips（下一转 / Lv 达标 / 材料）用不可换行 HBox 与「转职进度」标题、176px 按钮同行，单行最小宽 ~660px 撑破页签体（~602px），且职业页签内容未套滚动容器 | chips 改 HFlowContainer 自动折行（对齐概念 `.jchips` flex-wrap:wrap）；职业页签内容套纵向 ScrollContainer（与技能页签一致，超高滚动兜底）；几何探针核验页签最小宽回落容器内、越界计数归零 | 已修复 |
| B-038 | v0.36.14（程序 0.8.10.22） | 武将养成左列表卡刷新污染：卡片文本变「关羽/骑兵 · Lv.1/骑兵/Lv1」、姓名/职业错位、等级不更新 | 选中武将后用练兵令直接升级或切回面板刷新 | `_sync_owned_button_labels` 按 `find_children` 返回顺序覆盖——头像 Label 排在首位被写成全名、职业行写成「职业 · Lv.x」、等级 Label 从未更新 | 卡片关键 Label 命名 CardName/CardSub/CardLv，刷新按名更新；DevelopRefreshRunner 断言改按新卡片结构逐卡校验（姓名/职业/等级标签 + 头像字不被覆盖） | 已修复 |
| B-039 | v0.36.14（程序 0.8.10.22） | 转职详情叠层二转分支两卡纵向全宽堆叠，未按概念 `.brow` 并排 | 未一转武将打开转职详情查看二转预览 | 分支卡 `add_child` 误挂到转职树容器而非 `branch_row`（branch_row 建立后从未使用） | 分支卡改挂 `branch_row` 并排；分支卡补 二转·强化线/新技能线 tag、数值行（含大招倍率）、Lv/材料 条件胶囊（满足绿 / 未满足灰） | 已修复 |
| B-040 | v0.36.14（程序 0.8.10.22） | 职业页签与概念不符：身份卡写死虎贲文案且绿边、技能未习得 tag 用黄、进度卡竖排缺完整路线、职业页签无可转提示 | 切换职业页签 / 查看未转职武将 | 身份卡描述硬编码单职业文案；tag 色与结构未按 ui_develop_job.html；Godot 4 TabBar 不暴露内部 tab 按钮无法挂徽标 | 身份卡改浅白卡（职业 tags 中文映射定位 tag + 克制 lock tag + 右状态列，描述取职业数据文案）；未习得改灰 lock；转职进度卡改横向 chips + 完整路线说明 + 176×46 按钮；「可转」提示以页签标题后缀近似 | 已修复 |
| B-041 | v0.36.14（程序 0.8.10.22） | 信息卡攻速口径与概念不符（概念 0.67s 攻击间隔，游戏显示 1.43 次/秒） | 查看武将养成右侧基础信息卡统计格 | 攻速沿用次/秒口径 | 改显示攻击间隔秒（`stats.attack_interval` 保留两位小数 + s） | 已修复 |
| B-037 | v0.36.13（程序 0.8.10.21） | 转职详情叠层蓝色标题条未渲染——弹层顶部直接是转职树节点，无「转职详情」蓝头 | 打开武将养成 → 职业 → 转职详情 | 历次补丁将 `box.add_child(header)` 丢失，header 构建后从未挂入弹层树；且头部 Panel 无内容传导最小高度为 0 | 补 `box.add_child(header)`；头部 Panel 设 min height 56；窗口化探针截图核验蓝条+白字+右侧圆形✕渲染 | 已修复 |
| B-036 | v0.36.11（程序 0.8.10.19） | 章节下拉弹层高度固定 308px，七行章节实际需 ~322px，末行「第七章 · 北伐中原」被截断一半 | 打开地图选关章节下拉，观察末行 | 弹层高度按行数硬算且行高漏计分隔间距（7×42+14=308 < 实际 7×46-4+12≈322），且无滚动兜底 | 列表盒套 ScrollContainer（横向禁用），弹层限高 262px（约 5 行）超出滚动；滚动条按概念图 .scroll 样式（10px 浅蓝圆角细条，悬停亮蓝）；探针验证滚动到底末行完整可见 | 已修复 |
| B-035 | v0.36.10（程序 0.8.10.18） | v0.20.3 下拉自绘补丁三处回归：①关卡预告统计胶囊整体消失（用户「胶囊被你吃了」）②预留章行缺「第二章/第三章…」编号 ③下拉框右侧向下三角标缺失 | 真机打开地图选关，点开章节下拉、观察预告条 | ①重写胶囊循环时漏掉 `stats.add_child(pill)`，胶囊创建后从未挂入列表（headless 树检查 children=0）②预留行只显示名称未按概念图编号 ③普通 Button 没有 OptionButton 的 arrow 主题槽，`add_theme_icon_override("arrow")` 无效 | 补 `stats.add_child(pill)`；预留行按概念图编号「第N章 · 名称」（N=index+2）；三角标改右锚定 TextureRect 子图标随按钮 resized 重定位；首次真机鼠标点击验证完整交互链路（开→禁用行无响应→选行关闭→文字保持） | 已修复 |
| B-034 | v0.36.8（程序 0.8.10.16） | 科技树点击科技块瞬间黑色闪烁（用户反馈「点击科技块时有黑色闪烁」） | 科技树页点击任意未选中节点 | 节点为 toggle 按钮，未选中时只覆盖 normal/hover 样式，点击瞬间 pressed/hover_pressed 回落项目默认深色主题再被 _refresh 覆盖，产生黑闪 | 未选中态同样覆盖 pressed/hover_pressed 样式（与 normal 一致），点击不再闪黑 | 已修复 |
| B-033 | v0.36.6（程序 0.8.10.14） | 设置页三个分组面板塌缩为 0 高互相重叠，「显示/音频/玩法」标题与行文字全部叠印（用户反馈「设置页面字体混乱」） | 打开大厅 → 设置页观察分组布局 | 分组用 Panel + 全矩形锚定的内容 VBox——锚定子节点不向非容器父级（Panel）传导最小尺寸，分组面板最小高度为 0 | 分组改 PanelContainer（内容撑开传导最小尺寸），标题/行/辅助文字字号层级统一（组题 16 墨蓝/行 15/辅助 13 弱色），CheckButton 补悬停/按下态字色 | 已修复 |
| B-032 | v0.36.5（程序 0.8.10.13） | 换肤后部分按钮「未加载」：百科图鉴切换未选中侧（敌人图鉴/武将图鉴）、背包类目筛选未选中项呈默认深色主题，点击后样式才出现；Kenney 按钮 disabled 态同样回落深色 | 打开百科/背包/地图选关观察未选中与禁用按钮 | 百科图鉴切换只在按钮被选中时才 apply 浅卡样式，未选中侧与初始双按钮均无样式；背包类目按钮初始仅「全部」有样式；`apply_kenney_rect_button` 未覆盖 disabled stylebox，禁用态回落项目默认深色主题 | 分段/筛选类按钮改为创建时即应用样式、切换时全量重应用（选中与未选中）；`apply_kenney_rect_button` 补 disabled 样式（灰 Kenney + DISABLED 字色） | 已修复 |
| B-031 | v0.35.8（程序 0.8.10.6） | 编辑器窗口整体只占左上角一小块——画布实测仅 ~325×183，其余大量空白（用户反馈「整块这么大只用了 1/4 位置展示，其他干嘛用的」），数据面板/顶栏同被压缩 | 打开地图编辑器窗口，观察画布与右侧数据面板占窗口比例 | 根 `MarginContainer` 是 `Window` 直接 Control 子节点但从未设置锚点——Godot 4 `Window` 不会自动拉伸直接 Control 子节点，默认 anchors 全 0 → 根容器按最小尺寸（顶栏最小宽 ~633px）挤在左上角，画布被压成 ~325px 宽；B-028 前的画布裁切观感与 B-030 后的「画布偏小」同源于此（B-030 只修画布等比缩放，未触及布局挤压根因） | 根容器 `set_anchors_and_offsets_preset(PRESET_FULL_RECT)` 全矩形锚点（随窗口缩放铺满）；窗口默认尺寸调为 1604×822（1280×720 画布恰好 1:1 零留白 + 300px 数据面板 + 顶栏/状态条），min_size 放宽 1024×640 兼容小屏；真实渲染窗口化探针截图核验（1197×662 / 1766×993 两档画布均铺满、16×9 全网格可见）；MapEditorRunner 增补根容器全矩形锚点断言 | 已修复 |
| B-025 | v0.34.3（程序 0.8.9.3） | 编队概念图（`docs/ui_concept/ui_squad.png`）武将卡「建造费用」做在卡片右侧虚线隔离的贴片区（fk 标签 / fv 数值 / 状态徽标竖排），视觉像后补上去的补丁、与卡面割裂 | 打开 ui_squad.png 观察任意已拥有武将卡 | 概念图早期把费用当附属信息塞进卡片右侧独立栏，未按卡面一体构图设计 | 费用改**卡面底部内嵌通栏**：细虚线上承、与卡面同底色同圆角一体；金色「建造费用」小标签 + 金币数值居左，出战 / 可上阵 / 未解锁状态徽标居通栏右侧；锁定卡不显费用、通栏左侧 🔒。`ui_squad.html` 与 PNG 同步覆盖更新（Chrome headless 重渲逐源一致）；UI_LAYOUT v0.17.3 回写卡面费用展示规则（禁止贴片式右栏） | 已修复（概念图口径；局内卡当前不展示费用，落地随换肤另行排期） |
| B-024 | v0.34.2（程序 0.8.9.2） | 武将图鉴左侧卡第二行文本过长触发省略号截断（如「舞娘 · 辅助 · 光…」「弓箭手 · 输出 · 单…」），半截内容不知所云 | 大厅 → 百科，武将图鉴页查看左侧卡片 | 卡片第二行 = 职业名 + 完整定位串（含攻击方式，如「辅助 · 光环」），在 146px 卡片内放不下即被 `OVERRUN_TRIM_ELLIPSIS` 截断；硬塞长文本 + 省略号无信息量 | 卡片文本按宽度精简：武将卡第二行改为「职业名 · 战斗定位词」（输出/控制/辅助/混合，单字词完整放下），攻击方式 / 克制等长信息保留在右侧「基础」页与头部；武将/敌人卡片移除省略号截断；FlowRunner 增补「卡片文本不含 …」回归断言 | 已修复 |
| B-019 | v0.33.1（程序 0.8.8.1） | 局内遗物在编队页确认出征时即消耗（`consume_squad_relics` 调用），无视 v0.22.0"胜利结算后消耗"规则、失败/退出同样扣库存；且遗物效果偏弱、定位可有可无 | 选带遗物出征 → 背包库存立刻减少；失败或退出后库存仍未恢复 | 消耗时机实现与 v0.22.0 设计不一致（v0.19.0 遗留的"确认即扣"逻辑未随 v0.22.0 拍板收敛）；用户拍板（2026-09-03）遗物改为常驻构筑件、永久使用 | 遗物改**永久使用**：获取即永久解锁、选带不消耗库存（移除 `consume_squad_relics` 调用链），v0.19.0"出战消耗"与 v0.22.0"结算后消耗"规则一并废弃（强度维持低值预算）；移除武将养成「遗物」页签（队伍级：选带在编队页、库存/效果在背包页）；新增编队持久记忆（存档 `squad_character_ids` / `squad_relic_ids`，确认出战写入、再次出征自动预填）；同步 UI_LAYOUT v0.10 / CHARACTERS 4.8 / NUMBERS 10.8·10.10 / GDD v0.33.1 | 已修复 |
| B-013 | v0.31.2（程序 0.8.6.1） | 地图面板难度切换为复选按钮（可同时点亮多个、再点当前项会取消选中、选中高亮不明显）；出征按钮点击即进入编队，无确认可误触 | 选关面板切换难度 / 点击出征 | 难度按钮使用 toggle_mode 未加 ButtonGroup 互斥与选中样式；出征未设二次确认 | 难度切换改 ButtonGroup 互斥单选 + 金色选中样式（UITheme.apply_selected_style）；出征新增确认弹窗（展示关卡与难度，确认后进入编队）；一并移除轻松难度（难度档位数据驱动 difficulty_presets，仅标准/困难） | 已修复 |
| B-012 | v0.31.0（程序 0.8.6.0） | 职业级转职树落地：旧 11 个角色绑定转职资源删除，旧档 `promotion_path` 仍指向已废弃 ID，加载后会取到 null 转职 | 旧档升级后查看养成/编队转职 | 转职体系从角色绑定改为职业级树，旧路径 ID 全部作废 | 存档 schema v1→v2 迁移：加载时清空所有角色 `promotion_path`（`_migrate_v1_to_v2`），玩家按职业树重新转职（一转 10 级 + 黄巾布×10） | 已修复 |
| B-011 | v0.30.6（程序 0.8.5.6） | v0.30.2 将新档初始武将改为仅刘备、关羽由 s01 首通解锁，与“初始双将”原始设计冲突；本次用户拍板回滚 | 新档查看初始武将 / 首通 s01 奖励 | 设计方向反复（v0.30.2 单将调整与用户预期不符） | 新档初始恢复刘备+关羽（`INITIAL_CHARACTER_IDS`）；ch01_s01 首通奖励移除关羽（黄巾布×2 等保留）；`ensure_initial_characters` 幂等补发旧档 | 已修复 |
| B-010 | v0.30.5（程序 0.8.5.5） | 科技树选中节点无高亮、解锁按钮不醒目；武将养成「未解锁武将」标题混入网格格子易误读（关羽在新档显示为未解锁） | 点击科技树节点后观察；新档查看养成面板 | 选中态仅覆盖 pressed 样式，鼠标悬停时按 hover 样式显示；节点边框为层级色、状态仅靠小字；hint 作为网格单元格参与排列 | 选中节点四态全覆盖金色高亮；节点边框色改表状态、根/叶用 ◆/○ 前缀；详情区解锁按钮金色描边；养成面板拆分已拥有/未解锁两网格并独立分组标题（未解锁=首通对应关卡解锁，关羽=s01 首通，属预期） | 已修复 |
| B-009 | v0.30.2（程序 0.8.5.2） | 科技树面板非树状且行内冗余摘要；详情泄漏 effect 代码键（damage_pct 等）；重置无确认框；解锁后详情不刷新；关卡首通文案把武将/道具/信物混排；s01 首通奖励关羽与初始武将重复 | 打开科技树/关卡面板观察 | 科技树平铺列表未按 requires/tier 渲染；`_effect_text` 直出字典键；重置未按 UI_LAYOUT 加确认框；首通文案无标签；新档初始拥有刘备+关羽与 s01 首通重复 | 科技树改树状布局（前置链分列 + tier 分层 + 竖线连接），行内仅成语名，摘要/描述进详情，删除 effect 键输出，重置加确认框，解锁/重置后刷新详情，科技点金色大字；首通文案分标签（解锁武将/道具/信物），摘要显示当前难度，一键通关提示自动清除；新档初始仅刘备，关羽由 s01 首通解锁；碎片确认保留（信物兑换 60/件 + 转盘产出）；**v0.30.3 布局修正**——同级节点并排成行（行 = tier、列 = 链）、根节点金边/叶子节点绿边/中间节点灰边、新增图例 | 已修复 |
| B-008 | v0.29.0（程序 0.8.5.0） | 职业技能参数分层读取取默认值；青龙偃月击杀返冷却被覆盖；辅助经验未按塔实例去重（同角色多塔互相遮蔽） | ① 稳射保底/档位系数数值不生效；② 关羽大招击杀返还冷却随后被覆盖；③ 刘备/张飞同角色多塔时辅助经验只计其一 | ① Tower.get_skill_param 按旧分层结构取默认值，未兼容 v0.28 扁平 skill_params；② cast_character_skill 冷却先置再执行，技能执行中击杀返冷却被后续覆盖；③ GameManager.add_support_contribution 覆盖键按角色 ID 去重 | ① get_skill_param 兼容扁平与分层两种格式；② 冷却先置再执行、执行失败回滚冷却；③ 覆盖键改按塔实例（character_id#instance_id）去重 | 已修复 |
