# 界面排版设计规范（UI_LAYOUT）

> 隶属《烽火连营·三国塔防》设计文档体系，总纲见 [../GAME_DESIGN.md](../GAME_DESIGN.md)，界面职责表见 [UI.md](UI.md)。
> 文档版本：v0.20.41（2026-09-11）
> v0.20.41 变更（2026-09-11，局内 HUD v2 概念稿定稿 / GDD v0.37.30 / UI_CONCEPT v0.12 / ART_ASSETS v0.12 / ART_PROMPTS v0.11，纯文档，程序 0.8.11.13 不变，用户确认 2026-09-11「确认，同步文档吧」，`ui_battle_hud_v2.png` / `src/ui_battle_hud_v2.html` 同源入库）：**§10 局内 HUD v2 定稿（战斗 UI 换肤以此稿对齐，落地排期另行登记）**——①**深墨绿 + 暖金局内皮肤**（底 `#13241b` / 描边金 `#c9a35c` / 文字 `#f2e7cd` / 数值 `#ffd479` / 生命 `#4ec97e`（≤30% 危急 `#ef5b52`）/ 主行动 `#ffcc00`；**地图与道路本身不改色**）；②**顶栏图标化**（关卡名 + 金币 / 基地生命 / 波次 = 图标 + 数字 + ☰ 菜单，收录 继续 / 设置 / 重开 / 退出、ESC 同效），**取消「开始第 N 波」按钮**——出怪 = 点击**敌人出口道路上的波次旗帜**（小箭头指向出口、Space 同效；自动开波开启后无需点击）；③**塔详情整合进底栏**：选中塔 → 建造位**整块替换**为同规格 5 卡（头像 / 伤害 / 攻速 / 升阶 / 回收）——升阶 · 回收 = 圆盘图标 46px + 右下角金币费用胶囊（卡面语言同建造位；满阶灰显不显费用、金币不足费用红）；详情**只显示伤害（按攻击类型换 `dmg_physical` / `dmg_magic` / `dmg_true`）与攻速**（`stat_attack_speed`）+ 数值，**不显示等阶、无射程数值**（选中即显示射程圈）、**取消选中金圈**；军需槽不随选中态变化；④**地图塔不显示名称**，阶级角标（1 / 2 / 3，**0 阶不显示**）置于头像右下（后续可换图标堆叠）；⑤**军需槽**（局外选带配置、局内花金币购买）：未购 = 虚线圆槽 + 右下角金币费用胶囊（点击购买）、已购 = 高亮 + 右上 ✓ + 左上剩余次数（点击使用）、**达限购次数置灰禁用**、锁 = 科技未解锁；⑥概念稿内**左侧大虚线胶囊为提示示意、不开发**。正文已同步（§10 新增定稿条目，v0.20.20 旧稿保留为历史存档）；v0.20.40 历史行保留。
> 文档版本：v0.20.40（2026-09-10）
> v0.20.40 变更（2026-09-10，经验池 + 军功/军需重构定稿同步 / GDD v0.37.22 / DESIGN_REVIEW v0.4.7，纯文档，程序 0.8.11.13 不变，排期 0.8.15 / 0.8.16）：**§6 养成面板经验口径 + §10 军需面板口径**——①§6 基础信息卡新增**经验池**（余额 + 分配入口：选武将 → 分配 → **二次确认**不可逆）与**「直接升级」**（消耗池内经验 +1 级、二次确认），原「使用练兵令」行随 `exp_scroll` 整体删除（0.8.15）；②§10 军需由「金币即买即用」改**「购买 → 军需带 → 择时使用」**——购买入带不立即生效、点击带内条目使用、**未使用结算作废**，新增带位区（默认 **2 槽** / 科技 +1 → **3 槽**）与 6 件军需池口径（掷石齐射 / 犒军新增，坚壁 / 伤药 / 火油罐移除）；**军需解锁 / 强化（军功）入口版式随 0.8.16 落地前另行设计**（版式重渲随 0.8.16，本版仅同步口袋径）。v0.20.38/39 历史行保留。
> v0.20.39 变更（2026-09-10，编队右栏「队伍加成」明细化 / GDD v0.37.21，程序 0.8.11.12 → **0.8.11.13**，用户反馈 2026-09-10「不需要折算显示总加成，我要的是竖着排列的：科技，攻速 +10%，物理伤害 +10% 这种形式」「不要居中，从上往下排列就行」，口径拍板「同类效果能加就加 / 羁绊每条激活一行、遗物每件一行 / 空态按推荐方案」）：**§7 右栏由三行合计改三组竖排明细**——①组头 = 色点 + 组名 + 延伸虚线，组内每行 = 效果名（左）+ 数值（右，金色），不做总加成折算、不标来源；②科技按已解锁条目**合并累加**逐条列出（职业限定项仅在该职业出场时列出并去重）；③羁绊按每条已激活一行（名称 n/n + 同队攻击 +N%）；④遗物按每件已选带一行（效果摘要，攻速由 ×倍率 改 +N% 口径）；⑤**顶对齐、从上往下排列**（不居中），空态组内灰字（未解锁 / 未激活 / 未选带 + `—`），明细超高组内纵向滚动；注脚改「仅计入已解锁科技 / 已激活羁绊 / 已选带遗物；同类效果合并累加。」。正文已同步；v0.20.37/38 历史行保留。
> v0.20.38 变更（2026-09-09，信物重构定稿同步 / GDD v0.37.20 / DESIGN_REVIEW v0.4.6，纯文档，程序 0.8.11.12 不变，排期 0.8.14）：**§6 信物页签口径 + §10 转盘演示奖池注记**——①「信物」页签由"武将专属信物装备/更换"改为**双信物槽**（专属信物槽先锁住占位不开放 + 可选信物槽开放、装通用/Boss 签名信物）；碎片兑换/数量相关 UI 随碎片整体删除；槽位版式（锁定占位/通用列表/装配交互）随 0.8.14 落地前另行设计，本版仅同步口径（§7 编队/§9 背包不受影响）。②§10 结算转盘概念演示奖池含「碎片×10 / 求贤令×1」条目——碎片随本次定稿删除、求贤令随 v0.30.0 抽将暂停，实装与重渲基准以 NUMBERS 10.9 为准（概念图重渲随 0.8.14 一并处理）。
> v0.20.37 变更（2026-09-09，编队界面 UI 简化 / GDD v0.37.19，程序 0.8.11.11 → **0.8.11.12**，用户反馈 2026-09-09「遗物那块不要显示持有数量多此一举」「右侧战力面板太混乱要清晰、总战力不需要、科技羁绊遗物小项直接写数值不必标注来源」「遗物详情面板空白太多」「二次确认界面遗物图片占位加名称即可」）：**§7 四处简化**——①队伍遗物 pill 去「持有 ×n」（名称直排图标右侧）；②右栏「总战力加成」改「队伍加成」三行清单（科技 / 羁绊 / 遗物，色点 + 名称 + 右侧合计数值；去总战力大数字、去来源明细/预览行），战力面板不再滚动；③遗物悬停浮层紧凑化：效果 chip 改定宽 232 自动换行文本块（浮层不再被长效果撑宽），元信息去持有数；④「确认出战」弹窗遗物行改图标占位 + 名称小卡（150×52 居中并排），去效果与持有数。正文已同步；v0.20.35/36 历史行保留。
> v0.20.36 变更（2026-09-09，编队界面与「确认出战」弹窗落地 / GDD v0.37.18，程序 0.8.11.10 → **0.8.11.11**，用户拍板 2026-09-09「只提交你修改的部分，然后开发编队界面与出战弹窗」）：**§7 由概念版式转已落地实现**——`SquadSelect.gd` 全量重写按 `ui_squad.png` v3 / `ui_squad_confirm.png` 布局：三区滚动兜底（武将 >9 增行纵向 / 遗物 >5 横向 / 战力明细超高组内滚动，滚动条 #7ec8ea 圆角细条同 MapPanel）、卡面底部内嵌金色费用通栏（B-025）、遗物 pill 悬停浮层（移开即消失）、「确认出战」自绘弹窗 640×560（55% 遮罩；概念稿 557 高按渲染微调）。落地差异：武将网格由角色数据驱动，未实现概念中的虚线「新武将·预留位」扩容卡——当前 9 将 3×3 一屏铺满不滚动，>9 时自动增行滚动（同 §7 滚动兜底）。§7 正文补实现口径。
> v0.20.35 变更（2026-09-09，编队概念图 v2/v3 定稿 / GDD v0.37.16 / UI_CONCEPT v0.10，纯文档，程序 0.8.11.10 不变，用户拍板 2026-09-09「编队面板概念图优化（角色面板不空 / 遗物悬停详情 / 右侧总战力加成）」「要支持可拓展滚动条」「只提交你修改的部分，然后开发编队界面与出战弹窗」）：**§7 编队按 `ui_squad.png` v3 重写版式规范**——整体改「标题区 + 操作提示行（含「溢出自动滚动」tag）+ 左右两栏 + 底部操作条」；左栏武将区 = 3 列滚动网格（≤9 一屏 3×3 铺满、行高 ≥130 不压缩、第 10 位起自动增行滚动 + 虚线「新武将·预留位」扩容卡）+ 队伍遗物行（说明列 + pill：图标占位 / 名称 / 持有 ×n / 已选·可选徽标；悬停浮层看详情，移开即消失；>5 件横向滚动、pill 最小宽 150）；右栏新增「总战力加成」面板（实时计算 tag + `+N%` 大数字 + 科技 / 羁绊 / 遗物三组明细 + 概算说明，明细超高组内纵向滚动）；三区滚动兜底规范（滚动条同 MapPanel 样式），**禁止压缩卡面 / 胶囊适配更多数据**；卡面底部内嵌建造费用通栏（B-025）与「确认出战」弹窗版式（v0.20.21）、编队记忆预填（v0.33.1）规则保留。
> v0.20.34 变更（2026-09-09，技能释放 v2·入口 3 修复 / GDD v0.37.15 / BUGS v0.44（B-057），程序 0.8.11.9 → **0.8.11.10**，用户反馈 2026-09-09「我还没点击（鼠标刚移上去）就释放了，选中后会持续释放，很怪需要优化一下」）：**§10 就绪胶囊两处修正**——①手动开关（`manual_ultimate`）由建塔快照改**逐帧生效**（GameFlow gameplay flag 运行时缓存 + `Tower._process` 每帧同步）：战斗中切换后全部塔立即生效，旧实现已建塔保持自动、满怒见敌即放（未点击即释放/选中后持续释放根因）；②塔顶胶囊**顶栏避让**：胶囊中心默认 y=-62，塔心 ≤154 时收至屏幕 y=92（顶栏 0..80 下缘 +12）——绘制与 QuickCastArea 命中区同源 `quick_cast_center_local_y()` 动态锚定，可建首排（行 1）胶囊不再落入顶栏；命中/释放语义不变：未就绪与自动模式不绘制不响应、仅左键点击释放、悬停不触发、一次点击一次释放。
> v0.20.33 变更（2026-09-09，技能释放 v2·入口 3 落地 / GDD v0.37.14 / CHARACTER_SKILLS v0.6 / SKILLS v0.11，程序 0.8.11.8 → **0.8.11.9**，用户拍板 2026-09-09「推送然后开发v2」）：**§10 塔身就绪胶囊快放（入口 3）由 v2 预留转落地**——手动模式（`manual_ultimate`）满怒塔顶新增**金色胶囊 + R 字**呼吸提示（复用 `_draw_capsule` 语言，塔顶 y −67..−57；与脚下怒气胶囊区分 = 快放入口）；点击区 = Tower.tscn 新增 `QuickCastArea`（Area2D，CollisionShape r13 @ y=−62）与选中区（r32 @ 原点）**解耦**——点胶囊直发该塔大招、**不改变选中态**；未就绪 / 自动模式不绘制不响应、ghost 虚影不绘制；选中塔面板按钮 / R 键路径不变（同源 `cast_ultimate_manual`）；释放反馈 = 状态条「XX 释放大招！」+ 大招名飘字（既有），选中态释放后面板刷新。v0.20.32 的入口 4（全局就绪聚合提示）与区域型拖拽落点仍 v2 预留。
> v0.20.32 变更（2026-09-09，技能释放入口设计定稿 / GDD v0.37.12 / CHARACTER_SKILLS v0.6 / SKILLS v0.11，纯文档，程序 0.8.11.7 不变，用户拍板 2026-09-09「两个都处理，然后再开发」）：**§10 新增「技能释放入口」规范**——①**现网入口** = 选中塔底部详情面板（手动模式）「释放大招！（R）」按钮 + R 键直发（v0.15.0 保留）+ 塔身满怒金色脉动提示（0.8.11.x）；②**展示纪律**：职业技能 / 角色技能 A 型 / B 条件被动一律**不出现释放按钮与动作行**（维持 v0.20.22 / 0.8.11.0 移除技能区口径），自动技能就绪只由塔身冷却环表达；③**未来扩展（v2 预留，未开发）**：塔身就绪胶囊快放（入口 3，未选中可点）、全局就绪聚合提示（入口 4，仅提醒非主入口）、Q = 可手动技能槽 1（R = 大招恒主位）；区域型技能拖拽落点指示器复用建造拖拽绿/红提示；④**「可手动」登记制**：需手动入口的技能须先在 CHARACTER_SKILLS / SKILLS 登记，未登记不开发入口；手动是「覆盖」非「门槛」（AI 智能目标 ≈90%+ 效率、手动收益 ≤10~15% 且通关不依赖）。
> v0.20.31 变更（2026-09-09，阶段 8·提交 11 延伸，程序 0.8.11.6 → **0.8.11.7** / GDD v0.37.11 / BUGS B-056，用户拍板 2026-09-09「顶部左侧胶囊均移除，其他的按你说的来改」）：**§9 背包按概念图 ui_inventory 对齐**——①顶栏资源胶囊（黄巾布/练兵令）移除，仅标题 + 右侧「共 N 种」chip（测试发放容量口径不变）；②类目筛选收敛为 全部/材料/消耗品/遗物（货币/抽奖券/碎片类目无产出系统移除）；③道具卡 = 类型色渐变圆角方块 52px（白字白描边，`UITheme.tile_label`，材料蓝/消耗品橙/遗物紫）+ 名称 + 类型徽标（语义配色）+ 两行描述（省略号截断）+ 金色 ×数量；卡高 116；④列表按类型分组（材料→消耗品→遗物）+ id 字典序；⑤网格超高滚动兜底（ScrollContainer，滚动条样式同 MapPanel）；⑥空类目网格空态提示 + 底部说明条清空出提示；⑦底部说明条 = 56px 大方块 + 名称/类型徽标/遗物「永久使用」tag + 两行描述 + 遗物金色 note（队伍级 · 不消耗 · 最多 2 件）+ 右列 ×n/持有。
> v0.20.30 变更（2026-09-08，阶段 8·提交 11 延伸，程序 0.8.11.5 → **0.8.11.6** / GDD v0.37.10 / CHARACTERS 4.5·4.8 / DROPS_GACHA 7.1，用户拍板 2026-09-08「背包顶部新增遗物分类，所有遗物把物品分类从消耗品修改为遗物」）：**§9 背包新增「遗物」类目**——`ItemData.ItemType` 末尾追加 `RELIC`（数值存储勿改中间顺序），类目筛选 chips 追加「遗物」（图标底色 teal）；5 件局内遗物物品分类由消耗品改遗物（消耗品类目现仅余测试练兵令），卡片类型徽标随数据自动显示「遗物」。另 §4.5 关羽称号展示随数据同步为「武圣」（见 CHARACTERS 4.5）。
> v0.20.29 变更（2026-09-08，阶段 8·提交 11 延伸，程序 0.8.11.4 → **0.8.11.5** / GDD v0.37.8 / UI_CONCEPT v0.9 / BUGS B-055，用户反馈「局内设置页面有些问题」「局内面板需要调整，概念图同步修改」）：**§10 两处修订——①战斗内设置弹窗亮色化**：战斗「设置」弹窗外框改 Kenney DIALOG 语言（`DIALOG_PANEL #f2faff` / `DIALOG_BORDER #1c9fd7` 3px 描边 + 7px 下边 / 圆角 18 / 投影，同 MainMenu「新的征程」确认弹窗；遮罩 alpha 0.62），底栏「关闭设置」Kenney 蓝按钮 220×48 居中白字，内嵌 SettingsPanel 布局不变（B-055）；**② 局内 HUD 概念稿同步 0.8.11.1 运行版式重渲**（承接 v0.20.24「概念图同步重渲另行排期」）：顶栏贴顶全宽 0..80 + 底部建造卡条 640..720 常驻 + 怒气胶囊收格三态 + 塔详情面板收起/上展交互，细则见下方「局内 HUD 概念稿」条目。
> v0.20.28 变更（2026-09-08，阶段 8·提交 11 延伸，程序 0.8.11.3 → **0.8.11.4** / GDD v0.37.7 / UI_CONCEPT v0.8，用户拍板「去除地图选择-关卡详情中的经验胶囊体和推荐胶囊」）：**§5 底部关卡预告条信息胶囊收敛**——①移除「经验」「推荐」两枚胶囊（信息冗余，短内容胶囊被均分撑宽留白多）；②保留「敌人」「波次」两枚，胶囊改**内容自适应宽**（SIZE_SHRINK_BEGIN），键值自然排版不再撑满整行；面板 148 高与右列徽章/按钮布局不变。
> v0.20.27 变更（2026-09-08，阶段 8·提交 11 延伸，程序 0.8.11.2 → **0.8.11.3** / GDD v0.37.6 / BUGS B-054，用户反馈 2026-09-08「地图选择界面s03，关卡详情面板直接超出界面」）：**§5 地图选择·底部关卡预告条自适应**——①四个统计胶囊的值 Label 加 overrun（最小宽度归零）并均分胶囊宽，超宽省略号截断；②右列首通奖励徽章行 HBox→HFlowContainer 流式折行；面板 148 高贴底不变，任意关卡内容不再撑破白卡/屏幕（B-054）。
> v0.20.26 变更（2026-09-08，阶段 8·提交 12 排期登记，程序 0.8.11.2 不变（0.8.11 修复/延伸期）/ GDD v0.37.5 / ART_ASSETS v0.10 / UI_CONCEPT v0.7，用户拍板 2026-09-08「按你推荐来」，纯文档/素材无程序逻辑改动）：**新增 §15 鼠标光标规范（Kenney Cursor Pack，提交 12 排期）**——原图白芯黑边 → 定稿配色 B（芯 `#cdeffb` / 描边 `#14538a`，灰度明度线性映射双色重着色）；图标映射 = 全局默认 `pointer_toon_a` / hover `hand_point` / 拖拽 `hand_closed` / 战场·编辑器画布 `cross_large` / 编辑器笔刷 `drawing_pen·drawing_brush·drawing_eraser`（禁建 `cursor_disabled`）；热点按尖端/中心逐枚校准登记；应用点 = 全局默认光标 + ui_theme 悬停手型 + BuildManager 拖拽 + 地图编辑器画布与笔刷；素材入 `assets/ui/cursors/`（提交 12 落地）；对照图 `docs/ui_concept/ui_cursor_palette.png`（非屏规范图）。
> v0.20.25 变更（2026-09-08，阶段 8·提交 11 延伸，程序 0.8.11.1 → **0.8.11.2** / GDD v0.37.4 / BUGS B-053，用户反馈「怒气条默认不显示？攻击敌人获取怒气后才显示吗」）：**§10 怒气条空槽常驻**——移除 0 怒气早退：新建塔 0 怒气时脚下显示弱化空槽（底槽 alpha 0.35），获得怒气后底槽加深（0.66）+ 灰蓝填充 → 满怒金色呼吸外发光；玩家随时可见怒气机制与积攒进度。
> v0.20.24 变更（2026-09-08，阶段 8·提交 11 延伸，程序 0.8.11.0 → **0.8.11.1** / GDD v0.37.3 / BUGS B-051·B-052 / ART_ASSETS v0.9，用户拍板 2026-09-08「可以 按你推荐的来」）：**§10 战斗版式修订（顶部遮挡重构 + 怒气条收格 + 虚影实塔化）**——①**顶栏贴顶全宽 0..80**（圆角去 0、内容边距 16，不再悬空遮挡 row1）；②建造卡条整体移至**底部常驻 640..720**（半透明深色底 + 顶边细线，横版卡 124×68 = 左 48px 圆立绘头像 + 右 名称/Lv/费用）；战斗禁建区 = **y≤80 or y≥640**（战场 row1..7 全可建）；③卡条让位：塔详情面板（底部上展开）与结算展示时收起卡条，剧情对白面板上移 **448..638** 不遮卡条（对话期保持可建造）；开局 BuildHint 一次性顶部提示 y88..142；④怒气条 44×7 y48/y31 → **38×10 y30/y28 收进格内**（B-051）；⑤拖拽虚影占位头像圆 → **实塔同款小人**（spine 优先 / 程序化身体+武器回退，`set_ghost_mode`，保留射程圈与落点格绿红染色）；⑥`ch01_s03` 路径/建造位/禁建重排避让 row8（底栏）与 row0（顶栏）——路格 ≥24 等布局规范保持（FlowRunner 断言同步校准）；⑦ui_battle_hud 概念稿为后续换肤基线，本次运行版式偏移（贴顶 + 底部卡条）登记，概念图同步重渲另行排期。
> v0.20.23 变更（2026-09-08，阶段 8·提交 11 设计先行·战斗结算三屏概念初稿（待拍板），程序 0.8.11.0 不变（0.8.11 修复/延伸期）/ GDD v0.37.2 / UI_CONCEPT v0.6，纯文档/素材无程序逻辑改动（概念初稿待拍板））：**§10 战斗界面新增结算三屏概念版式初稿（`ui_victory.png` / `ui_wheel.png` / `ui_defeat.png`，源码 `docs/ui_concept/src/*.html` 同源，待拍板）**——①胜利结算页（1160×656 亮蓝白面板，绿色横幅）：元数据行 + 左奖励明细（4 武将出战经验 / 材料 / 新武将 / 科技点，难度材料倍率）× 右本局战绩 +「挑战目标 · 星级」阶段 10 预留虚线位 +「胜利特典」金色转盘入口卡（剩余金币 ≥150 抽 1 次）→ 重试本关 / 返回选关 / 下一关；②结算转盘页（1180×656，金色标题条）：352px 六扇区圆轮（conic 色扇区 + 白分隔线 + 金 rim 铆钉 + 顶部固定指针 + 中央 ★ 抽钮，演示命中扇区白环高亮）+ 奖池与权重 6 行（SettlementWheel.POOL 同源：×2·30 / ×5·20 / 碎片×10·20 / ×20·10 / 科技点×1·5 / ×10·5）+ 抽取规则（胜利且剩余金币 ≥150、每局仅 1 次、即时入档、抽取后置灰「转盘已抽取」）；③战败结算页（820×600，红色横幅）：战况行 +「本局未结算奖励 · 全部作废」三卡灰显 + 失败规则条（不入档 / 退出崩溃同处理 / 转盘仅胜利开启 / 遗物永久持有失败不回扣 v0.33.1）→ 返回选关 / 重试本关；细则见 §10「结算页」条目下方三屏 bullet。
> v0.20.22 变更（2026-09-08，阶段 8·提交 11 落地，程序 0.8.10.33 → **0.8.11.0** / GDD v0.37.1 / UI_CONCEPT v0.5，用户拍板 2026-09-07 五项 + 2026-09-08「目前技能自动释放就行，开发提交 11 吧」）：**§10 局内表现与建造交互定稿（五项拍板 + 角色技能自动释放口径落地）**——①建造卡片 = 56px 概念九色圆头像占位（姓氏首字，控件预留立绘替换）+ 武将名 + 底行 Lv（蓝）/ 费用（金），职业名行移除；卡条高 196（`is_point_over_battle_ui` 禁建区 y≤196 同步，调试面板下移至 y206）；②拖拽虚影主体改与卡片同源的占位头像（84px、底边贴地，可建绿 / 不可建红染色 modulate），攻击范围圈与落点格绿/红提示保留（虚影不再绘制实塔身体/怒气/冷却环——`Tower.set_ghost_avatar_mode`）；③塔详情面板移除技能区（职业技能/角色技展示行 +「释放技能」按钮删除）；**角色技能 A 型就绪即自动释放、与手动大招开关解耦**（手动大招仅约束大招本身）；冷却状态改由塔身冷却环表达；④怒气条胶囊化 = 44×7 脚下小胶囊（圆角端=半圆；未满灰蓝填充、满怒金色 + 放大呼吸与外发光；spine 塔 y48 / 普通 y31）；⑤冷却环脱战冻结修复（A 型冷却递减期间每帧 queue_redraw，BUGS B-050；spine 塔 r40）；⑥`ui_battle_hud.png` 概念图同步移除详情面板「职业技能 · 角色技」副行并重渲（UI_CONCEPT v0.5）。
> v0.20.21 变更（2026-09-08，阶段 8·提交 11 设计先行，程序 0.8.10.33 不变（0.8.11 开发中）/ GDD v0.37.0 / UI_CONCEPT v0.4，纯文档/素材无程序逻辑改动（概念初稿待拍板））：**§7 编队补「确认出战」二次确认弹窗版式、§10 新增军需面板规范（概念图 `ui_squad_confirm.png` / `ui_supply.png` 初稿，待拍板）**——①§7 确认弹窗（640×557 亮蓝白、55% 遮罩）：蓝标题条（「确认出战」+ 章节 chip + 圆 ✕）→ 元数据行（sNN 关名 / 难度 · X / 出战 n/n）→「出战武将」虚线分隔区四行名单（头像占位 / 姓名 / 职业 · 定位 / Lv / 「出战 n」绿徽标）→「队伍遗物 · 永久使用不消耗库存」两卡 → 虚线提示条 → 底栏（存档记忆灰字提示 + 取消灰 150×44 / 确认出战黄 176×44）；②§10 军需面板（908px 亮蓝白、**战斗不暂停**、遮罩自顶栏下缘起）：标题条 = 军需 + 「即买即用 · 战斗不暂停」chip + 金币胶囊 + ✕；主体左 476px 军需卡列表（图标虚线圆占位 / 名称 / 一行描述 / 费用金 chip / 状态 tag[限 n 次·悬停预览·基地生命已满]；**列表可纵向扩展，超高滚动兜底**），右 372px 详情预览卡（效果/持续/剩余三格虚线盒 + 规则提示 + 「购买」黄钮 190×46）；**交互口径 = 悬停卡片右侧即时预览、点击金框固定选中、按「购买」即买即用（无二次确认弹窗，NUMBERS 10.9「不加购买确认」规则不变）**；满生命修整禁用灰显、限购用尽/金币不足置灰。
> v0.20.20 变更（2026-09-07，阶段 8·提交 10 延伸，程序 0.8.10.32 / GDD v0.36.25 / UI_CONCEPT v0.3，用户拍板 2026-09-07）：**§10 战斗界面新增局内 HUD 概念稿基线（`ui_battle_hud.png` / 源码 `src/ui_battle_hud.html` 定稿存档，战斗 UI 后续换肤以本稿对齐）**——①顶栏资源条：章节名 + 金币 / 基地生命 / 波次三白胶囊（金币与基地生命图标 = `docs/ui_concept/src/icons_battle/` 裁剪图；波次图标虚线圆占位待美术）+ Kenney 按钮行（黄「开始第 N 波」/ 蓝「军需」（带虚线圆占位图标）/ 灰 暂停·设置·重开 / 红 退出）；②**基地生命变色规则**：数值常态绿 `#0e9f58`，≤30% 危急变红 `#e5484d`（概念图左上图例两态对照）；③**底部人物详情面板**（选中武将/塔）：深蓝半透明底 `rgba(12,18,30,.62)` + 2px 浅蓝虚线描边 `rgba(160,205,240,.8)` 圆角 16，460×166——头像 42px / 姓名·职业 / 「职业技能 · 角色技」副行 / 「局内阶数 n/3」黄徽标 + 伤害·攻速·射程三格居中胶囊（139×47）+ 底部仅「升阶（36 金币，黄）」「回收（返还 120，灰）」两钮（大招与冷却不入面板）；④场景示意 s01 风格（主路 / 基地 / 敌人占位点 / 选中塔张飞·阶 2），图例说明 HP 两态与虚线圆占位。
> v0.20.19 变更（2026-09-06，阶段 8·提交 10 延伸·修复 3，程序 0.8.10.31 → **0.8.10.32** / GDD v0.36.24 / BUGS B-049 / ENCYCLOPEDIA v0.1.9，用户反馈「武将图鉴的页签名称 scroll 删除」「详情面板里文字离面板边框提太近了，你再看看 UI」）：**百科 §12 页签标题与页签体内边距修正**——①页签标题 = 概念页签文字（TabContainer 标题默认取直接子节点名，滚动容器直接作页签子节点会把「Scroll」后缀带进标题；百科 `_make_tab` 改显式 `set_tab_title`，节点名不再进标题）；②页签体内容按概念 `.tabbody padding: 10px 14px` 外包 MarginContainer 内边距（14/14/10/10 + 横向 EXPAND_FILL），文字/卡片不再紧贴页签浅蓝描边（像素量测 3px/4px → 17px 与概念图一致）。EncShotProbe 增补页签标题断言与逐页签切换零越界审计；回归 Smoke / Flow 绿。
> v0.20.18 变更（2026-09-06，阶段 8·提交 10 延伸·修复 2，程序 0.8.10.30 → **0.8.10.31** / GDD v0.36.23 / BUGS B-048 / ENCYCLOPEDIA v0.1.8，用户再对照概念图反馈「还有问题」后拍板按推荐顺序修改）：**百科 §12 残留偏差终版修正**——①顶部「武将图鉴 / 敌人图鉴」分段按钮按概念 `.segbtn` 改 **Kenney 蓝底白字（选中）/ 灰底深字（未选）**（apply_kenney_rect_button，同科技树 .tabs 分类按钮口径），替换 v0.20.17 误用浅金卡选中样式；②武将左列卡 / 详情头圆头像改**概念九色键**（CHARACTER_AVATAR_COLORS 映射 UITheme.avatar_label；avatar_label 补 brown/pink/teal 三键），不再按职业色蓝化；③`CHARACTER_ORDER` 概念序修正（黄忠第 4 / 皇甫嵩第 5）；④默认选中 = 张飞 / 黄巾渠帅·张梁（概念稿 .lcard.sel）；⑤EnemyAvatar 渐变纹理 setup 预生成 ImageTexture（B-048：_draw 内现建运行时纹理同帧画不上致白屏）。像素探针同坐标比色核验；EncShotProbe / FlowRunner 同步；回归 Smoke / Flow 绿。
> v0.20.17 变更（2026-09-06，阶段 8·提交 10 延伸·修复，程序 0.8.10.29 → **0.8.10.30** / GDD v0.36.22 / BUGS B-047 / ENCYCLOPEDIA v0.1.7，按概念图 ui_encyclopedia.png / ui_encyclopedia_enemy.png 逐项对齐）：**百科两屏版式终版对齐**——①武将左列表头 + 计数 + 2px 虚线分隔（DashedLine 自绘），9 卡按**概念顺序**（CHARACTER_ORDER）排、卡副行改「职业名 · 打法词」（CHARACTER_FLAVOR）；②武将详情头部 .dhead 概念化（头像 52px + chips 职业定位/称号 + 右侧获取方式实值 + 头下 2px 虚线），「基础」页签改 irow 键值行 + statrow 四统计卡（伤害 Lv1 / 攻速 秒 / 射程 / 建造费用金）；③数值模拟器移出详情页改**底部整行 .sim 卡**（等级 −/＋ 步进 + 转职下拉 .pick + 局内升阶 0~3 按钮[选中 #1c9fd7 蓝底白字] + `=` + 三枚金色结果卡 + 「预览不影响存档」tag）；④敌人左列卡 = EnemyAvatar 方形对角渐变头像（白描边圆角方 + 135° 渐变，七色 ENEMY_AVATAR_GRADIENTS）+ 副行「定位词 · 打法词」（ENEMY_LOCATIONS / ENEMY_TACTIC_SUB）；⑤敌人详情体卡化：statrow 五统计卡（击杀奖励金）→ 特殊行为卡 → 各档难度卡行（标准×1.0 绿 / 困难×1.4 红）→ 口径注脚 → 多关登场明细；⑥详情头部补「出现关卡」摘要（单关 sNN·关名 / 多关 sNN~sMM · 共 N 关）。几何探针核验武将/敌人/逐敌选中/模拟器操作各态**横向纵向零越界**（1280×720 窗口实测）；回归 Smoke / Flow 绿。
> v0.20.16 变更（2026-09-06，阶段 8·提交 10 延伸·修复，程序 0.8.10.28 → **0.8.10.29** / GDD v0.36.21 / BUGS B-046，用户反馈「切换科技块有黑色闪烁」「点击解锁无反应」）：**科技树两处交互回归修复**——①节点点击黑闪（B-034 同款）：非选中节点保持 pressed/hover_pressed 状态底色覆盖，不再移除回落主题深色；②详情「解锁」按钮补回 `pressed` → `_on_unlock_pressed` 连接（重构时遗漏致点击无反应）；解锁链路 = unlock_tech + 存档 + `_refresh`。新增 tests/TechUnlockRunner headless 回归全绿。
> v0.20.15 变更（2026-09-06，阶段 8·提交 10 延伸，程序 0.8.10.27 → **0.8.10.28** / GDD v0.36.20 / BUGS B-045，用户对照概念图找问题后拍板按推荐顺序修改、保留「科技点+50（测试）」按钮）：**科技树第三次对齐 ui_tech.png / ui_tech.html**——①树主体改行式（首列 52px 竖排层级胶囊 .tlabel + 同层节点 HBox 行 + 层间 26px 连接带独立行逐链画竖线），修 GridContainer 8 格占位列宽撑开致竖线错位折行、层级标签落入连接带、文字挤左；②军略职业链按概念顺序排、军略终极层补 .t3-note 虚线提示条（DashedPill）；③节点卡概念三态色与卡内结构（左上 ◆/○ 层级符 + 右上状态字 + 名称/摘要 + 底部 消耗/✓/前置名；白底 3px 描边 #52c48a/#ffcc00/#cddbe6，底 #f2fbf5/#fffdf0/#eef3f7）；④分类改 .tabs 按钮行（4 枚 118×44 Kenney 蓝选中/灰未选中）+ .legend 图例右对齐，顶栏标题/科技点 chip/已解锁 chip/重置/测试按钮单行排布（重置 118 灰钮；「科技点+50（测试）」为 v0.30.4 开发辅助钮，正式结算不读取）；⑤详情 pills 三枚 = 前置 / 同类已投入 / 职业加成合计 +9%（上限 +10%，职业链 effect 求和、非职业链 —）。几何探针核验零越界；回归 Smoke 绿。
> v0.20.14 变更（2026-09-06，阶段 8·提交 10 延伸，程序 0.8.10.26 → **0.8.10.27** / GDD v0.36.19，用户拍板「页面名称改成『职业』就行，去除可转」）：**职业页签标题去除「可转」后缀**——v0.20.9~v0.20.13 以标题后缀「职业 · 可转」近似概念 `.tabbadge`（Godot 4 TabBar 不暴露内部 tab 按钮，无法挂文字徽标）；按用户要求标题恒为「职业」，删除 `_update_job_tab_badge` 逻辑与调用；`.tabbadge` 黄色小标方案作为对概念图的偏离登记在案、不再实现。回归 PromoteDialog / Smoke 绿。
> v0.20.13 变更（2026-09-06，阶段 8·提交 10 延伸，程序 0.8.10.25 → **0.8.10.26** / GDD v0.36.18 / BUGS B-044，用户反馈「改了啥啊，一点变化都没有」：①顶部虚线长胶囊缺失且文字挤左 ②蓝头 who 信息应在左侧紧贴标题 ③中间面板宽高对不上）：**转职详情叠层按 ui_develop_promo.png 逐像素对齐**——①外框 1160×570 整窗居中（叠层改挂 GameHub 顶层，不再挂在 ~988px 内容区上）；②蓝头渐变 #46b7ea→#1c9fd7 + who 胶囊紧贴标题 + ✕ 贴右；③`.ptip`/`.pfoot` 改 1px 虚线长胶囊（DashedPill 自绘：浅底 + #bfdcef 虚线 + 圆角 10，文字铺满整行）；④`.ptree` 竖轨版式（3px #bfe2f4 贯穿 + 状态点骑轨：彩色核心 + 白边 + 状态色外环）；⑤节点卡去头像圆、灰锁正文弱色 #5b7688。三态几何探针零越界；回归 PromoteDialog / Smoke 绿。
> v0.20.12 变更（2026-09-06，阶段 8·提交 10 延伸，程序 0.8.10.24 → **0.8.10.25** / GDD v0.36.17 / BUGS B-043，用户拍板按 1→7 顺序修改职业详情页实现与概念差异、明令「别再出现超出面板的问题」）：**转职详情叠层节点与外框对齐 ui_develop_promo.html**——①基础节点状态分层（未转=绿「当前职业」+ step 浅字「基础职业 · 未转职」；已一转=绿「基础职业」+ step「基础职业 · 已转「首个一转」」）；②图例口径去「绿点/金点/灰点」（画面为 ↓ 箭头树），提示与 ↓ 一致；③二转分支标题需求改取**分支自身数据**（Lv 20 · 黄巾布 ×20，原误用一转候选 Lv 10 · ×10）；④已一转后二转两分支同样**并排两卡各半宽**（灰锁=未解锁 / 金=可转职+转职为按钮），杜绝全宽堆叠；⑤分支卡补概念 `.fx` 详情——强化线由 `skill_params` 数据驱动拼「「军旗+」强化：光环半径 150 → 180px · 友军伤害 +4% → +6%」、新技能线拼「保留「军旗」，习得「护卫」：…」+ 数值行；⑥节点名称 16→18px、头部间距拉开、右列 act 定宽（全宽卡 180 / 分支卡 176 / 灰锁 128）；⑦叠层外框对齐概念 `.promo-panel`（#f2faff 底 / 18 圆角 / 3px #1c9fd7 描边 / 7px 粗下边近似）+ 头部标题 26px。几何探针核验未转 / 已一转 / 已二转三种状态叠层横向越界计数归零；回归 PromoteDialog / Smoke 绿。
> v0.20.11 变更（2026-09-06，阶段 8·提交 10 延伸，程序 0.8.10.23 → **0.8.10.24** / GDD v0.36.16，用户拍板「右上角的黄巾布，练兵令去掉」）：**养成页顶栏右上资源胶囊移除**——武将养成面板顶栏右侧「黄巾布 ×n / 练兵令 ×n」两枚资源胶囊（概念图 `.topbar` 元素）按用户要求移除，顶栏仅留「武将养成」标题；资源数量以背包页顶栏为准。此为对概念图的偏离，登记在案（§6）。
> v0.20.10 变更（2026-09-06，阶段 8·提交 10 延伸·修复，程序 0.8.10.22 → **0.8.10.23** / GDD v0.36.15 / BUGS B-042，用户反馈「详情面板都超出去了」）：**职业页签溢出修复**——转职进度卡三枚 chips（下一转 / Lv 达标 / 材料）原用不可换行 `HBox` 与标题、右按钮同行，单行最小宽 ~660px 把职业页签体（~602px）撑破，整页内容右溢约 80px 顶出详情面板并越出窗口；chips 改 `HFlowContainer` 自动折行（对齐概念 `.jchips` 的 `flex-wrap:wrap`），职业页签内容整体套纵向 `ScrollContainer`（与技能页签一致，内容超高时滚动兜底而非外溢）。
> v0.20.9 变更（2026-09-06，阶段 8·提交 10 延伸·按用户拍板 A→E 顺序对齐养成界面剩余偏差，程序 0.8.10.21 → **0.8.10.22** / GDD v0.36.14 / BUGS B-038~B-041）：①**左列表卡刷新污染修复（B-038）**——卡片文本按旧布局 find_children 顺序覆盖，把头像写成全名、职业行写错、等级不更新；改卡片关键 Label 命名 CardName/CardSub/CardLv，按名更新；②**技能页签**——角色专属技能名右侧补概念 `.roletype` 金色小字（A 主动 · CD x s / B 被动 · 条件触发；A 冷却读 `character_skill_params.cooldown`），大招 tag「职业大招」改动态「{职业名} · 大招」；③**职业页签（B-040）**——身份卡改浅白卡（定位 tag「近战 · 持旗鼓舞」由职业 tags 中文映射 + 克制 lock tag「对骑兵克制」+ 右状态列 Lv x/y · 达标 / 当前已转职，描述改职业数据文案不再写死虎贲）、技能未习得 tag 改灰 lock、转职进度卡改概念 `.jcard.jpromo` 横向 chips + 完整路线说明（动态拼 基础→下一转→二转分支（军旗+/护卫））+ 按钮 176×46；「可转」提示以页签标题后缀近似（Godot 4 TabBar 不暴露内部 tab 按钮，无法挂 `.tabbadge` 徽标）；④**转职详情叠层（B-039）**——二转分支两卡并排（branch_row 此前未挂载子卡）+ 分支卡补 二转·强化线/新技能线 tag、数值行（含大招倍率）、Lv/材料 条件胶囊（满足绿 / 未满足灰）；⑤**信息卡攻速改间隔秒口径**（概念 0.67s，原 1.43 次/秒）。DevelopRefreshRunner 断言按新卡片结构修正（姓名/职业/等级标签 + 头像字不被覆盖）。回归 PromoteDialog / Smoke 绿。
> v0.20.8 变更（2026-09-05，阶段 8·提交 10 延伸·修复，程序 0.8.10.20 → **0.8.10.21** / GDD v0.36.13 / BUGS B-037）：**转职详情叠层蓝标题条渲染修复**——历次补丁将 `box.add_child(header)` 丢失（header 构建完整但从未挂入弹层，叠层顶部直接是转职树节点），头部 Panel 虽已设 min height 56 仍整条不渲染；修复=补回 `box.add_child(header)` 挂载（亮蓝底 #38a8dd 圆角顶 + MarginContainer 内距 + 圆形✕）；窗口化探针截图核验蓝条白字/右侧圆形✕/完整转职树（当前职业绿 → 一转金[习得技能+数值行+条件胶囊+转职为钮] → 二转分支预览灰卡）渲染。
> v0.20.7 变更（2026-09-05，阶段 8·提交 10 延伸·转职详情节点排版按用户拍板改底部通栏，程序 0.8.10.20（随 **0.8.10.21** 交付）/ GDD v0.36.13，用户反馈「排版还是有问题，看看UI」）：①先行重构（v0.20.6 延伸，未独立成版）——节点卡改 PanelContainer **随内容长高**（修按钮/胶囊溢出卡外）、节点含头像+名称+状态 tag+描述+习得技能行+数值行（伤害/攻速倍率）+Lv/材料条件胶囊+右列转职为按钮（未转职）/未解锁灰钮（二转预览）、二转分支 2 卡并排+分支说明行（需 Lv/材料聚合）；②**用户拍板定稿版式**——节点卡 = 顶行（头像圆+名称+状态 tag+右侧按钮/注记）+ 描述/习得技能/数值/条件胶囊**统一放卡片底部通栏**，不再做头像右侧窄列排版。已知遗留=叠层蓝色标题条未渲染（v0.20.8 修复）。
> v0.20.6 变更（2026-09-05，阶段 8·提交 10 延伸·养成界面五点重构，程序 0.8.10.19 → **0.8.10.20** / GDD v0.36.12，用户反馈「列表块宽高与排版不对/统计格没包块/技能三卡与 UI 完全不符/职业页签排版严重不符/转职详情缺完整转职树」）：①**列表卡**——姓名 15px 与职业 12px 两行分离 + 右对齐 Lv（accent 色），锁定卡=职业·未解锁+来源行+灰 Lv；②**信息卡四统计格**改 PanelContainer 独立包块（VBox 裸样式不绘制为无框根因）；③**技能页签**——大招卡与职业技能卡**同行并排**（概念图 .skrow），角色专属卡整行，怒气条移入大招卡头部右端，技能卡改 PanelContainer 有框有底；④**职业页签按 ui_develop_job.png 重排**——职业身份卡（绿边：头像+名称+近战 tag+未可/已可一转徽标+desc）+ 职业大招/职业技能两卡（已习得绿/未习得黄 tag + 一转习得说明）+ 转职进度白卡（下一转 chip、等级/材料 pills、转职详情黄按钮）；⑤**转职详情叠层按 ui_develop_promo.png 重构**——蓝色标题条（转职详情 + 武将·职业·Lv chip + 圆形✕）+ 完整转职树（基础职业→已转链绿节点→下一转金节点[习得技能行+Lv/材料 pills+转职为按钮]→二转分支预览灰卡）+ 底部二次确认说明行，修复「没有展示出完整转职树」。
> v0.20.5 变更（2026-09-05，阶段 8·提交 10 延伸·修复，程序 0.8.10.18 → **0.8.10.19** / BUGS B-036）：**章节下拉弹层加滚动**——弹层高度按行数硬算漏计分隔间距致末行「第七章」被截断一半，列表盒套 ScrollContainer 限高 262px（约 5 行）超出滚动（横向禁用），滚动条对齐概念图 .scroll 规范（10px 浅蓝圆角细条、悬停亮蓝）；行高统一 42+4 间距。
> v0.20.4 变更（2026-09-05，阶段 8·提交 10 延伸·修复，程序 0.8.10.17 → **0.8.10.18** / BUGS B-035）：**v0.20.3 下拉自绘三处回归修复**（用户真机反馈）——①预告统计胶囊补回 `stats.add_child`（补丁漏挂载致胶囊整体消失）；②预留章行按概念图编号「第N章 · 名称」（N 从二起）；③框按钮右侧三角标改右锚定 TextureRect 子图标（Button 无 arrow 槽）。**验证方式升级：真机鼠标点击跑通完整交互链路**（打开游戏→进大厅→点开下拉→禁用行无响应→选行关闭→文字保持），不再只靠合成信号截图。
> v0.20.3 变更（2026-09-05，阶段 8·提交 10 延伸·地图选关四处修复，程序 0.8.10.17）
> v0.20.3 变更（2026-09-05，阶段 8·提交 10 延伸·地图选关四处修复，程序 0.8.10.17 / GDD v0.36.9，用户反馈：①下拉项文字排版对表 ②选中项应显 ✅ ③按下框体文字消失 ④预告胶囊缺「敌人/波次/经验/推荐」键名）：①**章节下拉自绘重构**——OptionButton（原生弹层无法承载行内右对齐状态）换为自绘弹层（top_level 整屏点击遮罩 + 白色圆角列表 + 框按钮正下定位）；行 = 名称（左）+ 状态（右）：当前章浅蓝选中底 + 绿色 ✅，预留章灰字「敬请期待」；文案改「第一章 · 黄巾之乱」（章节号中文数字）；②**按下文字消失修复**——框按钮补 font_pressed/hover_pressed/focus 色覆盖（默认按压白字落白底）；③**预告统计胶囊补键名**——每胶囊 = 键（敌人/波次/经验/推荐，弱色）+ 值（墨蓝），原只有值导致「6 波 50」式含义不明；④预留章行按钮保持 disabled（FlowRunner disabled 计数断言不变）。
> v0.20.2 变更（2026-09-05，阶段 8·提交 10 延伸·科技树二次对齐，程序 0.8.10.16 / GDD v0.36.8 / BUGS B-034，用户反馈五点：节点状态未显示/详情缺文本/点击黑闪/节点尺寸不齐过长/连线不美观）：①**节点卡重构**——固定尺寸 118×92（不再随链长横向拉伸），内容层三行（名称 + 右上状态徽标 tag[已解锁绿/可解锁黄/需前置灰] + 效果摘要两行 + 状态行 ✓已解锁/科技点×N/需前置科技）；②**点击黑闪修复（B-034）**——未选中态覆盖 pressed/hover_pressed；③**详情条重构**——黄框 3px（对齐概念图），左列 = 名称 + 分类·X chip + 状态徽标 + 效果摘要 + 描述 + 前置/同类已投入 pills，右列 = 消耗 科技点×N + 余额 N·充足/不足 + 解锁按钮（黄/灰 Kenney）；④树网格**靠左展示**（SHRINK_BEGIN）+ 层级行标签列，连接线居中加高（26px 浅蓝），节点 118 宽七链一屏收齐。
> v0.20.1 变更（2026-09-05，阶段 8·提交 10 延伸·百科对齐概念图，程序 0.8.10.15 / GDD v0.36.7，用户反馈「百科与概念图出入太大，敌人图鉴章节用下拉框」）：①**§12 百科对齐 ui_encyclopedia*.png**——顶栏右侧计数/只读双 chips（武将 N 名·全量可见 / 敌人 N 种·第一章·黄巾军 + 只读信息·不写存档）；左列 2 列卡片加**头像圆**（姓氏首字，职业色/敌人 body_color 着色）+ 姓名 + 职业·定位副行；武将详情头部重排（头像 64 + 名称 + 职业·称号 meta + 右侧「获取方式」实值）；基础页签顶部增**四统计格**（伤害/攻速/射程/建造费用，Lv.1 基准）；敌人详情头部加阵营色头像；②**敌人图鉴章节选择改下拉框**（用户拍板；对齐地图选关样式：白底蓝边圆角 + 缩放箭头 + 预留章节灰显，弹出列表浅色；节点名 ChapterRow 保留，B-023 显隐断言不变）；③UITheme.avatar_label 支持直接传基色（base 参数）。
> v0.20.0 变更（2026-09-05，阶段 8·提交 10 延伸·养成界面重构落地 + 科技树对齐，程序 0.8.10.14 / GDD v0.36.6 / BUGS B-033，用户反馈「养成与概念图差太多：技能页签缺失、职业页签不符、需加转职详情」「科技树与概念图不符」）：①**§6 武将养成重构 ✅ 落地（原设计定稿转开发）**——左列表卡改头像圆+姓名+职业·Lv 白卡（未解锁灰卡含来源行，计数「N 名 · 已拥有 M 名」）；信息卡按概念图 .base 重排（头像圆 64 + 称呼 chips（职业·近战/远程 + 称号）+ 右侧大号 Lv + 经验条带 n/m 数值 + 伤害/攻速/射程/建造费用四统计格 + 练兵令行）；**新增「技能」页签**（概念图 .skcard 三卡：怒气大招卡[红块+怒气条满怒100]+职业技能卡[金块，一转习得]+角色专属技能卡[蓝块，A 主动/B 被动]，文案同源百科 hints）；**「转职」页签更名「职业」**——当前职业身份 + 能力状态（大招已习得/职业技能一转习得）+ 转职进度摘要 +「转职详情 ▸」**整屏叠层**（1160px 白面板：转职路线树 + 候选条件逐行 + 转职按钮 Kenney 黄/灰，转职操作与确认弹窗（v0.32.2 规则）迁入叠层，PromoteDialogRunner 回归通过）；②**科技树对齐 ui_tech.png**——每行首列层级行标签（基础强化/进阶强化/终极强化）、节点卡三行文本（名称+效果摘要+状态行 ✓已解锁/科技点×N/需前置）、顶栏补「已解锁 N 项」chip；③**设置分组面板修复（B-033）**——Panel+锚定内容盒改 PanelContainer 撑开，字号层级统一。
> v0.19.1 变更（2026-09-05，阶段 8·提交 10 延伸·修复，程序 0.8.10.13 / BUGS B-032）
> v0.19.1 变更（2026-09-05，阶段 8·提交 10 延伸·修复，程序 0.8.10.13 / BUGS B-032）：**分段/筛选类按钮「未加载」修复**——百科图鉴切换与背包类目筛选改为创建时即应用样式、状态切换全量重应用（原只在选中时应用致未选中侧回落默认深色主题）；`apply_kenney_rect_button` 补 disabled 态（灰 Kenney）。
> v0.19.0 变更（2026-09-05，阶段 8·提交 10 延伸·六屏 Kenney 换肤落地，程序 0.8.10.12 / GDD v0.36.4，用户拍板「继续换肤，编队别漏」）：**养成/科技/背包/设置/百科/编队六屏按各自概念图完成亮色换肤，v0.18.0 过渡深色底板移除（§4 过渡兼容条款作废）**——①**共享基建（UITheme）**：新增亮色色板（LIGHT_* 22 项 + TAG_* 四组）与 `light_panel_style` / `light_card_style` / `tag_label` / `avatar_label`（圆形头像首字块）/ `apply_light_selectable`（浅卡四态+金框选中）/ `apply_light_tab_container`（圆角顶页签）/ `style_exp_bar` 七个共享组件，全部面板复用；②**武将养成**：标题/信息卡/经验条/列表卡（浅卡四态）页签化浅色，转职/信物/练兵令按钮 Kenney 化（可转=黄/不可=灰/信物=蓝）；③**科技树**：节点白卡状态描边（绿=已解锁/金=可解锁/灰=需前置）、浅色页签容器、详情白面板、解锁按钮黄 Kenney、科技点金 chip；④**背包**：按概念图重构——顶栏资源胶囊 + 类目筛选 chips（全部/货币/抽奖券/材料/碎片/消耗品，Kenney 蓝 on）+ 道具 4 列卡片网格（图标块/名称/类型徽标/两行描述/×数量，点选金框）+ 底部黄框说明条；⑤**设置**：白底三分组面板（显示/音频/玩法）+ Kenney 按钮；⑥**百科**：全局亮色映射 + 左列卡/页签/数值模拟器浅色化；⑦**编队**：背景亮化、武将/遗物卡浅卡四态、返回选关灰 Kenney、确认出战黄 Kenney；窗口化探针逐屏截图核验（六屏 + 编队）。
> v0.18.0 变更（2026-09-05，阶段 8·提交 10 延伸·大厅 Kenney 换肤落地，程序 0.8.10.11 / GDD v0.36.3，用户拍板「按游戏大厅概念图与源码换皮，参考已落地首页」）：①**§4 大厅外壳（GameHub）换肤**——按 `ui_hub_map.png` 概念图与 `src/ui_hub_map.html` 落地：天空三档渐变 + 顶部柔光背景 → 居中 1232×648 圆角大面板（`#f2faff` 底 + `#1c9fd7` 边框 + 底边 `#167da8` 加厚 + 大投影）→ 左侧 204px 亮蓝侧栏（logo「烽火连营」+「TOWER DEFENSE · HUB」、Kenney 九宫格导航：选中=蓝 gloss 白字 / 未选=灰 flat 深蓝字、底部红「返回主菜单」+ 版本号）；**过渡兼容**——养成/科技/背包/设置/百科五屏尚未按各自概念图换肤，激活非地图页签时内容区垫深色圆角底板保证可读，随五屏换肤逐屏移除；②**§5 地图选关（MapPanel）换肤**——章节选择由胶囊按钮行改为**顶部下拉框**（白底蓝边圆角 + 下箭头，后续章节灰显「敬请期待」，弹出列表同步浅色样式）；顶栏右侧通关进度胶囊（已通关 n/m · 总星 n/m，星级为阶段 10 预留恒 0）+ 难度 Kenney 分段按钮（蓝 on/灰 off，随 difficulty_presets 扩展）；关卡卡片改 **4 列 × 2 行**白卡（状态徽标 已通关绿/可挑战黄/未解锁灰 + 星级三槽（预留灰）+ sNN 关名 + 两行简介 + 敌人/波次·经验两行 + 虚线分隔首通奖励行；状态描边绿/黄（可挑战带光晕）/灰，锁定灰底禁用）；底部**关卡预告条**（白底黄框圆角：徽标+关名+难度 chips、简介一行、敌人/波次/经验/推荐四统计胶囊、首通奖励徽章（武将绿/道具蓝）+ 一键通关(测试)灰 + 出征·编队黄大按钮）；③UITheme 新增 `spaced_font` 共享字距字体封装（首页同名私有实现收敛复用）。
> v0.17.4 变更（2026-09-04，阶段 8·提交 10 延伸·修复 2，程序 0.8.10.2 / GDD v0.35.3 / UI_CONCEPT v0.2，武将养成「职业」页签收纳 +「转职详情」概念图定稿，用户拍板 2026-09-04，纯文档/素材无程序逻辑改动）：①**「转职」页签更名「职业」并收敛为当前职业信息**——页签只保留：当前职业身份（职业名 / 定位标签 / 克制 / 未转职状态）+ 职业能力状态（大招「破阵」已习得·职业自带、职业技能「军旗」未习得·一转解锁，效果收敛「技能」页签不重复铺开）+ 转职进度条（下一转目标 + 等级 / 材料达标徽章）；页签文案带黄色「可转」徽标提示入口；②**新增「职业 → 转职详情」二级叠层**——职业页签「转职详情 ▸」按钮打开 1160px 整屏叠层（沿用弹窗视觉语言，不替换页签、不嵌套层级）：树状展示该角色全部转职信息 = 当前职业（绿点）→ 一转（唯一，金框可转职 +「转职为」按钮）→ 二转分支 2 选 1（陷阵·强化线 / 虎卫·新技能线，未达标灰锁），每节点分行展示技能习得 / 强化、数值倍率与等级·材料需求；**转职操作由原「转职」页签迁入此处**，二次确认弹窗规则不变（v0.32.2）；③**养成页签内容区取宽**——左列武将列表 402→382px，页签卡内分行排版、按钮定宽（176px），消除文字横向溢出；④**技能页签表述同步**——「二转强化详情见『转职』页」改为见「职业」页「转职详情」；⑤概念图 `ui_develop_job.png`（职业页签）+ `ui_develop_promo.png`（转职详情叠层演示）新存档（源码 `src/ui_develop_job.html`·`src/ui_develop_promo.html`，映射见 UI_CONCEPT v0.2 §4）；百科武将详情「转职」页签（只读全量）维持不变。
> v0.17.3 变更（2026-09-04，阶段 8·提交 9 延伸·修复 3，程序 0.8.9.3 / GDD v0.34.3 / BUGS v0.19，编队概念图卡面费用样式修正 B-025）：①**「建造费用」贴片式右栏改卡面底部内嵌通栏**——概念图 `ui_squad.png` / 源码 `ui_squad.html` 覆盖更新：费用不再置于卡片右侧虚线隔离的贴片区（fk/fv/状态竖排观感如后补补丁），改卡面底部内嵌通栏：细虚线上承、与卡面同底色同圆角一体，金色「建造费用」小标签 + 金币数值居左，出战 / 可上阵 / 未解锁状态徽标居通栏右侧；锁定卡不显示费用、通栏左侧 🔒；②**卡面费用展示规则**——任何卡面需展示费用时一律用底部内嵌通栏，禁止右侧贴片式色块栏（当前游戏内编队卡不展示费用，落地时机另行排期，本规则约束概念图与未来换肤落地）。
> v0.17.2 变更（2026-09-03，阶段 8·提交 9 延伸·修复 2，程序 0.8.9.2 / GDD v0.34.2 / ENCYCLOPEDIA v0.1.6）：①**百科卡片文本宽度纪律（BUGS B-024）**——左列卡片只写放得下的内容：武将卡 = 姓名 + 职业名·战斗定位词（输出/控制/辅助/混合），攻击方式 / 克制等长信息移入右侧详情；敌人卡 = 名称 + 定位·阵营；②**卡片不再启用省略号截断**（原武将卡第二行过长出现「舞娘 · 辅助 · 光…」式半截文本），宁精简不放不下。
> v0.17.1 变更（2026-09-03，阶段 8·提交 9 延伸·修复 1，程序 0.8.9.1 / GDD v0.34.1 / ENCYCLOPEDIA v0.1.5）：①**百科左列网格铺满修复（B-023）**——武将/敌人卡 2 列网格横向铺满左栏（原 GridContainer 列宽按单元格最小宽度塌陷至 8px、卡片文字裁切不可点选），ScrollContainer 禁横向滚动 + 网格与单元格 `SIZE_EXPAND_FILL`；②**敌人图鉴视图新增顶部章节选择行**（第一章可点、后续章节灰显「敬请期待」，与地图选关同一预留清单）；③**敌人左列改 2 列卡片网格**（名称 / 定位·阵营），替代原整列竖排全宽卡片。
> v0.17 变更（2026-09-03，阶段 8·提交 9 游戏百科落地，程序 0.8.9.0 / GDD v0.34.0 / ENCYCLOPEDIA v0.1.4）：①**GameHub 侧栏「百科」按钮接入**（位于「设置」与「返回主菜单」之间，新增不改既有侧栏结构）——§12 游戏百科排版规范随 `EncyclopediaPanel` 落地（代码构建 UI）：武将页 = 左 2 列可滚动网格 + 右侧 五页签 + 底部数值模拟器条（只读，标注「预览不影响存档」）；敌人页 = 左列表 + 右侧详情滚动容器（基础面板 / 特殊行为 / 各档难度面板 / 出现关卡）；②滚动容器按 v0.14 ③规范（列表超高滚动兜底、不撑爆页面）；③语义色与按钮选中/四态沿用 UITheme（金=关键预览值、蓝=数值、灰=说明、绿=获取方式），无面板私有颜色魔数。
> v0.16 变更（2026-09-03，阶段 8·提交 8 延伸·修复 8，武将养成技能页签 + 游戏百科概念图存档，用户拍板 2026-09-03）：①**大厅侧栏「百科」按钮入列六屏概念图**——地图选择×2 / 武将养成 / 科技树 / 背包 / 设置，按钮位于「设置」与「返回主菜单」之间（阶段 8 提交 9 前置设计，开发随提交 9 接入 GameHub，新增只加按钮不改结构）；②**武将养成新增「技能」页签**（概念图 `docs/ui_concept/ui_develop.png` 覆盖更新，页签序=技能/转职/信物/特性）——技能页=职业大招（怒气上限 100，满怒自动释放/可选手动）+ 职业技能（常驻光环 + 当前档位状态）+ 角色专属技能（A 主动/B 条件）；职业技能档位强化仍收敛「转职」页；③**新增 §12 游戏百科排版规范**（原 §12 验收标准顺延 §13、§13 不做什么顺延 §14）——概念图 `docs/ui_concept/ui_encyclopedia.png`（武将图鉴）与 `ui_encyclopedia_enemy.png`（敌人图鉴）存档，完整设计见 ENCYCLOPEDIA v0.1.3。
> v0.15 变更（2026-09-03，主菜单 Kenney 换肤首屏落地，用户拍板 2026-09-03）：①**新增 §3 主菜单排版规范**——概念图 `ui_home.png` 落地 `MainMenu`（程序 0.8.8.6）：天空三档蓝渐变（#46afe6→#3399da→#277fb9）+ 顶部/两下角白色柔光 + 低透明度星形装饰；主标 78px 白字深蓝投影、副标/星条饰件/章节行齐全；主按钮 296×84 38px 黄（主行动，深蓝墨字 #22384a）/ 红（新的征程，白字，仅存档存在时显示）/ 灰（退出，深蓝墨字），Kenney 九宫格三态素材 `assets/ui/buttons/rect/{yellow,red,grey}/{normal,hover,pressed}.png`（原图 384×128、裁边 24），换肤封装 `UITheme.apply_kenney_rect_button` 供后续面板复用；「新的征程」确认弹窗改自绘白面弹窗（对齐 `ui_dialog.png`，替换原生 ConfirmationDialog）；底栏左版本右素材署名；②**章节编号顺延**——新增 §3 后，原 §3~§12 顺延为 §4~§13（当前正文引用同步，历史 changelog 编号保留原貌）。
> v0.14 变更（2026-09-03，UI 概念图扩充与地图选关改版，用户拍板 2026-09-03）：①**大厅·地图选关概念图改版**（`ui_hub_map.png` 覆盖更新）——章节选择移至顶部下拉框（后续章节灰显「敬请期待」）、关卡 2×4 卡片紧凑满档（状态/星级/一句话简介/敌人/波次·经验/首通奖励，数据对齐 `StageData` 与 STAGES.md）、通关统计上移顶栏、底部新增「关卡预告」详情条（敌人/波次/参战经验/推荐经济四栏 + 首通奖励徽章 + 出征按钮）；②**新增五屏概念图存档**——`ui_develop.png` 武将养成（左 2 列列表 + 右上基础信息卡 + 右下转职页签，属性按 `compute_stats_at` 实算）、`ui_tech.png` 科技树（四页签 + 三层节点状态色 + 底部详情）、`ui_inventory.png` 背包（类目筛选 + 网格 + 底部说明条，未获得信物灰卡示源）、`ui_settings.png` 设置（画面/音频/玩法/数据与关于分组）、`ui_squad.png` 编队（独立整屏：标题带关卡与难度 → 武将 3×3 → 羁绊提示条 → 队伍遗物 2/2 → 底部操作条，对应 §6）；③**滚动容器规范**——科技树/背包/编队列表区用可滚动容器（内容超出滚动兜底、不撑爆页面；滚动条样式统一浅蓝圆角）；④**术语统一**——「出击费用」更名「建造费用」（= 局内 `build_cost`，养成属性格与编队卡面同步）。
> v0.13 变更（2026-09-03，UI 视觉风格定稿与全局字体，风格拍板 2026-09-03）：①**视觉风格方向定稿**——Kenney UI Pack 亮蓝底 + 站酷快乐体（Q 版活泼风），概念图存档 `docs/ui_concept/`（`ui_home.png` 首页 / `ui_dialog.png` 新的征程确认弹窗 / `ui_button_states.png` 按钮四态 / `ui_hub_map.png` 大厅·地图选择）；②**全局字体接入**——`assets/fonts/ZCOOL-Kuaile.ttf` + `assets/ui/ui_theme.tres`（默认字体=快乐体，回退微软雅黑/黑体等系统字体），`project.godot` `gui/theme/custom` 全量应用（冒烟/流程/回归测试全绿）；③**语义色映射**——Kenney 黄=主行动（UITheme GOLD 语义，视觉上以黄替金）、红=警示（RED）、灰=中性（GRAY）、蓝=信息（BLUE）、绿=成功（GREEN）；按钮四态（normal/hover/pressed/disabled）样式见 `ui_button_states.png`；正式换肤（按钮九宫格/面板底图/背景替换程序化色块）排期待定。
> 文档版本：v0.12（2026-09-03）
> v0.12 变更（2026-09-03，阶段 8·提交 8 延伸·修复 3，局内建造交互重构，用户拍板 2026-09-03）：①**建造方式唯一化为拖拽**——移除点击建造 / 两次点击确认 / 绿色 + 推荐位 / 预锁位解锁（B-021 悬停预览退役，可建反馈由拖拽虚影承担）；②**拖拽建造规范**——按住顶部武将卡片拖出：角色半透明虚影 + 攻击范围圈（同实塔计算）跟随鼠标，落点格可建=绿、不可建=红（道路/禁建/已占用/越界/金币不足/战后）；松手直接建造，右键 / ESC / 原地松手取消；拖入顶栏 / 卡条等 UI 区虚影隐藏、松手取消；③**武将卡片面板重设计**——武将名 / 职业全名 / Lv（蓝）/ 费用（金），卡片即拖拽手柄（按下金色高亮），金币不足置灰不可拖；原“选中武将”概念删除。
> v0.11 变更（2026-09-03，阶段 8·提交 8 延伸·修复 2，编队显示与战场可建辨识度，用户反馈 2026-09-03）：①**编队武将卡片加宽至 232px**——双羁绊武将标签行 186px 在选中态金色样式（可用约 176px）下被裁剪，行尾「1/3」裁成「1/」（BUGS B-020）；卡片文本收敛 `_character_button_text()`，羁绊 X/Y 随勾选实时刷新；②**战场可建格悬停预览**（BUGS B-021）——澄清绿色 + 为推荐建造位（软引导、非唯一可建点），除道路/禁建地形外任意空地可建；选中武将后鼠标扫过可建格显示半透明绿色方块（道路/禁建/已占用格不显示），开局引导文案同步澄清。
> v0.10 变更（2026-09-03，阶段 8·提交 8 延伸·修复 1，出征流程与遗物调整）：①选关页「出征」**直达编队**——删除 v0.31.2 确认弹窗，防误触确认收敛到编队页「确认出战」二次确认；②编队页**纵向分区定稿**：武将选择区 → 羁绊提示条 → 遗物选带区 → 底部固定操作条（返回选关 / 确认出战），返回选关保留关卡与难度；③「确认出战」新增**二次确认弹窗**（展示关卡 / 难度 / 出战武将名单 / 遗物清单），确认后才进入游戏；④上次出战编队**持久记忆自动预填**（存档 `squad_character_ids` / `squad_relic_ids`，武将须仍拥有、遗物须仍有库存，超上限自动裁剪）；⑤武将养成移除「遗物」页签——遗物为队伍级，选带在编队页、库存与效果说明在背包页。
> v0.9 变更（2026-09-03，阶段 8·提交 7 延伸·修复 2）：养成面板**一转确认弹窗**改用 Godot 4 `canceled` / `close_requested` 信号释放（原误用 Godot 3 旧名 `cancelled`，点击一转弹窗弹出前即报 Invalid access，确认弹窗无法出现）。
> v0.8 变更（2026-09-02，阶段 8·提交 7 延伸·修复 1）：养成面板左侧列表**每次切回面板整体重载**——首通解锁武将实时移入已拥有区、卡片等级随档案刷新（原左侧停留在面板构建时状态）。
> v0.7 变更（2026-09-02，阶段 8·提交 6 延伸）：难度切换由复选样式改为**互斥单选 + 金色选中态**（ButtonGroup + `UITheme.apply_selected_style`，原复选框可多选、选中高亮不明显）；难度档位移除轻松（仅标准/困难，随 `difficulty_presets` 自动扩展）；「出征」按钮新增**确认弹窗**（展示关卡与难度，确认后进入编队，防误触）。
> v0.6 变更（2026-09-02，阶段 8·提交 6）：养成面板基础信息卡新增**角色称号**展示（金色小字，`CharacterData.titles`，纯记录无机制；称号 = 原角色专属转职名，见 CHARACTERS.md 4.5）。
> v0.5 变更（2026-09-02，应用户反馈）：科技树节点边框色改表状态（金=可解锁/绿=已解锁/灰=需前置）、根/叶用 ◆/○ 前缀区分、选中节点四态金色高亮、解锁按钮金色描边；养成面板未解锁分组独立标题。
> v0.4 变更（2026-09-02，应用户反馈）：科技树树状布局修正——行 = tier 层级、列 = 前置链（同级并排），根节点金边、叶子节点绿边、中间节点灰边，新增图例行。
> v0.3 变更（2026-09-02，应用户反馈）：科技树改树状布局（前置链分列 + tier 分层 + 竖线连接，行内仅成语名与状态）；地图选关首通文案分标签、摘要显示当前难度、一键通关提示自动清除。
> v0.2 变更（文档同步）：阶段 8·提交 4 已随 v0.27.0 落地实施，应用状态由"未开发"更新为"已实施"；规范内容不变。
> 应用阶段：**阶段 8·提交 4（界面排版重构）**——已随 v0.27.0 落地实施（地图选关 2×4 网格一屏、武将养成页签化、全局语义色板、交互态统一），规范见下文。
> 范围：主菜单、大厅、地图选关、武将养成、编队、科技树、背包、战斗、结算、设置的排版、信息层级与色彩规范。

---

## 1. 设计总原则

1. **信息分层**：一个界面只展示当前任务需要的信息；低频/进阶信息（转职、升星、信物、特性、详细数值）通过**页签、折叠或二级弹窗**承载，禁止全部堆在一屏。
2. **一屏一任务**：每个面板聚焦一个操作流程；默认布局在 1280×720 内一屏放得下，主功能区**禁用滚动条**，滚动只允许出现在内容确实超长的次级区域。
3. **网格优先**：同类条目（关卡、武将、道具）一律用固定列数网格（`GridContainer`），列数按内容数与一屏容量设定，不依赖竖排列表+滚动。
4. **关键信息强调**：关键信息统一使用语义色高亮（见第 2 节），玩家扫一眼即可区分"可做 / 已做 / 不可做 / 数值"。
5. **拓展空间**：所有面板为后续功能预留位——页签可增、网格可加列/翻页、卡片可加角标；**新增功能只增不重构**。
6. **交互反馈**：每个可交互元素具备 hover / 按下 / 选中 / 禁用四种明确状态；点击目标 ≥ 40×40px；操作有即时反馈（颜色变化 / 音效 / 飘字）。

## 2. 语义色板（全局统一，收敛为共享常量）

| 语义 | 色值 | 用途 |
|---|---|---|
| 强调 / 主色（金） | `0.92, 0.78, 0.42` | 标题、关键资源（金钱/科技点/材料）、可解锁、选中态 |
| 成功 / 达标（绿） | `0.55, 0.92, 0.80` | 已解锁、已通关、可升级、材料充足、胜利 |
| 警示 / 不足（红） | `0.90, 0.45, 0.40` | 锁定、材料不足、生命告急、失败、Boss 预警 |
| 信息 / 数值（蓝） | `0.65, 0.84, 1.00` | 属性数值、说明文字、经验 |
| 次级文字（灰） | `0.70, 0.72, 0.68` | 提示、占位、未解锁说明 |
| 底色（深） | `0.06, 0.09, 0.08` / 侧栏 `0.09, 0.13, 0.11` | 界面背景与侧栏 |

- 所有面板引用**同一色板常量**（新增 `scripts/ui_screens/ui_theme.gd`，静态常量或 autoload）；重构时删除各面板私有的 `TITLE_COLOR / OK_COLOR / BAD_COLOR` 等重复定义，禁止私有魔数颜色。
- 相同语义必须同色：例如"材料不足"在养成、科技树、编队中均为红色，不因面板不同而换色。

## 3. 主菜单（MainMenu）——Kenney 换肤首屏落地（v0.33.6）

> 概念图：`docs/ui_concept/ui_home.png`（确认弹窗见 `ui_dialog.png`）；程序 0.8.8.6 全量落地，`MainMenu.gd` 代码构建。

- **背景**：天空三档蓝渐变（自上而下 `#46afe6 → #3399da → #277fb9`）+ 顶部与两下角白色径向柔光 + 低透明度星形装饰（`assets/ui/icons/star_outline.png`，白色调淡），原黑底程序化色块退役。
- **标题区**（顶部居中）：主标「烽火连营」78px 白字、字距 14、深蓝投影（`#14538a`，偏移 0/3）；副标「三国塔防」25px `#eaf6ff` 字距 24；饰件行 = 白色半透明横条 120×5（圆角）+ 黄色实心星（`assets/ui/icons/star.png`，约 28×26）+ 白色半透明横条；章节行「第一章 · 黄巾之乱」19px `#eaf6ff` 字距 6。亮底白字统一深蓝投影保证可读。纵距以快乐体实际行高微调，视觉对齐 `ui_home.png`（首钮顶边 ≈ y260）。
- **主按钮**（296×84、38px、字距 8）：Kenney 九宫格三态素材 `assets/ui/buttons/rect/<色>/{normal,hover,pressed}.png`（原图 384×128、裁边 24），换肤封装 `UITheme.apply_kenney_rect_button`（normal/hover/pressed + hover_pressed 复用 pressed、focus 去框），后续面板换肤复用同一入口。
  - 黄=主行动（开始游戏 / 继续出征），深蓝墨字 `#22384a`。
  - 红=新的征程（白字带细微深色投影），**仅存档存在时显示**。
  - 灰=退出游戏，深蓝墨字。
  - 行为：无存档=黄「开始游戏」入大厅；有存档=黄「继续出征」入大厅 + 红「新的征程」；「退出游戏」=进程唯一退出入口（v0.10.1 保留）。
- **「新的征程」确认弹窗**（自绘，对齐 `ui_dialog.png`，替换原生 ConfirmationDialog）：55% 深蓝遮罩（rgba(10,38,64,0.55)，模态拦截点击）→ 白面面板（`#f2faff`、蓝框 `#1c9fd7`、圆角 18、投影）→ 蓝标题栏（高 56：标题「新的征程」26px 白字 + 右侧圆形半透明白底关闭 ×，图标 `assets/ui/icons/cross_blue.png`）→ 正文三行：主句「新的征程将开启一份全新存档。」24px `#1b4d78`、红警示句「当前进度将被覆盖，且无法恢复。」20px `#d71935`、灰提示句 15px `#7d9cb4` → 右下操作：灰「取消」/ 红「继续」（160×68、28px）。ESC / 关闭 × / 「取消」→ 关闭；「继续」→ 覆盖开新档并进入大厅。
- **底栏**：左=版本「FengHuoLianYing · ALPHA <config/version>」（17px `#eaf6ff`），右=素材署名「素材 Kenney UI Pack · 字体 站酷快乐体」（15px `#cfe7f7`）。

## 4. 大厅布局（GameHub）——Kenney 换肤（✅ v0.18.0）

- **整体**（对齐 `ui_hub_map.png` / `src/ui_hub_map.html`）：天空三档渐变背景（SKY_TOP/MID/BOTTOM）+ 顶部柔光 → 居中 1232×648 圆角大面板（`#f2faff` 底、`#1c9fd7` 3px 边框、底边 7px `#167da8`、圆角 18、深色大投影）。
- **左侧亮蓝侧栏 204px**（`#e9f6fd→#dcedf8` 渐变观感、右缘 `#bfe2f4` 分隔）：顶部 logo「烽火连营」25px `#14538a` + 「TOWER DEFENSE · HUB」10px `#7d9cb4`，虚线分隔；导航按钮 Kenney 九宫格（高 50、字 21px 字距 3）——**选中=蓝 gloss 白字 / 未选=灰 flat `#33566f`**（地图 / 养成 / 科技 / 背包 / 设置 / **百科**，见 §12）；底部红 Kenney「返回主菜单」+ 版本号小字居中。
- **右侧内容区**：浅色面板内边距（上 14 / 左右 20 / 下 12）；六屏（地图/养成/科技/背包/设置/百科）已全部亮色换肤（v0.19.0）。
- 预留：侧栏按钮可新增（如后续"挑战"入口），新增只加按钮不改结构。

## 5. 地图选关（MapPanel）——Kenney 换肤（✅ v0.18.0）

- **顶栏**：「地图选择」27px `#1b4d78` + **章节下拉框**（白底蓝边圆角 + 下箭头图标 `arrow_basic_s_blue`，当前章节可点、后续章节灰显「敬请期待」，弹出列表同步浅色样式）→ 右侧通关进度胶囊（「已通关 n/m」「总星 n/m」，`#e1f1fb` 底；**星级为阶段 10 预留恒 0**，挑战目标落地后接入实值）+「难度」+ **Kenney 分段按钮**（蓝 on / 灰 off，92×42，随 `difficulty_presets` 自动扩展，选中互斥规则不变）。
- **关卡卡片网格 `GridContainer` 4 列 × 2 行**（间距 12，一屏 8 关无滚动），白卡圆角 14，状态描边：已通关 `#9ad8b4` / 可挑战 `#ffcc00`（带光晕）/ 未解锁 `#cddbe6` 灰底禁用；卡面自上而下：状态徽标（已通关绿底 / 可挑战黄底 / 未解锁灰底）+ 星级三槽（`star.png` 实星 / `star_outline_grey.png` 空槽，预留）、sNN 关名 19px `#1b4d78`、两行简介 13px `#6b93ad`、「敌人 / 波次·经验」两行、虚线分隔的首通奖励行（紧凑文案：解锁 X · 道具×N · 信物 X）。
- **底部关卡预告条**（白底 `#ffcc00` 边框圆角 14，高 ≥152）：左列=「关卡预告」徽标 + 关名 + 「难度·X」绿 chip + 一行简介 + **敌人/波次两统计胶囊**（v0.37.7 移除经验/推荐——内容自适应宽、键值自然排版，不再均分撑满整行）；右列=首通奖励徽章（武将绿 `#16bb77` / 道具蓝 `#2eaadc`）+「一键通关(测试)」灰 Kenney +「出征·编队」黄 Kenney 大按钮（点击直达编队，v0.33.1 规则不变）。
- 交互不变：卡片点选 → 预告条联动；返回选关保留关卡/难度（v0.33.1）；一键通关结果提示在刷新/切换时自动清除。
- 分辨率内一屏完整，不出现任何滚动条。

## 6. 武将养成（DevelopPanel）——页签化重构

**现状问题**：单面板堆叠全部信息（等级/属性/一转/升星/遗物/求贤），信息过载、无拓展空间。

**新布局**：
- **顶部标题行（v0.20.11 定稿）**：顶栏「武将养成」27px 墨蓝标题；概念 `.topbar` 右上资源胶囊（黄巾布 / 练兵令）与「已上阵 n/n」中——**资源胶囊按用户要求移除**（本版偏离概念图，登记 v0.20.11），资源数量改看背包页顶栏；「已上阵」仍未实现。
- **左侧武将列表**：网格 2 列卡片（头像占位 / 姓名 / 职业 / 等级；**每次切回面板整体重载**——新增武将与等级随档案实时刷新，v0.32.1）；未解锁=图鉴灰卡（显示获取方式）；选中金色边框。
- **右侧上半=基础信息卡**（只展示基础信息）：等级 + 经验条、职业、**角色称号（金色小字）**、当前属性摘要（伤害 / 攻速 / 射程，蓝色数值）；**经验池（2026-09-10 定稿、排期 0.8.15）**：追加**经验池余额**行 + **分配入口**（选武将 → 分配 → **二次确认**，不可逆）+ **「直接升级」**按钮（消耗池内经验 +1 级、二次确认）；原「使用练兵令」行随 `exp_scroll` 删除（见 CHARACTERS 4.4 / NUMBERS 10.14）。
- **右侧下半=页签区（TabBar）**，进阶信息全部经页签切换：
  - 「技能」（概念图已存档 v0.33.8，开发待排期）：职业大招（怒气上限 100，满怒自动释放 / 设置面板可选手动）+ 职业技能（常驻项与当前档位状态，二转强化详情见「职业」页「转职详情」）+ 角色专属技能（A 主动冷却 / B 条件触发，见 CHARACTER_SKILLS.md）；与百科武将详情技能页签（§12）信息同源、展示侧重不同。
  - 「职业」（v0.17.4 由「转职」更名；概念图 `ui_develop_job.png` 存档，开发待排期）：只展示**当前职业信息**——当前职业身份（职业名 / 定位标签 / 克制 / 未转职或当前转职状态）+ 职业能力状态（职业大招已习得·职业自带、职业技能未习得·一转解锁，效果详见「技能」页签，不重复铺开）+ 转职进度条（下一转目标 + 等级 / 材料达标徽章）；页签标题恒为「职业」（v0.20.9~v0.20.13 曾以标题后缀「职业 · 可转」近似概念 `.tabbadge`——Godot 4 TabBar 不暴露内部 tab 按钮无法挂徽标，v0.20.14 按用户拍板移除后缀，`.tabbadge` 登记为对概念图的偏离不再实现）；转职操作与完整路线不在本页签铺开。
  - 「职业 → 转职详情」（v0.17.4；概念图 `ui_develop_promo.png` 存档，开发待排期）：职业页签「转职详情 ▸」按钮打开 **1160px 整屏叠层**（沿用弹窗视觉语言）——树状展示该角色全部转职信息（当前职业 → 一转 → 二转分支），每节点分行展示技能习得 / 强化、数值倍率与等级·材料需求；**转职功能迁入此处**（现有转职流程 + 二转分支选择）；转职操作带**确认弹窗**——提示不可逆、展示目标职业，确认 / 取消 / 右上角关闭均正确释放（v0.32.2 规则不变）。
  - **v0.20.9 概念对齐（ui_develop.html / ui_develop_job.html / ui_develop_promo.html）**：左列表卡等级/姓名刷新改按命名标签更新（防 find_children 顺序覆盖污染头像与职业行，B-038）；技能页签角色专属技能名右侧补 `.roletype` 金色小字（A 主动 · CD x s / B 被动 · 条件触发）、大招 tag 改「{职业名} · 大招」；职业页签身份卡=浅白卡（tags 中文映射定位词 + 克制 lock tag + 右状态列，描述取职业数据）、未习得职业技能 tag 改灰 lock、转职进度卡改 `.jcard.jpromo`（标题 + 横向 chips（下一转 / Lv 达标 / 材料）+ 完整路线说明 + 右黄按钮 176×46）；转职详情二转分支两卡并排（B-039）+ 分支卡补技能线 tag / 数值行 / 条件胶囊；基础信息卡攻速改攻击间隔秒口径（0.67s）。
  - **v0.20.10 溢出修复（B-042）**：职业页签内容整体套纵向滚动容器（与技能页签一致）；转职进度卡 chips 按概念 `.jchips`（`flex-wrap:wrap`）改 `HFlowContainer` 自动折行——窄于单行总和时折为多行、不再把卡片/页签撑宽外溢（1280×720 下「下一转 / Lv / 材料」三 chips 仍单行完整展示）。
  - **v0.20.12 转职详情节点定稿（B-043）**：节点顶行 = 头像圆 + 名称 18px + 状态 tag + step 浅字 + 右列定宽 act（转职为 180 / 分支卡 176 / 未解锁 128）；底部通栏 = 描述（弱色）+ `.fx` 详情（习得/强化技能 + 数值，自动折行）+ 条件胶囊流（HFlow 可折行）。二转分支（无论灰锁预览还是已一转真实候选）一律 `.brow` 两卡并排各半宽；分支需求标题（Lv / 材料）取分支自身数据。外框 = `.promo-panel` 蓝描边圆角大面板，头部亮蓝底白字 26px。
  - **v0.20.13 转职详情叠层终版（B-044，当前定稿）**：叠层 = 1160×570 `.promo-panel`（#f2faff 底 / 18 圆角 / 3px #1c9fd7 描边 / 7px 粗下边）**整窗居中**（挂 GameHub 顶层），内容列 = 蓝渐变头 `.promo-title`（56px，#46b7ea→#1c9fd7；h2「转职详情」26px 白字 6px 字距 + who 胶囊**紧贴标题右侧** + ✕ 圆钮贴右）+ 顶部 `.ptip` 虚线说明胶囊 + `.ptree` 树（左侧 3px #bfe2f4 竖轨贯穿；节点卡顶行 = **名称 18px（无头像圆）** + 状态 tag + step 浅字 + 右列定宽 act；正文 = `.fx` 弱色/墨蓝 + 条件胶囊流）+ 底部 `.pfoot` 虚线说明胶囊（弱色 #7d9cb4 + 金色「二次确认」）；`.ptip`/`.pfoot` 均为 DashedPill（浅底 + #bfdcef 1px 虚线 + 圆角 10）自绘虚线描边。树内容超高时滚动兜底（570 面板三态内容均一屏放下）。二转分支一律两卡并排各半宽。
  - 「升星」：星级展示、碎片数量、升星按钮与效果说明（现有升星区迁入；**随 v0.30.0 暂时移除**）。
  - 「信物」：**双信物槽（2026-09-09 信物重构定稿，排期 0.8.14）**——专属信物槽（绑定武将；先锁住占位、不开放）+ 可选信物槽（开放，装通用 / Boss 签名信物）；碎片兑换入口随碎片整体删除；槽位版式细节（锁定占位 / 通用信物列表 / 装配交互）待 0.8.14 落地前设计，本行为口径同步（旧口径"武将专属信物装备/更换、信物 = 武将专属与队伍级遗物区分 v0.33.1"保留作历史）。
  - 「遗物」：**随 v0.33.1 移除页签**——遗物为队伍级：选带在编队页 §7、库存与效果说明在背包页 §9「遗物」类目；不再作为武将页签展示（原只读库存说明迁至背包页）。
  - 「特性」（预留页签）：角色常驻特性说明，阶段 9 升阶特性接入时直接挂载，不改布局。
- **求贤（抽奖）**：从养成页移除作为独立入口（侧栏新增"求贤"按钮或养成页右上角按钮弹窗），不再占用养成主区；**随 v0.30.0 暂时移除（入口关闭，恢复时按本设计接入）**。
- 未来新增角色功能 = 新增页签，**不动既有布局**。

## 7. 编队（SquadSelect）——概念图 v2/v3 版式（v0.20.35 定稿；✅ v0.20.36 / 程序 0.8.11.11 按图落地；v0.20.37 / 0.8.11.12 右栏与遗物展示按用户反馈简化；v0.20.39 / 0.8.11.13 右栏改三组竖排明细；承 v0.33.1 纵向分区 / B-025）

- **整体版式（1280×720 一屏，权威稿 `docs/ui_concept/ui_squad.png` / 源码 `src/ui_squad.html`，Kenney 亮蓝 + 站酷快乐体）**：
  - **标题区**：左「出征 · 编队」+ 章节 tag（如「第一章 · 黄巾之乱」）+ 关卡 · 难度 tag（如「s04 东郡驰援 · 标准」）+ 右侧「出战 n / n」计数。
  - **操作提示行**：说明「点选 / 取消 · 金色描边为出战 · 羁绊与右侧队伍加成实时刷新」+ 蓝 tag「溢出自动滚动」+ 右侧「已拥有 N · 未解锁 M」计数（扩容预留格不计名）。
  - **主体两栏**（左 914px + 右 254px，间距 14）：
  - **左 · 武将选择区（3 列滚动网格，✅ 已落地）**：卡片 = 圆头像（首字 / 职业语义色）/ 姓名 + Lv chip / 「职业 · 定位」副行（如「虎贲 · 辅助」）/ 羁绊进度 mini 标签（激活绿「桃园 3/3」、预览黄「五虎 3/5」，随勾选实时刷新）/ **卡面底部内嵌通栏建造费用（B-025）**：细虚线上承、与卡面同底同圆角，金色「建造费用」小标签 + 金币数居左，出战（绿）/ 可上阵（蓝）/ 未解锁（灰 + 🔒，无费用）状态徽标居右；选中金边 #ffcc00；未解锁灰卡标注解锁来源（如「通关 s04 东郡驰援解锁」）。点击选中 / 取消，最多 `squad_size` 名，满员后未选卡禁点（沿用 v0.33.1 禁选语义）。**行高 ≥130 不随人数压缩**：≤9 名 3×3 一屏铺满；第 10 位起自动增行、网格超高纵向滚动。实现口径（0.8.11.11）：网格由角色数据驱动，**未做**概念中的虚线「新武将·预留位」扩容卡——当前 9 将恰好 3×3 铺满不滚动，未来 >9 时自动增行滚动。
    - **左 · 队伍遗物行**：左侧 88px 窄说明列（「队伍遗物」+ 小字：悬停看详情 / 移开即消失 / 图标待美术）；右侧遗物 pill 横排 = 紫晶渐变图标占位（首字白字，待美术）/ 名称 / 状态徽标（已选黄、可选蓝）；**不显示持有数量**（v0.20.37 简化）；点击选带**最多 2 件**（超出禁选）；**悬停浮层看详情**（黄框小浮窗：名称 + 状态 + 一句描述 + 效果文本块（定宽 232 自动换行）+ 「队伍级 · 永久使用 · 不消耗库存」元信息；移动即展示、移开即消失，浮层不占页面布局）；遗物超过一行**横向滚动**（pill 最小宽 150px，禁止压缩）。
    - **右 · 队伍加成面板（254px 白卡，随编队实时计算；v0.20.39 明细化定稿）**：标题「队伍加成」+「随编队实时计算」tag + 虚线分隔 → 三组**竖排明细**（组头 = 色点 + 组名 + 延伸虚线；组内每行 = 效果名（左，墨蓝 #1b4d78）+ 数值（右，金色 #b8860b），11px、行缩进 14px、组间 10 / 行间 2）：①科技 = 已解锁条目按效果**合并累加**（全武将伤害 / 出战职业伤害·攻速·增幅（仅该职业出场时列出，同职业去重）/ 初始金币 / 波次奖励 / 基地生命 / 怒气获取 / 出售返还 / 升级折扣 / 军需折扣）；②羁绊 = 每条**已激活**羁绊一行「名称 n/n + 同队攻击 +N%」（未满员不列）；③遗物 = 每件**已选带**一行「名称 + 效果摘要」（攻速按 (1/interval−1) 折算为 +N%）。**顶对齐、从上往下排列（不居中）**；单组空态灰字占位（未解锁科技 / 未激活羁绊 / 未选带遗物 + `—`）；明细超高时组内纵向滚动；底部灰字注脚「仅计入已解锁科技 / 已激活羁绊 / 已选带遗物；同类效果合并累加。」。**不展示**总战力折合大数字、不做总加成折算、不标注来源（v0.20.37 / v0.20.39 按用户反馈收敛）。
  - **底部固定操作条**：「返回选关」（灰，保留所选关卡与难度）→「确认出战」（金色主按钮）。
- **滚动兜底（v0.20.35 规范 / ✅ 0.8.11.11 落地；v0.20.39 右栏明细组内滚动）**：武将网格 / 遗物行两区均为滚动容器（滚动条样式同 MapPanel：浅蓝 #7ec8ea 圆角细条、悬停亮蓝）——当前数据一屏铺满不显滚动条；武将 >9、遗物 >5 时自动出现；右栏队伍加成明细超高时组内纵向滚动（禁用横向滚动）。**禁止**压缩卡面 / 胶囊去适配更多数据。
- **「确认出战」二次确认弹窗（v0.33.1 防误触收敛点）**：展示 关卡名 / 难度 / 出战武将名单（含职业与等级）/ 遗物清单（注明永久使用不消耗）与简短说明；确认后 `goto_battle()` 正式进入游戏；取消 / 关闭回到编队可继续调整。
- **「确认出战」弹窗版式（v0.20.21 概念初稿，✅ 0.8.11.11 自绘落地，`ui_squad_confirm.png` / `src/ui_squad_confirm.html`）**：640×560 亮蓝白弹窗（55% 遮罩，沿用 §3 弹窗视觉语言）——蓝标题条（白字「确认出战」+ 章节名 chip 半透明白底 + 圆形 ✕）→ 元数据行（sNN 关名蓝 tag / 难度 · X / 出战 n/n）→「出战武将」分区（1px 虚线分隔条）：名单行 45px（38px 头像占位 / 姓名 / 职业 · 定位 / Lv / 「出战 n」绿徽标右对齐，行间 1px 虚线；>4 行纵向滚动）→「队伍遗物 · 永久使用不消耗库存」分区（150×52 小卡居中并排：紫晶图标占位 + 名称；v0.20.37 起不再显示效果与持有数；未选带灰字占位）→ 虚线提示条（确认立即进入战斗等规则）→ 底栏（左侧灰字存档记忆提示 / 取消灰钮 150×44 / 确认出战黄钮 176×44）；ESC / ✕ / 取消关闭，确认写入编队记忆并 goto_battle。
- **编队记忆自动预填（v0.33.1）**：确认出战即把编队写入玩家存档（`squad_character_ids` / `squad_relic_ids`）；再次进入编队页时自动预填上次出战配置——武将须仍拥有、遗物须仍有库存，超上限 / 缺失项自动裁剪，无匹配则空槽显示。


## 8. 科技树（TechPanel，配合提交 3 分类重构；v0.20.15 按 ui_tech.png / ui_tech.html 对齐）

- **顶栏一行（v0.20.15）**：左「科技树」标题（27px），右「科技点 ×N」金 chip /「已解锁 N 项」浅蓝 chip +「重置」灰钮（无条件重置，点击弹确认框）+「科技点+50（测试）」测试钮（v0.30.4 开发辅助，仅测试用，不进入正式结算逻辑）。
- **分类行（v0.20.15，概念 .tabs）**：军略 / 后勤 / 工事 / 将略（v0.23.0 拍板定名）4 枚 118×44 Kenney 按钮，选中蓝底白字 / 未选中灰底深字；行右 .legend 图例 = 状态色块（金可解锁 / 绿已解锁 / 灰需前置）+「◆ 根节点 → 逐层解锁」说明。
- **树状版式（v0.30.2 行=层级列=链、v0.20.2 固定卡、v0.20.15 行式重构）**：每层一行，首列 52px **竖排层级胶囊**（基础强化 / 进阶强化 / 终极强化），其后为该层节点卡按前置链顺序并排（军略 = 全武将 → 虎贲 → 骑兵 → 弓手 → 术士 → 舞娘 → 投石车）；层间 26px 连接带独立成行，父→子同列浅蓝竖线；节点卡 = 白底 3px 状态描边（已解锁 #f2fbf5 底 + #52c48a 边 / 可解锁 #fffdf0 底 + #ffcc00 边 / 需前置 #eef3f7 底 + #cddbe6 边，圆角 12），卡内左上 ◆/○ 层级符 + 右上状态字 + 名称 + 摘要 + 底部行（✓ 已解锁 / 科技点 ×N / 需前置名）；军略终极层行尾补概念 .t3-note **虚线提示条**（DashedPill，铺满剩余行宽）；选中节点金色外发光四态高亮（v0.19.2 防黑闪）。
- **详情条（v0.20.2 重构、v0.20.15 补 pill）**：黄框 3px；左列 = 名称 + 分类 chip（如「军略 · 职业强化」，CATEGORY_SUB）+ 状态徽标 + 摘要 + 描述 + pills（需前置 / 同类已投入 N 级 / 职业加成合计 +N%（上限 +10%）——职业链各条目 effect 数值求和，非职业链显示 —）；右列 = 消耗 科技点 ×N + 余额 N·充足/不足 + 解锁按钮（可解锁黄 Kenney / 不可灰）。
- 预留：分类可扩展，后续新分类直接加按钮与树。

## 9. 背包（InventoryPanel）
- 顶栏：标题（27px 字距）+ 右侧「共 N 种」chip；无资源胶囊（v0.20.31，黄巾布/练兵令数量以道具卡为准）。
- 类目筛选：全部 / 材料 / 消耗品 / 遗物（v0.20.31 收敛——货币/抽奖券/碎片无产出系统不生成；遗物 v0.20.30 落地=5 件局内遗物物品分类遗物，永久持有不消耗），Kenney 蓝 on / 灰 off，选中即过滤 + 重排。
- 道具卡片（4 列白卡，点选金框）：类型色渐变圆角方块图标 52px（`UITheme.tile_label`，材料蓝/消耗品橙/遗物紫）+ 名称 + 类型徽标 + 两行描述 + 金色 ×数量；列表按类型分组（材料→消耗品→遗物）+ id 字典序；超高滚动兜底。
- 底部说明条（白底黄框）：选中道具完整描述——56px 大方块 + 名称/类型徽标/遗物「永久使用」tag + 两行描述 + 遗物金色 note（队伍级 · 编队选带生效 · 不消耗库存 · 最多 2 件）+ 右列 ×n/持有；空类目/未选中时提示文案。
- 描述查看：悬停 tooltip 或选中后底部说明条，避免卡片内堆长文本。

## 10. 战斗界面（Main / UI）

- 顶栏（0.8.11.1 贴顶全宽 0..80，圆角去 0，不再悬空遮挡 row1）：左→右 金钱（金色）、基地生命（≤30% 变红脉动）、当前波次/总波次；按钮：军需、设置、自动开波开关。
- 武将卡片面板（v0.33.3 重设计；提交 11 / 0.8.11.0 改版；**0.8.11.1 移至底部常驻 640..720**）：半透明深色底栏（顶边 2px 浅蓝细线）内横向居中排列出战武将**横版卡 124×68**——左 **48px 圆形立绘头像**（关羽等有素材角色用 SpineSprite Idle 首帧 PNG（本地不入库，缺素材回退概念色占位圆））+ 右 武将名（15px）+ Lv（蓝）/ 费用（金）（11px）；职业名行移除；卡片即**拖拽手柄**（按住左键拖出建造，按下金色高亮）；金币不足头像/文字置灰 + 红色费用、不可拖。
- **拖拽建造（v0.33.3 定稿唯一方式；虚影主体 0.8.11.1 实塔小人化）**：除道路 / 禁建地形 / 已占用格外的任意空地均可建；按住卡片拖出后——**实塔同款小人虚影**（spine 角色直接显示 sprite / 程序化身体+武器回退；可建=绿染色 / 不可建=红染色 modulate）+ **攻击范围圈**（等级 / 转职 / 特性 / 遗物计算后的实际射程）跟随鼠标；落点格整格高亮：**可建=绿、不可建=红**（道路 / 禁建 / 已占用 / 越界 / 金币不足 / 战后）；**松手直接建造**，右键 / ESC / 原地松手取消；拖入 UI 区域（顶栏 y≤80 / 底部卡条 y≥640）虚影隐藏、松手取消。绿色 + 推荐位、预锁位、hover 可建预览（B-021）一并移除。
- 已建塔属性面板：属性数值（蓝色）、升阶/回收/（手动模式）大招按钮；升阶可升=金色高亮，费用不足=红色；**v0.20.22 / 0.8.11.0 起移除技能区**——职业技能/角色技展示行与「释放技能」按钮删除（角色技能 A 型就绪即自动释放、与手动大招开关解耦，手动大招仅约束大招本身），冷却状态改由塔身冷却环表达。
- 怒气条（提交 11 胶囊化；0.8.11.1 收格）：脚下小胶囊 **38×10**（圆角端=半圆；底槽深色 + 内填 未满灰蓝 / 满怒金色；**满怒放大呼吸 + 金色外发光**）；spine 角色条中心 y30、普通 y28——**收进人物所在格**（格半高 40，下缘 ≤ 格底 y40，B-051）。
- 角色技能冷却环（提交 11 修复，v0.20.22）：塔身外细环按剩余冷却比例收缩、就绪隐藏；A 型冷却递减期间每帧重绘（B-050 脱战冻结修复）；spine 角色环半径 r40、普通 r25。
- 技能释放入口（v0.20.32 定稿，纯文档登记）：**现网仅大招手动**——选中塔底部详情面板（手动模式）「释放大招！（R）」按钮 + R 键直发 + 塔身满怒金色脉动提示；职业技能 / 角色技能 A 型 / B 条件被动一律不出现释放按钮或动作行（维持 0.8.11.0 移除技能区口径）；「可手动」登记制 + 槽位纪律（R = 大招恒主位，可手动技能限 1 位 Q 预留）；塔身就绪胶囊快放 = ✅ 0.8.11.9 落地（v0.20.33：手动模式满怒塔顶金胶囊 + R 字，点胶囊直发不改变选中；点击区 r13 与选中区 r32 解耦；v0.20.34 / B-057：手动开关逐帧即时生效 + 胶囊中心顶栏避让（≤154 收至 y=92）与命中区同源）；全局就绪聚合提示、区域型拖拽落点仍 v2 预留未开发（细则见档头 v0.20.32/33 变更行）。
- 布局遮挡与让位（0.8.11.1 / B-052）：顶栏贴顶 0..80、卡条常驻底部 640..720——战场 **row1..7 全部可建**（战斗禁建区 y≤80 / y≥640）；塔详情面板与结算展示期间底部卡条收起让位；剧情对白面板上移 448..638（不遮卡条、对话期保持可建造）；开局 BuildHint 一次性顶部提示（y88..142，淡入 5s 停留淡出）；`ch01_s03` 路径由原 row8 底行整体上移（row7 全程 → col15 上折 → row2 左折 → col13 上折 → row1 右出图），建造位 10 个与禁建格同步重排避让（MapValidator + FlowRunner 布局规范通过）。
- 结算页（自上而下）：胜利/失败横幅（绿/红）→ 奖励明细（经验、掉落、新武将）→ 目标勾选与星级（阶段 10 预留位）→ 结算转盘（提交 2）→ 下一关 / 重试 / 返回大厅。
- Boss 预警横幅：红色警示 + 登场演出（沿用 v0.15.0）。
- **局内 HUD v2 定稿（v0.20.41 / 2026-09-11 用户确认，`ui_battle_hud_v2.png` / `src/ui_battle_hud_v2.html` 同源；战斗 UI 换肤以此稿对齐）**：**深墨绿 + 暖金局内皮肤**（底 `#13241b` / 描边金 `#c9a35c` / 文字 `#f2e7cd` / 数值 `#ffd479` / 生命 `#4ec97e`、≤30% 危急 `#ef5b52` / 主行动 `#ffcc00`；**地图与道路本身不改色**）——①**顶栏（贴顶全宽 0..80）** = 关卡名胶囊 + 金币 / 基地生命 / 波次三胶囊（**图标 + 数字、无文字标签**：`icons_battle/coin.png` / `base_hp.png` / `wave_flag.png`）+ ☰ 菜单按钮（点开收录 继续 · 设置 · 重开 · 退出，ESC 同效）；**「开始第 N 波」按钮取消**——出怪 = 点击**敌人出口道路上的波次旗帜图标**（56px，小箭头指向出口；Space 同效；自动开波开启后无需点击）；②**底栏建造位（640..720 常驻）= 关卡出战上限枚 76×76 槽**（本例 4、≤6；未满显虚线空槽；卡面 = 46px 圆头像 + 右下角金币费用胶囊，金币不足 = 灰头像 + 红框红字）→ 竖虚线分隔 → **军需槽**（不随选中态变化）；③**塔详情整合进底栏**：选中塔时建造位**整块替换**为同规格 5 卡（头像 / 伤害 / 攻速 / 升阶 / 回收）——升阶 · 回收 = **圆盘图标 46px**（升阶金 `#ffe08a→#d9a53a`、回收红 `#ff9d7d→#c2604f`）+ **右下角金币费用胶囊**（语言同建造位；满阶灰显不显费用、金币不足费用红）；详情**只显示伤害与攻速**（图标 + 数值；伤害按攻击类型换 `dmg_physical` / `dmg_magic` / `dmg_true`），**不显示等阶、无射程数值**（选中即显示射程圈）、**取消选中金圈**；④**地图塔不显示名称**；阶级角标（1 / 2 / 3，**0 阶不显示**）置于塔头像右下（后续可换图标堆叠）；⑤**军需槽**（局外选带配置、局内花金币购买；默认 2 槽 / 科技 +1 → 3）：未购 = 虚线圆槽 + 右下角金币费用胶囊（**点击购买**）、已购 = 高亮 + 右上 ✓ + **左上剩余次数**（点击使用）、**达限购次数置灰禁用**、锁 = 科技未解锁；⑥概念稿内**左侧大虚线胶囊为提示示意、不开发**。
- **局内 HUD 概念稿（v0.20.20 定稿（**已由 v0.20.41 / v2 取代，保留为历史存档**）；v0.20.22 / 提交 11 移除技能副行；v0.20.29 / 0.8.11.5 按 0.8.11.1 起运行版式同步重渲，`ui_battle_hud.png` / `src/ui_battle_hud.html` 草稿 v0.4，战斗 UI 换肤以本稿对齐）**：**顶栏贴顶全宽 0..80**（深海军蓝渐变 + 下缘 2px 细线，不再悬空遮挡 row1）= 章节名 + 金币胶囊（`icons_battle/coin_crop.png`）+ 基地生命胶囊（`base_hp_crop.png`，**数值常态绿 `#0e9f58`、≤30% 危急变红 `#e5484d`**）+ 波次胶囊（图标虚线圆占位待美术）+ Kenney 按钮行（黄「开始第 N 波」/ 蓝「军需」带占位图标 / 灰 暂停·设置·重开 / 红 退出）；**底部建造卡条（640..720 常驻，选中塔 / 结算展示时收起）** = 半透明深色底（顶边 2px 浅蓝细线）内横向居中排列**横版武将卡 124×68**（关羽/张飞/刘备：左 48px 圆立绘头像 + 右 武将名 / Lv（蓝）/ 费用（金）），按住卡片拖出至空地建造；战场示意 row1..7 全可建，已建塔（张飞 · 阶 2）脚下 **38×10 怒气胶囊收格三态**（空槽常驻弱显 → 积怒灰蓝填充 → 满怒金色放大呼吸，同运行版 0.8.11.1~0.8.11.2）；**塔详情面板**（点击已建塔 → 卡条收起、自底中部上展开）= 2px 浅蓝虚线（`rgba(160,205,240,.8)`）描边半透明面板（约 460 宽，圆角 16）：头像 · 局内阶数徽标 / 伤害·攻速·射程三格 / 仅「升阶」「回收」两钮——无技能区（大招与冷却不入面板，v0.20.22 口径）；交互说明与 HP 两态图例入稿上图例区。
- **战斗内设置弹窗（0.8.11.5 亮色化 B-055）**：战斗中顶栏「设置」点击弹出暂停遮罩弹窗（战斗暂停、改动即时生效并持久化，复用 SettingsPanel）；弹窗外框 = **Kenney DIALOG 语言**（`DIALOG_PANEL #f2faff` 底 / `DIALOG_BORDER #1c9fd7` 3px 描边 + 7px 粗下边 / 圆角 18 / 底部投影 18，同 MainMenu「新的征程」确认弹窗），遮罩深蓝 `alpha 0.62`，内容区 margin 26/18/20，底部「关闭设置」= Kenney 蓝按钮（220×48、白字 18px、水平居中）。
- **军需面板（v0.20.21 概念初稿，`ui_supply.png` / `src/ui_supply.html`）**：顶栏「军需」蓝钮（Kenney + 虚线圆图标占位）点击打开 908px 亮蓝白面板，**战斗不暂停**——遮罩自顶栏下缘起（顶栏按钮行保持可见可点），开启态「军需」按钮金色高亮示意；面板 = 蓝标题条（「军需」24px 白字 + 「即买即用 · 战斗不暂停」chip + 金币胶囊 + 圆 ✕）→ 主体左右分栏：左 476px **军需卡列表**（行式卡 = 44px 图标虚线圆占位 + 名称 16px + 一行描述 11.5px 省略兜底 + 右列费用金 chip + 状态 tag；自上而下四卡 = 修整[满生命灰禁·限 1 次] / 火攻[限 1 次] / 擂鼓[悬停/选中金框演示] / 缓兵[限 2 次]；**列表可纵向扩展，超高滚动兜底**，扩展规则以列表内虚线提示行说明）+ 右 372px **详情预览卡**（54px 图标占位 + 名称 20px + tag 行 + 描述 + 效果/持续/剩余三格虚线盒 + 规则提示金底虚线 + 底部「购买」黄钮 190×46 含费用）→ 底栏虚线说明条（交互说明）。**交互口径：悬停卡片 = 右侧即时预览（跟随）；点击卡片 = 金框固定选中（悬停移开不丢）；按「购买」立即生效——无二次确认弹窗（NUMBERS 10.9「不加购买确认」即买即用规则不变）**；金币不足按钮置灰、限购次数用尽卡片灰关；军需图标待美术（虚线圆占位）。**0.8.16 重构口径（2026-09-10 定稿，见 DESIGN_REVIEW §12.6.2 / NUMBERS 10.15 / 本档 v0.20.40）**：由「金币即买即用」改**「购买 → 军需带 → 择时使用」**——购买入带**不立即生效**、点击带内条目使用、**未使用结算作废**；面板新增**带位区**（槽位默认 **2**、科技 +1 → **3**）与 **6 件军需池**（+ 掷石齐射 / 犒军，坚壁 / 伤药 / 火油罐移除）；军需**解锁 / 强化**（军功）入口版式随 0.8.16 落地前另行设计——本版仅同步口径。
- **胜利 / 战败结算与结算转盘三屏概念初稿（v0.20.23 待拍板，`ui_victory.png` / `ui_wheel.png` / `ui_defeat.png` + `docs/ui_concept/src/*.html` 同源；数值与实现同源 = `Main._on_victory/_on_defeat` → `ui.show_result`、`SettlementWheel.gd` 门槛与奖池、难度材料倍率）**：胜利结算页 1160×656 亮蓝白面板——绿横幅「胜利 · 城防未破 · 波次全清」（绿=胜利语义，与战败红横幅相对）→ 元数据行（章节 / 难度 / 首次通关 / 基地生命 12/20 / 剩余金币 186 / 波次 6/6）→ 主体左右分栏：左「奖励明细 · 已全部写入存档」= 4 名出战武将经验行（圆头像占位 / 姓名 / Lv / +经验）+ 材料掉落（黄巾布 ×2 · 首通）+ 新武将加入（貂蝉 · 舞娘 · 已入阵容）+ 科技点 +2（首通，难度材料倍率 ×1.0）；右「本局战绩」（防守波次 6/6 全清 / 基地生命 12/20 残存 / 出战 4 名全数存活）+「挑战目标 · 星级」= **阶段 10 预留位（虚线占位，不参与本期结算）**→「胜利特典」金色卡（剩余金币 ≥150 可抽 1 次 · 仅 1 次 · 奖励直接入档）+「转盘抽奖」黄钮 → 底栏 重试本关（蓝）/ 返回选关（灰）/ 下一关：宛城之围（黄）。结算转盘页 1180×656——金色标题条（结算转盘 · s04 东郡驰援 · 剩余金币 186 + ✕）；左 = 352px 六扇区圆轮：conic 色扇区（黄巾布×2 黄 / ×5 绿 / 碎片×10 蓝 / ×20 紫 / 科技点×1 橙 / ×10 珊瑚）+ 白分隔线 + 金 rim 九铆钉（45° 步进）+ **顶部固定指针**（轮盘 −30° 校准旋转，演示命中扇区停至指针下；命中标签白环高亮）+ 中央金色 ★ 抽钮；右 =「奖池与权重」6 行（命中行金框高亮，色点同扇区色）+「抽取规则」（胜利结算页开启且剩余金币 ≥150；每局仅 1 次，抽取后按钮置灰「转盘已抽取」；结果即时写入存档，关闭/重开不丢失）+ 结果横幅（恭喜获得：黄巾布 ×2 · 已入账 → 背包 · 材料）。战败结算页 820×600——红色横幅「战败 · 基地陷落 · 波次 5/6」+ 惨败场景（深色）+ 战况行（坚持波次 5/6 / 基地生命 0/20 红 / 结算转盘未开启）+「本局未结算奖励 · 全部作废」三卡灰显（材料掉落 ×2 作废 / 出战经验 4 名武将作废 / 首通解锁貂蝉作废）+ 失败规则条（本局经验/掉落/解锁/科技点一律不写入存档，中途退出、崩溃同处理；结算转盘仅胜利后开启、失败作废；已拥有遗物永久持有、失败不扣除库存 v0.33.1）→ 底栏 返回选关（灰）/ 重试本关（黄，重试 = 直接重新进入本关）。立绘 / 头像 / 材料 / 扇区图标均为虚线圆美术占位（待 ART_ASSETS）。

## 11. 设置（SettingsPanel）

- 分区（画面 / 音频 / 玩法）折叠分组；开关 + 滑块控件；当前值高亮（蓝色），改动即时生效并持久化（沿用 settings.cfg）。

## 12. 游戏百科（EncyclopediaPanel）——阶段 8 提交 9 前置设计（概念图存档 v0.33.8）；v0.20.18 按 ui_encyclopedia.png / ui_encyclopedia_enemy.png 对齐（v0.20.17 首轮落地、v0.20.18 修分段按钮/头像配色/默认选中/渐变渲染）

> 只读信息中心，完整设计见 [ENCYCLOPEDIA.md](ENCYCLOPEDIA.md)；本节省排版层约束（Kenney 亮蓝 + 快乐体，与其余大厅屏一致）。概念图 `../ui_concept/ui_encyclopedia.png`（武将图鉴）/ `ui_encyclopedia_enemy.png`（敌人图鉴）。

- **入口**：大厅侧栏「百科」按钮（位于「设置」与「返回主菜单」之间，见 §4）；提交 9 开发时接入 GameHub，新增按钮不改侧栏结构。
- **整屏结构**：顶栏标题「游戏百科」+ 状态标签（全量图鉴 / 只读不写档）→ 顶部大分段按钮「武将图鉴 / 敌人图鉴」互斥切换（概念 .segbtn：**选中 Kenney 蓝底白字 / 未选灰底深字**，186×46，同科技树 .tabs 口径）→ 敌人图鉴视图顶部另含**章节选择行**（下拉框样式，v0.20.1 用户拍板：第一章可点、后续章节灰显「敬请期待」，与地图选关共用预留清单，见 §4；仅敌人视图展示）→ 中部左右两栏（左列表 396px 白卡 .list + 右侧详情卡 .dp）→ 武将图鉴视图底部另有**数值模拟器整行卡**（概念 .sim）。
- **武将图鉴视图**：左列表卡（概念 .list）= 头部「武将列表」+ 计数「9 名 · 含未拥有」+ **2px 虚线分隔**（DashedLine 自绘，StyleBox 不支持 dashed 边框）→ 9 将 2 列网格卡（概念顺序 CHARACTER_ORDER：刘备/关羽/张飞/黄忠/皇甫嵩/貂蝉/周仓/赵云/诸葛亮；卡 = **概念九色圆头像**（首字，CHARACTER_AVATAR_COLORS 键映射 avatar_label：金/红/蓝/绿/紫/橙/棕/粉/青）+ 姓名 + 副行「**职业名 · 打法词**」（CHARACTER_FLAVOR 字典：辅助/斩将/士气/压制/稳定/鼓舞/抗线/越战越勇/控场）；**默认选中张飞**（概念稿 .lcard.sel）；选中金框高亮 apply_light_selectable；超高滚动兜底）→ 右侧详情卡：头部（概念 .dhead）= 头像 52px + 姓名 21px + chips（职业名·定位短词=职业 tags 中文词，与养成职业页同源 / 称号 · X）+ 右侧「获取方式」实值 + 头下 2px 虚线分隔（#d7e9f5）→ 页签「基础 / 技能 / 转职 / 信物 / 特性」（页签标题=概念文字 set_tab_title 显式设置，内部滚动节点名不进标题；页签体统一纵向 ScrollContainer 滚动兜底、内容按概念 `.tabbody` padding 外包 MarginContainer 内边距 14/10；基础 = irow 键值行（概念 .rows .irow：职业定位 / 等级上限 1~30）+ statrow 四统计卡（概念 .stat：伤害 Lv1 / 攻速 秒口径 / 射程 / 建造费用=金）+ 角色简介 + 获取方式实值；技能 / 转职 / 信物 / 特性与养成面板同源）。
- **数值模拟器（仅武将图鉴视图底部整行白卡，概念 .sim）**：行 1 = 标题「数值模拟器」+ 等级 −/＋ 步进钮（1 ~ 30，越界置灰）+ 转职下拉（.pick 白底蓝边圆角）+ 局内升阶 0~3 四枚按钮（选中 #1c9fd7 蓝底白字 / 未选浅蓝底深字）；行 2 = `=` + **三枚金色结果卡**（伤害 / 攻速（攻击间隔秒口径）/ 射程）+ 右侧「预览不影响存档」tag + 同源副注。只读不写档；切将时等级=1、升阶=0 重置。
- **敌人图鉴视图**：顶部章节选择行（下拉框样式，见上）→ 左列表卡 = 头部「敌人列表」+ 计数 + 虚线分隔 → 当前章节敌人 **2 列网格卡片**（第一章 7 敌；**默认选中黄巾渠帅·张梁**；卡 = **EnemyAvatar 方形对角渐变头像**（自绘：白描边圆角方 + 135° 渐变 + 首字白字，七色 ENEMY_AVATAR_GRADIENTS；渐变纹理 setup 预生成 ImageTexture 缓存绘制，B-048）+ 名称 + 副行「**定位词 · 打法词**」（ENEMY_LOCATIONS / ENEMY_TACTIC_SUB：炮灰·近战 / 快速·机动 / 精英·头目 / 远程·高漏伤 / 坦克·高血量 / 支援·治疗光环 / Boss·召唤）；v0.17.1 B-023 起横向铺满不塌陷）→ 右侧详情卡：头部 .dhead（渐变头像 + 名称 + chips（① 定位词 Boss=红底红字 / 其余蓝底蓝字 ② 阵营 · 首现关卡）+ 右「出现关卡」摘要（单关 = sNN · 关名 / 多关 = sNN~sMM · 共 N 关；未配置灰字）+ 虚线分隔）→ 详情体滚动卡化：**statrow 五统计卡**（生命 / 速度 / 护甲 / 漏怪伤害 / 击杀奖励=金）→ 特殊行为卡（玩家向文案）→ **各档难度卡行**（概念 .diffrow，随 `difficulty_presets` 自动扩档：每档一卡 = 难度名 + 生命 ×倍率 + 换算 HP 大字，标准×1.0 绿 / 困难×1.4 红 + 其余项注明「不随难度变化」）→ 口径注脚 → **多关登场明细**（单关不重复；Boss 召唤来源注明）。
- **滚动兜底**：内容超高时列表 / 详情区用可滚动容器（规范见 v0.14 ③），概念图一屏完整无滚动条。
- **解锁规则**：当前全量可见；「发现解锁 / 难度面板解锁」随阶段 10 接入（见 ENCYCLOPEDIA §2.3 / §3.2）。

## 13. 验收标准（阶段 8·提交 4）

- 地图选关：第一章 8 关 **2×4 网格一屏展示、无滚动条**；章节一行可点，锁定章节不挤占主区。
- 武将养成：基础信息一屏可见；转职 / 信物 / 特性（预留）经页签可达（升星页签随 v0.30.0、遗物页签随 v0.33.1 移除——遗物为队伍级：选带在编队页 §7、库存与效果在背包页 §9）；新增页签不改布局。
- 出征链路（v0.33.1）：选关页「出征」直达编队（无弹窗）；编队页「返回选关」保留关卡与难度；「确认出战」二次确认弹窗展示关卡/难度/武将名单/遗物清单，确认后才进入游戏；再次出征自动预填上次编队（武将 + 遗物）。
- 全局：语义色收敛为共享色板常量，各面板无私有颜色魔数；关键信息（金钱、科技点、材料不足、锁定、Boss 预警）颜色语义正确。
- 建造（v0.33.3）：拖拽为唯一建造方式——卡片拖出后虚影 / 攻击范围圈 / 绿红落点格反馈齐全；松手直建、右键与 ESC 取消；绿色 + 与预锁位不再出现。
- 回归：流程测试（解锁 / 编队 / 布局规范）与冒烟测试全绿，一键通关与结算链路不受影响。

## 14. 不做什么

- 不做正式美术素材（立绘 / 图标仍为程序化占位，见总纲【远期】）。
- 不做移动端/窗口自适应（分辨率固定 1280×720）。
- 不重做战斗场景布局（`Main.tscn` 已有基础，仅按本规范调整色板与信息层级）。
- 星级展示仅预留 UI 位，挑战目标系统本体在阶段 10 开发。

## 15. 鼠标光标规范（Kenney Cursor Pack，阶段 8·提交 12 排期，v0.20.26 建档）

- **素材与定稿配色（v0.20.26，用户拍板 2026-09-08「按你推荐来」）**：Kenney Cursor Pack 1.1（CC0，https://kenney.nl/assets/cursor-pack ）`PNG/Outline/Default` 32px；原图 = 近白芯 `#f0f0f0` + 纯黑描边，观感生硬 → 定稿 **B「Kenney 浅蓝」**：芯 `#cdeffb`（≈PALE）/ 描边 `#14538a`（=STROKE）——浅色弹窗靠深蓝描边保轮廓、深色战场靠浅蓝芯保可见。重着色配方 = 按原图灰度明度线性映射双色（v = (r+g+b)/3/240 clamp 0..1，outline→fill 插值），形状 / 描边结构 / 透明度保留；对照图 `docs/ui_concept/ui_cursor_palette.png`（原版 + A/B/C 候选 × 浅色大厅底 / 深色战场底，2026-09-08 归档）。备选：A 白芯 `#ffffff` + 描边 `#167da8`；C 亮蓝芯 `#1c9fd7` + 描边 `#0a4a75`（深色底偏暗，不采用）。
- **图标映射**：

  | 场景 | 图标（Outline/Default 32px） | 说明 |
  |---|---|---|
  | 全局默认指针 | `pointer_toon_a` | Q 版指针，贴合手绘 Q 版风格；备选 `pointer_a` |
  | 按钮 / 卡片 / 页签 / 可点项 hover | `hand_point` | 替换系统手型，ui_theme 封装统一出口 |
  | 拖拽（建造卡拖出 / 可拖动项） | `hand_closed` | BuildManager 拖拽态 |
  | 战场 / 地图编辑器画布 | `cross_large` | 替换 `CURSOR_CROSS` |
  | 地图编辑器笔刷 | 路径 `drawing_pen` / 装饰 `drawing_brush` / 擦除 `drawing_eraser` / 禁建 `cursor_disabled` | 随笔刷切换 |

- **热点（hotspot）**：指针 / 手型 = 引导尖端（箭头尖 / 食指尖）、准星 = 几何中心、笔刷 = 笔头；实现时逐枚按 alpha 定位并登记本表。尺寸 32px 基准；Windows 高 DPI 实机校验是否需要 64px（Double 套）。
- **应用点（提交 12 落地，素材入 `assets/ui/cursors/`）**：全局默认 = project.godot `[display]` 光标配置或启动时 `set_custom_mouse_cursor`；悬停手型 = `UITheme.apply_kenney_rect_button` / 卡片 / `apply_light_selectable` 统一封装（禁止散点设置）；拖拽 = BuildManager 按住拖出期间；编辑器 = `addons/map_editor/map_editor_window.gd` 画布与笔刷循环。
